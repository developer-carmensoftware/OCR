# Changelog — 2026-06-11

_Backfilled from git history. Times = commit time (ICT, +0700)._

- `09:39` Account-mapping components + logic for AP invoice processing (`7f4fe7c`).
- `09:52` Prepend `/carmen` to Carmen return URLs in production builds (`dfeb88e`).
- `10:07` Correct skeleton/empty states in credit-card review steps (`f5dbbf9`).
- `10:42` Credit-card extraction persistence service + frontend API client (`0af6ad7`).
- `10:42` Unit tests for `bank_detect` helper (`a75a204`).
- `11:06` Thread draft id to client so `submitted_at` is set (`b77e5d7`).
- `11:12` Generate graph structural analysis report for OCR codebase (`51c8ec5`).
- `17:29` Repair misread document figures, auto + manual (`5b26438`).
- `17:32` Regenerate graph structural analysis report (`45bdc25`).
