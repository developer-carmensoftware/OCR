# 2026-08-10

## DB

- `10:44` **`supabase db push` to prod failed with "Remote migration versions not found in
  local migrations directory" (`20260727000000`).** That version was applied to prod but its
  file was never committed — it is in no branch, no stash, and the 07-27 changelog. Recovered
  the SQL from `supabase_migrations.schema_migrations.statements` and restored it as
  `supabase/migrations/20260727000000_perf_log_retention.sql` (90-day `performance_logs` purge
  function + `perf-log-purge` cron at 03:40 UTC + two unused index drops). Restoring the file
  rather than `migration repair --status reverted` keeps the prod schema tracked in git; the
  repair path would have made the error go away by forgetting the change.
- `10:44` **Applied `20260730000000_order_reviewer_role.sql` to prod.** It was the only pending
  migration; local and remote history now match end to end.
- `10:44` Incidentally closes the `changelog/2026-07-27.md:173` open question — `20260721000000`
  and `20260721120000` **are** applied to prod.

## Uncommitted

- `supabase/migrations/20260727000000_perf_log_retention.sql` — recovered file, not yet committed.
