# 2026-09-04

## The queue's empty states stop being placeholders

`09:40` Reported on the screenshot: the caught-up screen *"ดูไม่เป็นทางการ และยังไม่เหมือน
ระบบ SaaS อื่น ๆ"*. It read:

> **All clear**
> Statements forwarded to aragent+1ad4e0b6@carmensoftware.com appear here.

Five faults, four of them the repo's own rules:

1. **The address was prose.** 40 characters of identifier set in body copy, unspaced and
   unbreakable, wrapping mid-address — while `NotSetUp`, two branches away in the same
   file, gives that same string a mono field and a copy button.
2. **One screen answered three questions.** `clearFilter = review || success || today` sent
   all three to one card, so an empty **Posted** chip said "All clear". It has not posted
   anything; that is not all clear. The comment at `dict.ts:803` shows this was already felt
   and answered by making the sentence vaguer, which is why it had stopped saying anything.
3. **No next step** — against `DESIGN.md:333` (*"an instructional empty state that teaches
   the next step"*). Since yesterday's `All` chip this was also a genuine dead end: the dev
   BU with **101 never-charged rows** lands on Today, is told there is nothing, and had no
   way to reach its own log.
4. **Two unrelated empty states on one page** — that card, versus a bare grey
   `<p>Nothing here yet.</p>` for Not posted and All.
5. **A shadow on a card with nothing to click** — against `DESIGN.md:238`'s Flat-By-Default
   rule (*"a shadow is a promise that something is interactive"*).

### Frontend

- `pages/ReviewQueue.tsx` — `AllClear` and the bare paragraph are replaced by one
  `QueueEmpty`, driven by an `EMPTY` map that sits beside `FILTER_LABEL`. Five states, each
  with its own title, body, icon and tone. `clearFilter` is deleted: a boolean could not
  express "the tick is earned" once there were five chips, and in the map each state's tone
  sits next to its words so the two cannot drift.
- **The tick is kept only where it is earned** — `review` and `today`, the two states that
  mean nothing is owed. `success`, `unposted` and `all` get a neutral glyph in a muted well.
  Bare glyph for a semantic colour, glyph-in-a-well for a neutral one, is the convention
  `orders-empty-icon` already set on the order history.
- **The ingest address is gone from this screen** (kept on `NotSetUp`, unchanged). It is
  also what lets each body be specific again: the old sentence was vague because it had to
  stay true with `auto_post` on *and* off, and none of the five replacements names
  forwarding or approval at all, so none of them can go stale that way.
- **One action, `View all activity`**, shown when `filter !== 'all' && counts.all > 0` —
  the only route the page did not already offer. Deliberately not a second
  `Upload documents`: that button is primary in the bar directly above, and repeating a
  primary CTA inside an empty state is what makes a screen read as filler.
- The icon markup moved into a wrapper `<span>` so a tone can draw a well, which an SVG
  cannot draw for itself; colour still reaches the glyph through `currentColor`. Applied to
  the error state here and to `ReviewDocument.tsx`'s `.rd-gone` card too, so the page has
  one icon idiom rather than three.
- `styles/pages/review-queue.css` — `.rq-empty-icon` base is now the wrapper
  (`display: grid`), `.rq-empty-icon--calm` is the new muted well. `--text-4` is correct
  there and only there: `DESIGN.md:176` calls that tier decorative-only and the glyph is
  `aria-hidden`; the body copy beside it stays on `--text-3`. `npm run contrast` already
  covers the pairing — `text-4 on muted`, 3.23:1 against a floor of 3.
- `box-shadow` moved off the shared `.rq-empty, .rq-intro` rule onto `.rq-intro` alone,
  which always carries a button. `.rq-empty-tab` deleted — nothing renders it now.
- `i18n/dict.ts` — out: `review.allClearTitle`, `allClearBody`, `allClearBodyNoAddress`,
  `emptyTab`. In: five `empty*Title` / `empty*Body` pairs plus `review.viewAllActivity`,
  both languages.

### Tests

- `ReviewQueue.test.tsx` — the six assertions on the old copy move to the per-chip titles.
  New: the address never appears on a merely-empty chip; no empty state names forwarding or
  approval (the `auto_post` trap, now asserted rather than described); an empty Not posted
  gets a real card with the calm icon and **not** the tick — the rule the source stated and
  no test pinned; `View all activity` switches the chip to `all`, and is absent when there
  is no history to see.
- 70 passed in this file, **643 passed** across the suite. `tsc`, `eslint` and
  `npm run contrast` clean.

### Docs

- `07-human-in-the-loop.md` §14 addendum, decisions #68–#71, and the "Not changed" line
  corrected — #63's "plain line" no longer exists. §6's `[ Recently posted ▾ ]` panel stays
  unbuilt and stays deferred; #70's link answers the same dead end for the price of one
  button.

**Pending release note** (batched — no `VERSION` bump): *"The Credit Card automation page
now says what each filter means when it is empty, and offers a link to the full activity log
instead of a dead end."*

## A manual scan names who ran it

`09:55` Requested: *"scanned and posted by hand" ตรงนี้อยากให้เป็น "scanned and posted by
{username}" ไปเลย เข้าใจกว่า.* Correct — "by hand" answers *how*, which the reader can already
see from the row, and not *who*, which is what they were reading the Message column for.

**No migration.** `credit_cards.carmen_user_id` is already stored (33 of 42 submitted rows on
dev carry it), and `tenant_lookup.username_map()` already bulk-resolves that opaque id to a
username out of `ocr_sessions` — it is what the admin user-usage page reads. One extra query
for the whole window, and every existing row is covered.

### Backend

- `routers/credit_card_activity.py` — `list_activity` resolves the scanners of the manual
  window in one `username_map()` call and hands each name to `_manual_row`.
- `schemas/email_automation.py` — `ActivityRow.posted_by_name`, **deliberately not reused
  from `reviewed_by_name`**, which it would otherwise fit. That is a stored ledger column on
  `email_documents`, written once and kept for ever, and nobody *reviewed* a manual scan.
  This one is resolved at read time and goes null once the session behind it is scrubbed at
  90 days. Two different guarantees; one field holding both is how a reader ends up trusting
  the weaker one.

### Frontend

- `QueueRow.tsx` — the manual branch names the person when we have one and keeps the old
  wording when we do not. `review.postedManuallyBy` (`scanned and posted by {name}` /
  `สแกนและลงบัญชีโดย {name}`) joins the existing `review.postedManually`, which survives as
  the fallback: printing a raw user id at somebody is worse than the vaguer sentence.

### Tests

- Backend: a manual row names its scanner; an unresolvable one names nobody and leaves
  `reviewed_by_name` null too. `_db` grew a sixth `db.execute` response for `username_map` —
  always supplied, consumed only when a manual row actually carries an id, so the positional
  `side_effect` cannot shift under the existing tests.
- Frontend: the named row and the fallback row, both asserted on the rendered sentence.
- **1245 backend · 645 frontend**, `tsc`/`eslint`/`ruff`/`prettier` clean.

## The Message column says the thing once

`10:35` Reviewed the **Not posted** tab on a real BU's screenshot. The column's whole job is
to tell a reader why a row did not become a JV, and it was refusing to:

| Printed on screen | Held in `error_message`, reachable only by hovering |
|---|---|
| `Carmen refused it` × 8 identical rows | Carmen's own verdict — the closed period, the bad dept |
| `already handled` | *"already submitted"* vs *"already waiting for review"* — two different next moves |
| `tax ID belongs to another company` | the tax ID |
| `rejected by somchai - rejected` | the reason somchai typed |

Three faults, one of them a real bug:

1. **The reviewer's typed reason was never on screen.** `QueueRow`'s rejected branch fed
   `{reason}` the `rejected_by_reviewer` *phrase* (`'rejected'`) instead of `error_message`,
   so every rejection read `rejected by <name> - rejected` — and the words a colleague left
   for the next person were tooltip-only. The reject dialog asks for them
   (`review.rejectReasonHint`, *"e.g. wrong company"*); nothing printed them.
2. **`tax ID belongs to another company` claimed more than the check establishes.** A BU's
   register holds an *array* of tax IDs, so a document carrying one that is not in it has
   failed to match — that is all. Same overreach in the stored detail
   (*"registered to another BU"*).
3. **`already handled` over a red `Failed` pill contradicted itself**, and covered both the
   already-posted case (nothing to do) and the twin-in-the-queue case (go look at the other
   row) with one vague phrase.

### The rule, after one wrong turn

First attempt joined phrase and detail — `duplicate — a copy is already waiting for review`,
`Carmen refused it — Code 1: …`. Rejected on sight as *"ไม่จำเป็นพูดซ้ำ ทำไมวกวน"*, and
correctly: a prefix that the detail immediately restates is a head the reader must skim past
to reach the part that differs. **The detail replaces the phrase; it never joins it.** The
i18n phrase is what stands in when there is no detail — a legacy row, or a Carmen refusal
with an empty body.

### Frontend

- `lib/reviewReasons.ts` — new `WITH_DETAIL`, deliberately two codes: `carmen_rejected` and
  `duplicate_document`. Those are the only ones whose phrase cannot tell two rows apart.
  Everything else keeps its translated phrase, and the two codes carrying a raw `str(exc)`
  from the PDF reader (`unreadable_document`, `wrong_pdf_password`) plus the paragraph-long
  `sender_not_allowed` stay on the hover title, where length costs nothing.
- `QueueRow.tsx` — the rejected branch prints `error_message` when a reviewer left one and
  drops the trailing clause when they did not. Two keys instead of one:
  `review.rejectedBy` (`reviewed and rejected by {name}`) and `review.rejectedByWith`
  (`rejected by {name}: {reason}`).
- `dict.ts` — `review.postedBy` becomes **`reviewed and posted by {name}`** /
  `ตรวจสอบและโพสต์โดย {name}`: the name is evidence that this document stopped at the queue
  and a person read it — the same job its sibling `scanned and posted by {name}` does for
  the manual path. `review.rcTaxIdMismatch` → `registered Tax ID does not match the
  document` / `เลขผู้เสียภาษีที่ลงทะเบียนไว้ไม่ตรงกับในเอกสาร`.

### The pill, and the colour that stayed

- `QueueRow.tsx` — `rejected` gets its own word. Both it and `failed` sit under Not posted
  and both stay red, but one means something broke and the other means a person read the
  document and said no; one pill for both had a BU counting somchai's judgement calls among
  its problems. New `review.statusRejected` (`Rejected` / `ถูกปฏิเสธ`).
- **Greying the duplicate row was tried and reverted.** "already posted to Carmen" names no
  problem — the JV exists, the copy is redundant — so `calm` looked right on paper. On the
  screen it was worse: *"มันทำให้สับสนกว่าเดิม และเกิดคำถามว่าทำไมมันไม่แดง"*. One exception
  in a column of red asks a question before it answers one. The words carry the nuance; the
  colour only says which pile the row is in.

### Backend

- `_carmen_verdict()` now returns the cell rather than a suffix to one: `Carmen: <what it
  said>`, or `Carmen refused it, no reason given (Code 1)` when it said nothing. The numeric
  code survives only in that second form — with no message it is the whole support ticket;
  with one it is noise in front of the sentence a human acts on.
- The two `duplicate_document` details are reworded to stand alone as the whole cell:
  `already posted to Carmen` and `a copy is already waiting for review`.
- `tax_id_mismatch` detail → `Tax ID {n} is not in this BU's register`. Not printed (support's
  copy), but it should not assert what it cannot show either.
- `scripts/seed_review_demo.py` follows the new verdict format.

**Known gap:** those two duplicate lines and Carmen's verdict are English for a Thai reader.
Carmen's words and a colleague's typed reason never could be translated — they are another
system's and another person's. The duplicate pair *could*, by splitting the reason code in
two; that is 6 files (admin filter list, notification map, both dictionaries, docs, tests)
for two strings, and it is not worth it until someone asks.

### Tests

- Frontend: Carmen's verdict replaces the phrase and the phrase is *gone*; the two duplicate
  kinds read differently; a rejection with and without a typed reason; a rejected row wears
  `Rejected` and not `Failed`; and a raw `PdfReadError` stays out of the cell.
- Backend: the verdict is asserted whole (`Carmen: Insufficient balance`) rather than by
  substring, plus a new test for the silent refusal that keeps its code.
- **137 `test_email_ingest_pipeline` · 651 frontend**, `tsc` clean. The full backend suite
  was not run to completion here — it hangs on the Sentry flush at teardown on this machine.

**Pending release note** (user-visible, batch at next version bump): the review queue now
shows Carmen's own reason for a refusal, tells the two kinds of duplicate apart, and prints
the reason a colleague gave when rejecting a document.

---

## Auto-post posts what is ready to post

`11:45` `auto_post` meant *"post everything that passed the gate ladder"*. So a statement
whose lines did not reconcile, whose GL rule the AI had invented thirty seconds earlier, or
whose fee VAT was assumed at 7% because the footer could not be read, reached the customer's
ledger with exactly the same silence as a clean one.

The system knew. `_review_flags()` runs on every document and had the sentence written down —
it just wrote it to `review_payload`, a column `_finish()` immediately nulls on the way to
`posted`, and nobody ever read it. That is the 2026-08-18 note (*"warnings ถูกเมินตอน
auto-post"*), parked at the time. Yesterday's #22 closed one half of the gap: a charged
reading is kept for a human instead of thrown away. This closes the other: a charged reading
is **questioned** before it posts.

**`auto_post` now decides whether a _clean_ document waits. It can no longer decide that a
doubtful one does.**

|  | Nothing flagged | Anything flagged |
|---|---|---|
| `auto_post = true` | posts | **parks** — was: posted |
| `auto_post = false` | parks | parks |

The gate is `_review_flags()` being empty — deliberately the same function that paints the
queue's Message column, whose empty case already read **Ready to post**. One predicate, so a
document the reviewer would have been shown a reason for cannot post behind their back. Two
definitions of "worth a human's eye" drift silently in exactly one direction.

### Backend

- `email_ingest_service.py` — the fork computes `flags` once and reads
  `if not auto_post or flags:`. `_park_for_review` takes them from its callers instead of
  recomputing, since the same list is now the decision *and* the row.
- **A fifth flag, `doc_no_missing`.** The only one about what we could not *check* rather
  than what we read: both duplicate guards key on `doc_no` — `has_submitted_doc` and
  `_already_pending` answer False without one — so an unnumbered statement forwarded twice
  posts twice into real books with nothing able to catch it. A flag and not a `_Skip`,
  because a reviewer can post one on purpose: it stops the machine, not the person.
- **`REVIEW_BACKLOG_CAP` and `_already_pending` stop reading `not auto_post`.** Both were
  written when a BU with review off could not park anything; #51 ended that and this makes
  parking routine there. They had become holes on the main path — an auto-post BU had **no
  backpressure at all**, so a dead credential would charge for every document while its
  queue filled, and a re-sent statement parked a second identical row.
- **The `mapping_incomplete` raise is deleted.** It was the `and auto_post` half of a branch
  whose other half already fell through to the fork. `mapping_missing` is a flag, so it parks
  under both settings — one path, rather than two that can drift on what the row says. New
  rows carry no `reason_code`; `reasonFor` names the unmapped fields from the payload, and on
  a `pending_review` row Review already outranked the Fix-mapping link. The code stays in
  `reviewReasons.ts` for the rows that already have it.

### Frontend

- `ReviewFlag` gains `doc_no_missing`; `QueueRow.reasonFor` gains its rung — below the two
  mapping reasons (a reviewer can post this one as it stands), above `warnings`.
- `review.reasonDocNoMissing` — *no document number* / *ไม่มีเลขที่เอกสาร*.
- **The auto-post copy described the wrong feature.** *"Post without review — documents post
  to Carmen as soon as they are read. Nothing waits here for approval"* is now *"Post clean
  documents automatically"*, and the hint **names what still stops**: a warning, a GL mapping
  the AI had to guess, amounts that do not reconcile, a missing document number. A person
  flipping this switch is trusting it; the copy has to say what they are trusting.

### Docs

`06-decision-log.md` #24, `07-human-in-the-loop.md` §15 (with the five decisions and what a
BU will notice), diagrams 6/7/8 in `02-architecture.md`, `CARMEN_INTEGRATION.md` §0.1/§0.2/§3.3
— the external contract, which promised automation with no human in it — plus the one-line
mentions in `01`, `03`, `04`, `05`, the README and `CLAUDE.md`.

### Tests

- **142 `test_email_ingest_pipeline`** (was 137) · **651 frontend** · full backend suite
  **1251 passed, 0 failed** (via `--junit-xml`, which gets past the Sentry-flush hang at
  teardown noted yesterday) · `tsc` clean.
- New: a clean document still posts unattended (the control); each of `warnings`,
  `unbalanced` and `doc_no_missing` parks it and never calls `post_gljv`; the backlog cap and
  `_already_pending` now hold an auto-post BU.
- Four existing tests changed meaning rather than breaking. Three assert the new outcome for
  a document the old rule posted: the AI-guessed mapping, and the two bank-detection cases
  where a rule over-reached — that warning exists to tell a reviewer which rule was too broad,
  and until now there was no reviewer. `test_auto_post_never_looks_at_the_backlog` is
  inverted, name and all.
- `_route` patches `_pending_count` by default: the cap is consulted on every message now, so
  a `db.scalar` left to a mock returns something that cannot be compared to a number.

**Fixed in passing:** `scripts/email_ingest_e2e.py` P19/P20 still asserted `failed` for the
duplicate and tax-ID cases, which have parked at `pending_review` since #22 shipped
yesterday — the script was never updated. P18 now accepts `posted` **or** `pending_review`
and prints the flags, because which one a real statement gets is a fact about the PDF in the
mailbox, not a pass/fail.

**Not run:** the end-to-end script itself, which needs the dev mailbox.

**Pending release note** (user-visible, batch at next version bump): automatic posting now
only covers documents read with nothing to check — anything with a warning, a guessed GL
mapping, amounts that do not add up or a missing document number waits in the queue for you.

---

## Nine banks through the real mailbox — and three guards that do not hold

`12:20` The section above ended with **"Not run: the end-to-end script itself"**. Run now, on
the live dev mailbox, against both configured BUs — not the two PDFs `email_ingest_e2e.py`
recycles, but all nine layouts in `Desktop/document_format` wrapped one-per-page into PDFs
and APPENDed with the `+tag` in `Delivered-To`, alongside a realistic pile of junk.

Four polls, **37 documents charged, ~$0.037**, nothing refunded:

| Run | BU | `auto_post` | Result |
|---|---|---|---|
| 1 | carmen | off | 9 parked, 0 failed |
| 2 | carmen | on | **8 posted — JV 1019-1026**, 1 parked, 0 failed |
| 3 | carmen | off | 9 parked (left in the queue to look at) |
| 4 | carmencloud | off (as configured) | 10 parked, 0 failed |

**What held.** All nine layouts read, `doc_no` extracted from every one, and `_resolve_bank`
named the issuer from the document itself in all nine — no rule-vs-document warning fired, so
the filename rules and the printed issuer agreed everywhere. Noise behaved identically with
`auto_post` on and off: no attachment → no row at all; a signature logo → `no_rule_match`; the
same message twice → **one** ledger row; `.xlsx` → `unsupported_attachment`; a disguised PDF →
`unreadable_document`; a locked one → `wrong_pdf_password`; untagged and unknown-tag mail →
`unrouted`, no row, no charge. A bank zip expanded correctly: the CSV dropped inside `_unzip`,
the summary PDF refused by name, and only the statement charged. `sender_not_allowed` fired on
carmencloud (which has `owner_emails` set) and named the addresses the mail actually carried.
Both `posted` and `pending_review` rows are visible in the queue, and every `posted` row
recorded its input-tax document with no note.

**The second factor is per-BU, and inverted between the two.** BBL and both SCB statements
print `0105555181506`, which is carmen's registered ID — clean there, `tax_id_mismatch` under
carmencloud. KBANK prints carmencloud's `0835553001610` — the reverse. Working exactly as
designed, and worth writing down: four of the nine samples belong to another company and can
never post unattended under carmencloud.

### Three findings, none of them fixed here

1. **`_already_pending` is unreachable for any document that also trips an earlier `_Skip`.**
   In `_run_document` the order is tax ID → `is_duplicate` → `_already_pending`, so a document
   that parks with `tax_id_mismatch` never reaches the guard whose own comment says *"a second
   identical row is the thing this check exists to prevent"*. Reproduced: BBL forwarded twice
   in two messages left **two identical `pending_review` rows** for `26079-0001954` in
   carmencloud's queue, both saying `tax_id_mismatch`. The guard only runs for documents clean
   enough to pass the two checks above it — the case where it is least needed. Moving it above
   the tax-ID check is the whole fix: it reads `bank_code` and `doc_no`, both known the moment
   extraction returns.

2. **`mapping_guessed` guards only the first document that needs the guess.**
   `fill_missing_mappings` saves what the AI invented with nothing marking it as AI-filled, so
   the next document carrying that payment type is not "guessed" any more and posts unattended
   — against the rule stated in `unmapped_payment_types`' own docstring (*"an LLM-guessed
   mapping must never post by itself"*, CARMEN_INTEGRATION §4). Demonstrated across runs 1 and
   2: KTC and SiamPay parked with `mapping_guessed`, then posted as JV 1023 and 1026 on the
   identical documents, on mappings no human ever confirmed. A source column on
   `bu_accounting_mapping_entries` that `_review_flags` reads until someone confirms would
   close it.

3. **A dead posting credential is invisible in the queue.** carmencloud's stored token is
   expired — `GET /Carmen.API/api/interface/accountCode` answered **401** during run 4 — but
   the call happens inside `_suggest_missing_mappings`, which catches `CarmenAPIError` and
   returns `{}`. So `mark_token_unverified` never runs, the bell says nothing, and the single
   symptom is one row reading `mapping_missing`, which sends the reader to the mapping page
   rather than to the credential. `carmen_token_verified_at` was already null. Carmen itself is
   fine — the browser's own session token fetched the same masters with a 200 minutes earlier;
   it is the stored unattended credential that is dead, and it is the only one the poll can use.

**Also seen, smaller.** `warnings` is not deterministic: the GHL invoice parked with *"the line
items do not add up"* in run 1 and posted in run 2 from the same PDF, so for that document the
auto-post gate turned on the model's variance rather than on the document — unlike `unbalanced`,
which is Python arithmetic and answers the same way every time. And the GHL payment type is
read as three lines run together (`TRANSACTION FEE 30-05-2026\nGross Amount 1,277,748.00
Baht\nDA00001562`), which `fill_missing_mappings` then stored as a mapping key: it carries a
date so it can never match next month's invoice, and `build_jv_rows` uses that same key as the
JV line's description.

### Operational note

The dev project's `email-ingest` cron fires every 10 minutes at the **deployed** backend
(`app.base_url` → onrender.com), which polls this same mailbox. Any live test has to pause it
(`select cron.alter_job(37, active := false)`) or the two race for the mail and the deployed
copy wins with main-branch code. `update cron.job` is denied to the app role; only the function
works.

### State deliberately left behind

Dev only. carmen: 9 `pending_review`, 8 `posted` (JV 1019-1026), 10 `rejected`. carmencloud:
11 `pending_review`, and seven per-issuer ingest rules added and kept so the queue and the
settings screen both have something real in them. The fixture mail is still in the `AR Agent`
folder, flagged `\Seen` — which does mean `email_ingest_e2e.py`'s `pick()` may now choose an
image-derived BBL PDF over the real one, since it takes the largest.

---

## The queue row's Actions cell holds one button

`14:20` Two subtractions from the activity table, reported as *"rq-amount … ไม่ต้องการ
มันรก"* and *"btn-icon rq-dismiss อยู่ตรงนี้ไม่ดี"*. Design: [`07-human-in-the-loop.md` §16](../docs/email-automation/07-human-in-the-loop.md).

### Frontend

- **The gross leaves the Document cell.** `QueueRow.tsx` appended `· 48,200.00` to the
  filename on pending rows, making it the fourth value in the one cell that was already
  truncating. §11 #33 had already declined an Amount *column* on the grounds that the reader
  opens the document rather than scans for it; the suffix carried that column's cost without
  its benefit. The figure is in the review dialog's JV total row, beside the legs it is made
  of. `.rq-amount` deleted.
- **`Details`, replacing the fix link + ✕ pair.** On the Review chip the Actions cell now
  holds one button, and both things you can do about a stopped row live in the dialog behind
  it: `[ Close ]` and the repair (`Open settings` / `Fix mapping` / `Reconnect`), with a
  quiet `Dismiss this row` and its hint in the body. The ✕ (#54) was the smaller target and
  the bigger action, labelled only by a `title`, sitting beside a link as though the two were
  equals.
- **Off the Review chip the Actions cell is now empty** — no dialog and no repair link.
  `_wants_a_human()` keeps every undismissed fixable row under `review`, so the copy showing
  under `Today` or `All` was the same row offering the same button twice; and under
  `Not posted` a fixable reason means the row was **dismissed** (§14 #67), where a repair
  button argues with the person who just put it away. The cause is still pointed at, by the
  chip carrying the amber dot — the 2026-08-28 lesson is untouched.
- **Dismiss is deliberately not in `CustomModal`'s cancel slot** — that is what Escape fires,
  and a keypress meaning "never mind" must not be the one that changes something. Still
  optimistic, still unconfirmed; the hint (*"Takes it off this list. The record stays under
  Not posted."*) is what #54's no-confirmation call was always relying on the reader knowing.
- **`stopText()` is now shared** by the Message column and the dialog, so a reviewer opening
  a row reads the sentence they just read rather than a paraphrase of it.
- `.rq-c-act` 10.5rem → 8.5rem: the cell now holds one word plus at most a glyph (`Review`,
  `Details`, `Open JV`), where it used to fit the longest repair label *and* the ✕.
  `.rq-dismiss` is now the dialog's quiet button.
- New keys, EN + TH: `review.actionDetails`, `review.detailTitle`, `review.dismissHint`.

### Fixed along the way

- **`CustomModal`'s focus trap cycled three refs**, so any control a caller passed in
  `children` was unreachable by keyboard: Tab from Cancel jumped to Confirm, Shift+Tab from
  Cancel wrapped to Confirm. It now reads the box's own DOM in order and filters on
  `tabIndex >= 0` (which keeps the password-reveal button out). Also fixes the notification
  bell's `Open JV` link, which had the same problem since it shipped.

### Verified

`npx tsc --noEmit` clean, `npx vitest run` 653/653, `npm run contrast` all pairs pass,
eslint clean on the touched files.

**Release note pending** (user-visible, batch at the next version bump): the queue's Actions
column is one button per row, and what happens to a row that could not post is explained
before you press anything.

---

## An AI-suggested mapping stops being the BU's rule before a human reads it

`15:10` Raised as a copy complaint: *"message ที่ว่า GL mapping missing ไม่ควรมีอยู่ แต่ควรจะขึ้นว่า
AI suggest mapping"* — every document with a gap already goes through the suggester, so the
queue naming a gap describes something that should not have survived to be named. Following
that through the pipeline turned up three findings recorded above, unfixed, plus one more,
all of which the wording was the visible end of.

### The row says who chose the account

`review.reasonGuessed` EN was *"GL mapping guessed"* — TH had said `AI เดาผังบัญชีให้` since it
shipped, so the two locales disagreed about whether this was a machine's proposal or a
missing value. Now `AI suggested mapping` / `AI แนะนำผังบัญชี`. `reasonMissingMapping` drops
the "GL": the fields are named after the colon, and the noun was doing nothing.

The flag itself stays. It is the fallback for a payment type the suggester genuinely could
not map, and a document holding one cannot post — deleting it would move the refusal to
Carmen, where it reads as a blank account rather than a sentence.

### The dead credential that was reading as a mapping gap (finding #3, closed)

`_suggest_missing_mappings` caught every `CarmenAPIError` and returned `{}`. So carmencloud's
expired posting token — 401 on `GET /accountCode` during run 4 — produced no suggestions,
every key came back unmapped, and the one symptom was a row reading *mapping missing*, which
sends the reader to the mapping page to fix a credential. `mark_token_unverified` never ran
and the bell said nothing.

401/403 now re-raises. The handler in `_run_document` was already right about it — it flags
the token and parks with `carmen_unauthorized` — and it would have caught the same 401 one
Carmen call later, at post time, anyway. Other statuses stay swallowed: a 503 says nothing
about the credential and must not unverify a token that is fine.

### The guess is no longer saved before anyone has read it (finding #2, closed)

`fill_missing_mappings` ran inside `_run_document`, so the AI's guess became the BU's own
rule the moment it was made. The second document carrying that payment type then found the
rule already there, `unmapped_payment_types` returned nothing, `mapping_guessed` was false,
`_review_flags` was empty — and with `auto_post` on it posted, unattended, on a mapping no
human had ever seen. KTC and SiamPay did exactly that across runs 1 and 2: parked, then
posted as JV 1023 and 1026 from the identical documents. Against the rule in
`unmapped_payment_types`' own docstring (CARMEN_INTEGRATION §4).

The write moved to the click that means a person confirmed it. Ingest keeps the pairs on the
ledger row instead — `review_payload.suggested`, `{key: {dept, acc}}` beside the `guessed`
names — and `ReviewDocument` seeds its pickers from them, so approving an untouched
suggestion is what saves it. **No new writer**: the review screen has called
`patchAccountingConfig` before posting since it shipped, and `overrides` arriving pre-seeded
is the whole mechanism. `approve_document`'s docstring gained the fourth "does not".

The cost is one suggestion call per document that arrives before the reviewer gets there,
instead of one per payment type. That is a text-model call against the price of posting to
someone's books on a rule nobody read, and `REVIEW_BACKLOG_CAP` still ends the queue.

`suggested` is on `ReviewDocumentDetail` only, not `_summarise`: the list endpoint and
`credit_card_activity.py` share that helper, and no row needs the codes.

### The ✨ badge survived being filled in by the AI

`guessed = !changed && guessedKeys.includes(row.key)` with `changed = row.key in overrides`
— but the background `suggestPaymentTypes` fill goes through `onOverride` like a keystroke,
so every AI fill rendered as the reviewer's own edit, Undo button and all. It only ever
worked for the ingest-time list, and only until anything touched the row.

`guessedKeys` is now live state on `ReviewDocument` (`aiKeys`): seeded from `doc.guessed`,
added to when `onOverride` fires with `byUser` false, removed when a person types over the
pick. `JvEditor` asks it directly and derives `changed` as the complement. Seeding from
`guessed` rather than `suggested` keeps the badge on documents parked before today, whose
codes went to the config the old way.

### A payment type that carried a date could never match twice

GHL's line description reads as three lines run together — `TRANSACTION FEE 30-05-2026 /
Gross Amount 1,277,748.00 Baht / DA00001562` — and that string is a **key**:
`canonical_payment_type` looks the BU's mapping up by it, and `build_jv_rows` prints it as
the JV line's description. So next month's invoice was a brand-new payment type: suggested
again, parked again, saved again as a rule good for exactly one document — and the date and
"Gross Amount" went into the customer's books as the line description.

`_clean_transaction_labels` takes the first non-empty line and cuts date tokens, in
`finalize_extraction` **after** the normalizers (which match on the raw label — `_is_summary_row`
reads "TOTAL"). Both entry paths come through there, so the key the browser shows and the one
the pipeline looks up are the same string by construction, and neither `cc_jv.py` nor its
`ccJv.ts` twin had to move. Digits otherwise survive: this BU has both `04-4100-03 SiamPay`
and `04-4100-04 SiamPay`, one character apart. A label that cleans away to nothing keeps what
it had.

Existing junk keys in `bu_accounting_mapping_entries` are left alone — nothing new lands
there now, and cleaning them is a SQL errand, not a code change.

### Verified

Backend suite green (`pytest -q`, exit 0), ruff clean. New: five cases on
`_clean_transaction_labels` (the three-line GHL label, an embedded date, the two SiamPay keys
that must not merge, a label that is only a date, an ordinary card type), the 401/403 re-raise
and the 503 that must not, and a pipeline case asserting the parked row carries `suggested`
and the config was **not** written. `npx tsc --noEmit` clean, `npx vitest run` 656/656,
eslint clean on the touched files. Three new frontend cases: the AI's pick reaches the
picker, the badge hands over to Undo on a keystroke, and approving an untouched suggestion
still saves it.

**Release note pending** (user-visible, batch at the next version bump): the review queue
says when the AI chose an account rather than calling it a missing mapping, and an account
the AI picked is saved only once you approve it.

---

## `18:30` The Review chip holds documents; the noise leaves the chips entirely

The queue's `Review` chip held 15 things on the dev BU and only 9 of them were documents.
The other six were `no_rule_match` and `wrong_pdf_password` rows — a *setting* each, one line
per attachment it stopped. Three releases in three days moved that pile between chips
(§11 #33 → §13 #53 → §14 #64), sorting on who can act and then on who paid; neither axis
separates "one setting, eleven rows" from "one document, one decision".

**The fix turned out to be subtraction.** Two rounds went into folding the refusals into one
row per cause — first behind a disclosure, then with the filenames on the row — and both came
out again (see "Two shapes built and removed" below). Once the noise arm takes 97 of a BU's
140 rows off the chips, what is left is a handful the table already knows how to draw.

Design: [`07-human-in-the-loop.md` §18](../docs/email-automation/07-human-in-the-loop.md),
decisions #82–#88. Decision log: [#27](../docs/email-automation/06-decision-log.md).

### Backend — `routers/credit_card_activity.py`

- `_wants_a_human()` is `status == PENDING` again, one comparison. Since #22 every charged
  refusal parks as `pending_review`, so the reason-code arm was serving pre-charge skips and
  legacy rows only.
- `_chip_expr()` swaps §14's charge arm for one keyed on the reason:
  `NOISE_REASONS = ("no_rule_match", "unreadable_document")` → `noise`, everything else falls
  to `unposted` and is listed there. **The noise arm is gated on `status = 'skipped'`** — the
  same code on a `failed` row is a crash inside the refund boundary (§13 #57 keeps the dot
  for exactly that). `UNCHARGED` is gone; `LEDGER_BUCKETS` is four.
- `FIXABLE_REASONS` → `ATTENTION_REASONS`, + `unsupported_attachment`. After the narrowing
  its only reader is `_attention`, so its name and its "stay in step with `FIX`" contract
  were both false. The addition closes a dead end: that code is in `NOTIFIABLE_SKIPS`, so it
  rang the bell and then appeared on no chip and lit nothing.
- **Both dismiss endpoints deleted.** Dismissal existed to stop `review` filling with rows it
  could never clear (§13 #54); `review` no longer holds them, and these rows now accumulate
  under `Not posted` like the failures and rejections beside them. `dismissed_at` stays as a
  column `_attention` reads — the 2026-09-03 migration back-dated every historical row as
  dismissed, and that is what keeps the pile from lighting the dot.

No migration. No schema change.

### Frontend

- `RowAction` renders `FIX[reason_code]` as a plain link on **every** chip. `Stopped` and its
  `Details` dialog are deleted: for a skipped row that dialog held the sentence the Message
  column already prints (`stopText` is shared — literally the same string), the filename the
  Document column already prints, and the dismiss. `.rq-c-act` back to 10.5rem, the width
  those labels needed before §16 took them away.
- `ReviewQueue` and `useReviewQueue` are back to rows and nothing else — no `causes`
  collection, no `CauseRow`, no `dismiss`.

### Two shapes built and removed

1. **A cause row with a disclosure**, its attachments as nested child rows plus a footer
   holding a bulk dismiss. Reported as *"ไม่อยากให้เป็น list ที่ซ่อนอยู่ใน dropdown"* — a small
   table inside the table, and a list a reader has to open is a list they do not read.
2. **The same row with the filenames printed on it**, three deduplicated names plus `+8 more`.
   Answered with *"ไม่เอาแบบนี้ เอาให้เหมือน skipped ของ tab all ไปเลย"*. Also correct: it was
   still a second row shape to learn, a second bucket to keep out of `total`, a `+8 more`
   pointing nowhere, and a `1 attachments` to pluralise.

Deleted with them: `ActivityCause` / `CauseDismissIn` / `CauseDismissOut`, `_causes()`, the
`array_agg` sample aggregate, `SAMPLE_NAMES`, `?reason=` and its `_email_stmt` branch,
`POST /activity/dismiss`, `CauseRow`, `dismissCause`, `rq-disclosure` / `rq-chev` /
`rq-row--cause` / `rq-cause-*`, and six i18n keys in both languages.

Worth keeping from the attempt, in the git history: `array_agg` needed the same
`dismissed_at IS NULL` filter `n` carries, or a put-away cause hands back names for
attachments it no longer counts — three names beside an `n` of 1, which the dev DB produced
the first time it ran for real — and the names needed deduplicating, because a bank resending
one locked file three times filled every sample slot with `BBLETAXACQ_locked.pdf`.

### Verified against the dev database

The new CASE executed for real (the 2026-09-03 500 was a `GROUP BY` that compiled fine and
failed only in Postgres). BU `af0786cd`:

| | review | unposted | noise | all |
|---|---|---|---|---|
| before | 15 | 10 | — | 140 |
| after | **9** | **26** | **97** | 140 |

`counts["all"]` still sums the buckets exactly. The 49 `no_rule_match` and 48
`unreadable_document` rows leave the status chips for `today` and `all`; the 16 a person can
clear from settings join the 10 rejections under `Not posted`, each carrying `Open settings`.

Backend `pytest` exit 0, ruff clean. New cases: the review chip holds documents only; the
noise arm ignores a charged `unreadable_document`; a clearable refusal is an ordinary row
counted in `unposted` and returned in `data`; the noise is still in the log; the chip
expression reads status alone for `review` and tests the status in its noise arm. Frontend
`tsc`/eslint/prettier clean, `vitest run` 655/655, production build green.

**Release note pending** (user-visible, batch at the next version bump): files your rules
threw out — a signature logo, something we could not read — no longer sit among the documents
waiting for you, and the ones you can fix from settings are under Not posted with a link
straight to the setting.
