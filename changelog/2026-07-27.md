# 2026-07-27

## Release v1.0.2

### `09:45` Version bump + batched release note

`VERSION` 1.0.1 → 1.0.2. New `releaseNotes.ts` entry (`2026-07-27`, EN+TH) cuts the release
that was drafted and deliberately reverted on 07-22 (`2f52efc`), and **clears every pending
release-note marker** left in `changelog/2026-07-21.md:345` and
`changelog/2026-07-22.md:86,93,130`.

The note leads with **bank / payment-gateway coverage**. The five commission formats — BAY,
KTC, GHL (NTT DATA), PAYPAL, SIAMPAY — merged `2026-07-03` (`bca717b`), which predates the
release-notes system itself (shipped `2026-07-20` as v1.0.0), so they have been live for
three weeks and were never announced. Worded as coverage ("supports eight issuers and
gateways"), not as a new arrival — nothing new ships in 1.0.2, which is why the bump stays
PATCH despite `CLAUDE.md`'s "a bank = MINOR" rule.

Batched into the same entry:

- **features** — the eight-issuer/gateway headline; department-restricted account codes
  (#108).
- **fixes** — AP discount column flipping the VAT reading (#106); AP scans failing with a
  system error on retry (#109, `parse_vision_json`); GHL fee-invoice blank Branch No. +
  English address (`b6dd20b`); dark-mode `additional mappings` hover (`4700bc1`).
- **qol** — gateway-aware AI Suggest (#110); wider mapping page with a live mapped-count
  pill; 5 MB / 5-page upload caps (#109).

The GHL bullet was **moved out of the shipped 1.0.1 entry** into 1.0.2, where it belongs to
the gateway story. 1.0.1 keeps its two payment-type-persistence fixes.

## Docs

- `09:50` `CLAUDE.md` env sample still read `MAX_FILE_SIZE_MB=20`. Real value is `5`
  (`config.py:125`, `.env.example:50`, `fileValidation.ts:8`) — the 07-22 caps change
  updated the prose ten lines above but missed the env block.

## Infra — pre-deploy audit findings (not fixed here)

Reported, no code change; the fix is repo settings, not this repo's source.

- `09:55` **The `Deploy` workflow has never succeeded — 40 of 40 runs failed since
  2026-07-15.** `supabase db push --db-url "$SUPABASE_DB_URL"` receives an empty string and
  falls back to a nonexistent local unix socket:
  `failed to connect to host=/var/run/postgresql user=runner database=var/run/postgresql:5432/`.
  `gh secret list --env production` returns nothing — none of the three secrets `deploy.yml`
  documents (`SUPABASE_DB_URL`, `RENDER_DEPLOY_HOOK_URL`, `HEALTH_CHECK_URL`) exist in the
  `production` GitHub Environment. Fix: Settings → Environments → production.
  Consequences: CI migrations never run (so `render.yaml`'s `autoDeploy: false`
  migrate-before-deploy ordering has not existed since 07-15), and **no release has ever
  been tagged** — `git ls-remote --tags origin` and `gh release list` are both empty, so
  v1.0.0 and v1.0.1 were never cut and v1.0.2 will not be either until the secrets land.
  It fails in ~20s with an error that reads like a local-tooling problem, which is how it
  survived 40 runs unnoticed.
- `09:55` **Verify two migrations are applied to prod.** Because `db push` never ran in CI,
  `20260721000000_legacy_prod_catchup.sql` and `20260721120000_session_scrub_keep_history.sql`
  may be unapplied; `changelog/2026-07-21.md:517` flags the latter as needing a push. Check
  `supabase_migrations.schema_migrations`.
- `09:55` Production is nonetheless current and healthy — `/api/version` returns
  `1.0.1`, health 200 — so the backend is being deployed by hand outside the pipeline.
  Frontend (Vercel, auto-deploy) and backend (Render, `autoDeploy: false`) ship
  independently, so a push to `main` shows users 1.0.2 before the backend has it.
