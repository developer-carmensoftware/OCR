# Changelog — 2026-06-10

_Backfilled from git history. Times = commit time (ICT, +0700)._

- `11:46` Security-hardening measures + unit tests (`626d0cd`).
- `13:36` Display QTY with 2 decimal places consistently (`21f2dda`).
- `14:22` Resolve tax-profile code from Carmen API instead of a hardcoded pattern (`c46e025`).
- `14:44` `InlineSelect` common component; replaced native selects in the AP invoice table (`da1cd28`).
- `15:23` System-wide UI polish — token unification and consistency (`07adaa3`).
- `16:16` `CustomSearchSelect` component with AI suggestion + top-choice features (`3b41981`).
- `17:12` UX polish pass — contextual help, label typeset, distill, z-index normalisation (`222a1af`).
- `17:17` Fix pre-existing test failures in `apGroup` and `useAPSubmission` (`3842755`).
