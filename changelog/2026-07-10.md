# 2026-07-10

## `02:41` Credit Card OCR: fixed columns again, blank amounts show `0.00`

Reverts the per-format column-hiding added `2026-07-03 12:13`. The Details table's
`visibleColumns` filter hid any amount column that was `0`/blank on every row, so the
column set varied by document type — payment-gateway fee invoices (KTC/GHL/PayPal/
SiamPay) always have `Net Amt = 0` and lost that column. Per user request the table
now always renders the same fixed 5 columns regardless of bank/gateway, and any empty
amount cell displays `0.00`.

### Frontend

- `02:41` `components/credit-card/DetailTable.tsx`: dropped the `visibleColumns`/
  `visibleAmountFields` derivation — the Details header/rows iterate `DETAIL_COLUMNS`
  and the Total Summary iterates `AMOUNT_FIELDS` directly again. `formatAmount` now
  returns `fmt(0)` (`"0.00"`) for blank/null input instead of an empty string, so a
  missing amount reads `0.00` consistent with every other amount cell. Display-only —
  extracted data, the backend fee-invoice normalizer, and JV/submission are untouched
  (downstream already treats blank as `0` via `parseNum`; `Total=0` rows still post no
  net JV row).

Verified: `tsc --noEmit` clean; `eslint` 0 errors (5 pre-existing unrelated warnings in
`useAPInvoice.ts`); credit-card `vitest` suite 54/54 pass.

## `10:47` Fix: "View Document" preview auto-opening the browser print dialog

Some uploaded PDFs (bank/gateway statements carrying an embedded `/OpenAction → /Print`)
popped the browser print dialog every time their preview iframe rendered. Root cause:
`6932ba0` (2026-06-30) removed the iframe `sandbox` so Edge would render PDFs — but that
sandbox's absent `allow-modals` had been *suppressing* the PDF's auto-print. Re-adding
`sandbox` isn't viable (Edge refuses to render PDFs in any sandboxed iframe, even with
`allow-same-origin`). Instead we strip the auto-open action from a throwaway copy before
the viewer sees it.

- `10:47` `lib/pdfPreview.ts` (new): `stripAutoOpen(bytes)` deletes catalog `/OpenAction`
  and `/AA` via `pdf-lib` (already a dep); `sanitizedPdfUrl(file)` returns a cleaned blob
  URL, falling back to the raw file URL if the PDF can't be parsed.
- `10:47` `hooks/credit-card/useFileUpload.ts`, `hooks/ap-invoice/useAPExtraction.ts`:
  route the raw-PDF preview through `sanitizedPdfUrl` (now async). The page-selected
  preview (`selectedPagesToPdfUrl`) was already immune — it rebuilds into a fresh doc.
- Admin `OrderWorkspace` PDF iframe left as-is (our own proformas / image slips, cross-
  origin signed URLs where a blob rewrite doesn't apply).

Verified: new `lib/pdfPreview.test.ts` (builds a print-on-open PDF, asserts the action is
gone after stripping); `tsc --noEmit` clean; `eslint` 0 errors on changed files;
`useFileUpload` + `useAPExtraction` + `pdfPreview` suites 37/37 pass.

## `11:05` Chore: over-engineering audit cleanup (drop a dep + dead imports)

Repo-wide ponytail audit — the tree is already lean (no factories/ABCs/hand-rolled
stdlib). Applied only the no-regret cuts; left the borderline ones (DocumentPreview's
zoom/pan `useReducer` — real gesture state, skip to avoid touch regressions; `fmt`/
`formatThb` unify — 86 call sites, low payoff; 1-line hook barrels — documented subdir
convention).

- `11:05` Dropped the **`@phosphor-icons/react`** dependency — it existed only for 4
  decorative tier icons in one card. `components/pricing/PlanCard.tsx` now uses lucide
  (`Rocket`/`Gem`/`Crown`/`Building2`, the app's icon set in 66 other files); the
  `weight="duotone"` fill is gone (lucide is single-stroke). **-1 dep.**
- `11:05` `pages/Mapping.tsx`: removed a dead `import React` + its `void React`
  keep-alive hack (the JSX runtime transform needs neither).

Verified: `npm uninstall @phosphor-icons/react`; `tsc --noEmit` clean; `eslint` clean;
`npm run build` succeeds (no phosphor chunk emitted).

## `11:55` Credit Card OCR: recalc/normalization hardening (16 defects)

Systematic pass over the credit-card amount-recalc path. The flow reconstructs money in
several places (backend footer-VAT spread / per-row solve; frontend JV Dr/Cr + input-tax
payload) but never validated the reconstruction against a trusted anchor, and several
footer-row classifiers stripped real fee lines. Scope agreed with user: comprehensive
(all 16), **hard-block** submit on JV imbalance, negatives **guarded** (positive-only).

### Backend — `services/credit_card_service.py`

- `11:55` **Classifier precision (B1/B2/B10):** anchored the fee-footer match instead of
  raw substring/prefix — `startswith("SUB")` → explicit subtotal tokens (so "Subscription
  Fee" survives); bare `"VAT"`/`"ส่วนลด"` now only match a label that *is* essentially the
  word (`_is_bare_footer_label`, so "VAT Advisory Fee" survives); `_labeled_footer_value`
  takes a per-group field preference (VAT line reads `tax_amt` first, etc.).
- `11:55` **Reconciliation guard (B7):** after the spread, cross-check `Σ fee + VAT`
  against the document's printed Grand Total (labeled line → summary `pay_amt`, else
  `Subtotal + VAT`) and warn on drift — the spread balances VAT exactly, so it hid
  misread line fees on its own.
- `11:55` **Blank-line handling (B3/B4):** `_spread_footer_vat` splits VAT evenly (not
  100% on the last row) when no per-line fee weights it, and warns when any line is blank.
- `11:55` **BAY (B8):** zero-commission blank rows now still get `net = gross − commis − 0`
  (was skipped when tax was `None`), with an unallocated-VAT warning.
- `11:55` **Solver + negatives (B5/B6/B9/B11):** footer-by-arithmetic now requires the
  candidate fee to strictly exceed every other line (kills the equal-pair false positive);
  3+-value solve uses the multiset; the 2-value rate-scored split flags `rate_assumed`;
  **negative amounts (refund/credit-note) short-circuit with a warning, rows untouched.**

### Frontend

- `11:55` **JV balance hard-block (F1):** `components/credit-card/AccountingReview.tsx`
  computes per-row `gross = commission + tax + net`; when Dr ≠ Cr it shows a red banner
  naming the offending line(s) and **disables Submit** (`cc.jvImbalance`).
- `11:55` **Payload hygiene (F3/F4):** `hooks/credit-card/useOcrSubmission.ts` rounds
  Cr/Dr to 2dp (`round2`) before Carmen; `InputTaxReconciliation.tsx` rounds the ACTX
  amounts and flags when the doc's effective VAT rate ≠ the chosen profile
  (`cc.effectiveRateNote`).
- `11:55` **Mapping staleness (F5):** `hooks/mapping/useMapping.ts` re-scans the wizard
  snapshot on `focus`/`storage` (was mount-only, so required-mapping counts went stale
  after later line-item edits).
- `11:55` `i18n/dict.ts`: `cc.jvImbalance`, `cc.effectiveRateNote` (EN + TH);
  `styles/components.css`: `.mapping-alert.is-danger` (rose variant).

Verified: backend `test_fee_invoice_clamp.py` 55/55 (14 new cases: B1/B2/B3/B4/B5/B7/B8/
B9/B10/B11) + `test_ocr_router.py` 8/8; frontend `vitest` full suite 238/238 (new F8
rounding case in `useOcrSubmission.test.ts`); `tsc --noEmit` + `eslint` clean;
`npm run build` succeeds.
