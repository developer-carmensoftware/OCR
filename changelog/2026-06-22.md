# Changelog — 2026-06-22

_Times in ICT (+0700). Committed entries = commit time; uncommitted = when the work was done._

## `02:56` — Checkout billing form: add purchaser name + required fields + proforma re-layout

Added a new `contact_name` (ชื่อผู้สั่งซื้อ) field end-to-end, made all buyer fields required, and re-arranged both the checkout form and the proforma "PROFORMA TO" block.

### DB
- `20260622022851_add_buyer_contact_name.sql` — `billing_documents.buyer_contact_name VARCHAR(255)` nullable.

### Backend
- `models/billing.py` — `buyer_contact_name` column (replaced stale commented-out `buyer_email` note).
- `models/schemas/credits.py` — `contact_name` on `BuyerInfoInput` + `CompanyProfileResponse`; `buyer_contact_name` on `BillingDocumentResponse`.
- `services/billing_document_service.py` — persist + read back `buyer_contact_name` (`BuyerInfo` TypedDict, `issue_document`, `get_last_buyer_info`).
- `services/carmen_service.py` — map Carmen `RegContact` → `contact_name`.
- `routers/credits.py` + `routers/admin/credits.py` — thread `contact_name` through the buyer override / tax-invoice snapshot.

### Frontend
- `lib/api/credits.ts` — `contact_name` on `BuyerInfo`, `buyer_contact_name` on `BillingDocument`.
- `hooks/credits/useCheckout.ts` — init + Carmen prefill of the new field.
- `components/pricing/CheckoutFlow.tsx` — re-laid-out form (row 1: name/taxId/branch · row 2: address · row 3: contact/email); all 6 inputs `required`; confirm button gated on `buyerComplete` (every field filled).
- `components/pricing/ProformaDocument.tsx` — "PROFORMA TO" re-layout: name → address → (taxId + branch) → (contact + email) on inline rows.
- `i18n/dict.ts` — EN/TH `checkout.fieldContactName`, `checkout.phContactName`, `checkout.fillAllFieldsHint`.
- `styles/pages/pricing.css` — `.checkout-row-3` 3-col grid (+ mobile stack), `.checkout-field-label::after` red `*`, `.pf-buyer-row` inline flex.

### Notes
- `test_billing_document_service.py` passes. The 2 red tests in `test_credits_api.py` are the pre-existing `fitz` (PyMuPDF not installed) collection error, unrelated.
- Needs `supabase db push` to apply the migration before the field persists.

## `03:04` — Remove dead i18n key (`baed58f`)

- `i18n/dict.ts` — dropped unused `checkout.enterNameHint` (EN/TH); superseded by `checkout.fillAllFieldsHint` above. Post-commit review cleanup; tsc clean.

## `03:12` — Thai baht amount-in-words on proforma

- `lib/money.ts` — new `bahtToThaiWords()` (ศูนย์–เก้า · สิบ/ร้อย/พัน/หมื่น/แสน/ล้าน, ยี่สิบ/เอ็ด rules, บาทถ้วน/สตางค์). ponytail comment marks the lone-trailing-1-after-ล้าน ceiling (unreachable at billing amounts).
- `components/pricing/ProformaDocument.tsx` — render Thai words line under the English one (always bilingual, matches the header).
- `lib/money.test.ts` — 3 cases for the Thai converter (0/490/1M, ยี่สิบเอ็ด/สิบเอ็ด, 2664.30 satang). 5/5 green, tsc clean.

## `10:55` — Fix: header REMAIN badge ignored subscription allowance

Bug: after buying a **subscription** pack (`sub_pro`, 1,500/mo), the header "REMAIN" counter stayed at the free-trial number (e.g. 27) — looked like the purchase didn't register. Backend was fine (`consume_document` charges subscription→free→credit; `/usage` already returns `subscription.docs_remaining`); the pricing-page `UsageSummaryStrip` showed it correctly. Only the header badge was stale.

- `components/common/UsageIndicator.tsx` — extracted the pool calc into a pure `computeUsageStats()`; `stats` now folds the active subscription into the pool: `REMAIN = subscription.docs_remaining + remaining_calls + credit_balance`, and the usage bar's capacity/used include the plan window. No backend change.
- `components/common/UsageIndicator.test.ts` — new; 4 cases (null, subscription summed=1527, free+credit, low/red). 4/4 green, `npm run build` clean.
