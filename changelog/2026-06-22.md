# Changelog — 2026-06-22

_Times in ICT (+0700). Committed entries = commit time; uncommitted = when the work was done._

## `02:56` — Checkout billing form: add purchaser name + required fields + proforma re-layout

Added a new `contact_name` (ชื่อผู้สั่งซื้อ) field end-to-end, made all buyer fields required, and re-arranged both the checkout form and the proforma "PROFORMA TO" block.

### DB
- `20260622022851_add_buyer_contact_name.sql` — `billing_documents.buyer_contact_name VARCHAR(255)` nullable.

### Backend
- `models/billing.py` — `buyer_contact_name` column (replaced stale commented-out `buyer_email` note).
- `models/schemas/credits.py` — `contact_name` on `BuyerInfoInput` + `CompanyProfileResponse`; `buyer_contact_name` on `BillingDocumentResponse`.
- `services/billing_document_service.py` — persist + read back `buyer_contact_name` (`BuyerInfo` TypedDict, `issue_document`, `get_last_buyer_info`).
- `services/carmen_service.py` — map Carmen `RegContact` → `contact_name`.
- `routers/credits.py` + `routers/admin/credits.py` — thread `contact_name` through the buyer override / tax-invoice snapshot.

### Frontend
- `lib/api/credits.ts` — `contact_name` on `BuyerInfo`, `buyer_contact_name` on `BillingDocument`.
- `hooks/credits/useCheckout.ts` — init + Carmen prefill of the new field.
- `components/pricing/CheckoutFlow.tsx` — re-laid-out form (row 1: name/taxId/branch · row 2: address · row 3: contact/email); all 6 inputs `required`; confirm button gated on `buyerComplete` (every field filled).
- `components/pricing/ProformaDocument.tsx` — "PROFORMA TO" re-layout: name → address → (taxId + branch) → (contact + email) on inline rows.
- `i18n/dict.ts` — EN/TH `checkout.fieldContactName`, `checkout.phContactName`, `checkout.fillAllFieldsHint`.
- `styles/pages/pricing.css` — `.checkout-row-3` 3-col grid (+ mobile stack), `.checkout-field-label::after` red `*`, `.pf-buyer-row` inline flex.

### Notes
- `test_billing_document_service.py` passes. The 2 red tests in `test_credits_api.py` are the pre-existing `fitz` (PyMuPDF not installed) collection error, unrelated.
- Needs `supabase db push` to apply the migration before the field persists.

## `03:04` — Remove dead i18n key (`baed58f`)

- `i18n/dict.ts` — dropped unused `checkout.enterNameHint` (EN/TH); superseded by `checkout.fillAllFieldsHint` above. Post-commit review cleanup; tsc clean.

## `03:12` — Thai baht amount-in-words on proforma

- `lib/money.ts` — new `bahtToThaiWords()` (ศูนย์–เก้า · สิบ/ร้อย/พัน/หมื่น/แสน/ล้าน, ยี่สิบ/เอ็ด rules, บาทถ้วน/สตางค์). ponytail comment marks the lone-trailing-1-after-ล้าน ceiling (unreachable at billing amounts).
- `components/pricing/ProformaDocument.tsx` — render Thai words line under the English one (always bilingual, matches the header).
- `lib/money.test.ts` — 3 cases for the Thai converter (0/490/1M, ยี่สิบเอ็ด/สิบเอ็ด, 2664.30 satang). 5/5 green, tsc clean.

## `10:55` — Fix: header REMAIN badge ignored subscription allowance

Bug: after buying a **subscription** pack (`sub_pro`, 1,500/mo), the header "REMAIN" counter stayed at the free-trial number (e.g. 27) — looked like the purchase didn't register. Backend was fine (`consume_document` charges subscription→free→credit; `/usage` already returns `subscription.docs_remaining`); the pricing-page `UsageSummaryStrip` showed it correctly. Only the header badge was stale.

- `components/common/UsageIndicator.tsx` — extracted the pool calc into a pure `computeUsageStats()`; `stats` now folds the active subscription into the pool: `REMAIN = subscription.docs_remaining + remaining_calls + credit_balance`, and the usage bar's capacity/used include the plan window. No backend change.
- `components/common/UsageIndicator.test.ts` — new; 4 cases (null, subscription summed=1527, free+credit, low/red). 4/4 green, `npm run build` clean.

## Uncommitted — Admin Order Review Console (redesign)

Rebuilt the admin order page (`#/admin/credit-orders`, nav now **"Order Review"**) from a generic table into a purpose-built **two-pane console**: status queue (left) + slip-forward review workspace (right). Says **"Company"** instead of "tenant"; surfaces buyer **company / purchaser / email** (mailto + copy) so the admin can confirm with the buyer; new **"On hold"** status to park an order after contacting them instead of rejecting. Plan: `~/.claude/plans/admin-review-graceful-elephant.md`.

### DB (needs `supabase db push` — NOT yet applied)
- `20260622030000_add_order_on_hold.sql` — `alter type creditorderstatus add value 'on_hold'` (own tx).
- `20260622030100_order_on_hold_open_index.sql` — recreate `uq_credit_orders_one_open_per_tenant` to include `on_hold`; add `credit_orders.admin_note text`.

### Backend
- `models/enums.py` — `CreditOrderStatus.ON_HOLD`. `models/billing.py` — `admin_note` column.
- `models/schemas/credits.py` — `CreditOrderResponse` += `approved_by`, `rejected_reason`, `admin_note`; new `HoldRequest`.
- `routers/admin/credits.py` — new `POST /credit-orders/{id}/hold` (toggle on_hold + note) and `/cancel` (pending only); approve/reject now accept `awaiting_review` **or** `on_hold` (`_DECIDABLE`); `GET /credit-orders` gains `tenant_id` filter + `status=all`.

### Frontend
- `pages/admin/CreditOrdersPage.tsx` — rewritten as the console shell (tabs + search + selection + payment-info).
- `components/admin/OrderQueue.tsx` (new) — status tabs w/ count badges, company search, list rows, shared `STATUS_META` + `timeAgo`.
- `components/admin/OrderWorkspace.tsx` (new) — header (+ prior-reject flag), Verify facts, Contact-buyer card, slip viewer (zoom/pan/rotate · iframe for PDF) beside full `ProformaDocument` (proforma/tax-invoice toggle), collapsible company credits+ledger+history, status-aware action bar (Approve single-confirm, Reject presets+note, Put-on-hold/Resume, Cancel).
- `lib/api/endpoints.ts` + `lib/api/adminClient.ts` — `creditOrderHold`/`creditOrderCancel`; `holdOrder`/`cancelOrder`; `listCreditOrders(status|'all', tenantId?)`; `AdminCreditOrder` extended; `'on_hold'` status.
- `styles/components/admin-credit-orders.css` — rewritten for the console (theme-aware tokens, dark mode, reduced-motion, responsive stack).
- `pages/admin/AdminLayout.tsx` — removed the "Order Review" sidebar entry (page is now standalone, see below).

### Frontend — Order Review as a standalone page (split out of admin dashboard)
- `pages/order-review/OrderReviewShell.tsx` (new) — own minimal shell (logo + email + logout, no admin sidebar) wrapping the console.
- `main.tsx` — new route **`#/order-review`** guarded by `AdminProtectedRoute` (reuses admin auth); removed the `#/admin/credit-orders` admin-branch route.
- `components/admin/AdminProtectedRoute.tsx` + `pages/admin/AdminLogin.tsx` — `admin_return_to` flow: hitting `#/order-review` unauthed → admin login → returns to `#/order-review` (not the admin dashboard).
- Console component (`pages/admin/CreditOrdersPage.tsx`) + `OrderQueue`/`OrderWorkspace` kept in place (imported by the shell) to avoid a rename cascade.
- `styles/components/admin-credit-orders.css` — standalone app-shell styles (sticky header, body full-bleed, queue sticky offset under the 56px header).

### Frontend — customer-facing (reject reason reaches the buyer)
- `lib/api/credits.ts` — `OrderStatus` += `on_hold`; `CreditOrder` += `rejected_reason`.
- `pages/OrderHistory.tsx` — rejected order now shows the **actual** `rejected_reason` (was a generic note); `on_hold` treated as an open order (under-review banner, not terminal history).
- `components/pricing/PendingOrderBanner.tsx` — `on_hold` reads as "under review" to the buyer (admin emails separately).
- `components/pricing/OrderStatusBadge.tsx` — `on_hold` → "under review" badge.
- `i18n/dict.ts` — EN/TH `order.rejectedReasonLabel`.

### Tests
- `tests/integration/test_admin_credits_api.py` — repaired 4 stale tests (bare-MagicMock order missing `CreditOrderResponse` fields; list tests mocked `.scalars()` but the endpoint reads `(order, name)` tuples via `.all()`) + added hold/resume/cancel/approve-on-hold coverage. **15/15 green.**

### Frontend — dark mode revived + bilingual toggles on admin surfaces
- `hooks/useDarkMode.ts` — was a no-op (forced light); now a real `[dark, toggle]` (sets `data-theme`, persists `localStorage['theme']`). `main.tsx` applies the persisted theme before first paint (no flash). `components/common/DarkModeToggle.tsx` renders a real sun/moon button (was `null`).
- `components/common/` toggles added to both the **Order Review** shell header and the **admin dashboard** sidebar footer (`LanguageToggle` + `DarkModeToggle`). New `.theme-toggle` style + `.admin-sidebar-toggles` row.
- **Order Review fully bilingual (EN/TH):** ~95 `orev.*` keys in `i18n/dict.ts`; `OrderQueue`/`OrderWorkspace`/`CreditOrdersPage`/`OrderReviewShell` now use `useT()`. `STATUS_META` split into `STATUS_TONE` + `STATUS_KEY` (i18n labels); `timeAgo(iso, t)`. Reject presets stay Thai (buyer-facing). Lang defaults to Thai (good for the Thai reviewer).

### Notes
- `npm run build` + `tsc --noEmit` clean. Order Review now reachable at `#/order-review`. Live browser pass still pending (needs `supabase db push`, already done by user this session).
- Reject presets are Thai (buyer-facing); admin dashboard chrome stays English (its language toggle sets the global lang, which the customer/Order Review surfaces honor).
