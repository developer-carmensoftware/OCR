# 2026-08-25

## Refactor audit — Tier 1: dead code

`10:30` A maintainability audit of the whole repo (23k LOC backend, 42k frontend) found
much less rot than expected: **0 unused i18n keys** out of 1,188, 3 over-exported
frontend symbols out of ~600, 0 TODO/FIXME, and all three CLAUDE.md conventions
(no `BaseModel` in routers, no `HTTPException` in services, single `AsyncOpenAI` factory)
held with zero violations. This is the first of four planned refactor PRs and removes
only what is provably unreferenced.

### Backend

- **Deleted the legacy master-key admin auth.** `routers/admin/deps.py` carried
  `require_admin_key()` and `require_admin()` — both self-labelled as
  backward-compat ("remove in Phase 2"; "used by maintenance.py which has no session
  dep"), and both with **zero references** anywhere in app or tests. `maintenance.py`
  had long since moved to `require_maintenance_auth()`.
- **Dropped `master_api_key` with them** — those two functions were its only readers.
  `MASTER_API_KEY` is gone from `.env.example` and from `render.yaml` (where it was a
  `sync: false` secret Render prompted operators to set on every deploy, for a
  credential path nothing served). Safe because `Settings.Config.extra = "ignore"`:
  an env var still set on a live service is ignored, not a boot failure.
- **Removed unread settings** `app_host`, `app_port`, `admin_jwt_ttl_hours`
  (`config.py`). `run.py` hardcodes port 8010 — `APP_PORT` had been documentation of a
  var nothing read. Purged from `.env.example`, `CLAUDE.md`, `README.md` and the SRS
  env table.
- **Removed `get_all_tenants()`** (`app/database.py`) — zero references.

### DB

- **`20260825000000_drop_retired_quota_tables.sql`** — drops `quotas`, `quota_usage`
  and the `quotaperiod` / `quotametric` enums. `20260813000100` moved the free-trial
  allowance into `tenant_credits` and kept the tables one release for reversibility;
  that release shipped. Confirmed unreferenced: no ORM model, no raw SQL. Charging runs
  entirely through `consume_document()`.
- Deleted `supabase/schemas/40_quotas.sql`, as CLAUDE.md required in the same change.

### Frontend

- **Un-exported three local-only symbols** — `DAY_MS` (`PeriodPicker.tsx`), `slipIsPdf`
  and `SlipSkeleton` (`SlipViewer.tsx`). All three are used inside their own file, so
  this narrows the public surface; nothing is deleted.

### Docs

- `CLAUDE.md`: the Quotas row in the DB layer table is gone and the "Two document pools"
  entry now records the drop migration instead of scheduling it.

**Verification:** `ruff check app/` clean · backend pytest exit 0 · `npm run build`
clean · 501 frontend tests in 49 files pass · app boots with `MASTER_API_KEY` absent
(127 routes).

No `VERSION` bump and no `releaseNotes.ts` entry — refactors bump nothing, and none of
this is user-visible.

## Refactor audit — Tier 3a: the JV body is now pinned across both implementations

`11:00` **The risk this closes.** `cc_jv.py` builds the Carmen `gljv` body for email
automation; `lib/ccJv.ts` + `useOcrSubmission.ts` build the same body for the wizard.
The Python docstring said the two were *"kept deliberately in step"* — by hand. Both
suites tested their own side and **nothing tested that the two agreed**, so a drift
would have posted wrong money with every test still green.

- **`contracts/cc-jv.contract.json`** — one fixture, read by both suites:
  `backend/tests/unit/test_cc_jv.py::test_contract_fixture` and the new
  `frontend/src/lib/ccJv.contract.test.ts`. Four cases: a two-payment-type bank
  statement, a gateway fee invoice (net = 0, so the Bank Account leg must be *shown*
  in Step 3 but *dropped* from `Detail`), unknown-bank-code + empty-description
  fallbacks, and a Buddhist-era date. All four assert a balanced JV.
- **New top-level `contracts/` directory.** The fixture was first put in
  `test-fixtures/`, which turns out to be **gitignored** (*"Local test fixtures"* —
  developer-generated mock invoices). It would never have reached CI. A cross-language
  contract is not a local fixture and neither side owns it, so it lives at the root.

**Three real divergences found while writing it** — all pinned, none were bugs:

- `JvhDate`: Python emits `+00:00`, JS emits `.000Z` for the same instant. The test
  compares instants, never strings.
- `UserModified`: `''` from the wizard, `'OCR-EMAIL'` from ingest. This difference is
  the *point* of the field (CARMEN_INTEGRATION.md §4.3), so it sits outside the shared
  `expected` and each side asserts its own value.
- Zero-padding is load-bearing: `new Date('2026-8-5')` parses as **local** time and
  lands on the previous day in UTC, while `datetime(2026,8,5)` does not move. Both
  producers pad today (`normalizeDateStringToCE`, `formatDateToDDMMYYYY`), so it is
  unreachable — now documented in the fixture and in `jvhDate()`.

### Frontend — one extraction, so the test covers shipping code

- **`buildGljvPayload()` moved out of `useOcrSubmission.ts` into `lib/ccJv.ts`**, next
  to `buildJvRows` and mirroring the Python module's shape. Without this the contract
  test would have asserted against a *copy* of the payload logic rather than the code
  that actually posts. `useOcrSubmission` keeps the snake_case/camelCase config
  normalisation (that is about where config came from) and passes one clean shape.
- `parseJvhDate()` moved with it as `jvhDate()`; the duplicate `JvRow` interface in
  `useOcrSubmission.ts` is now a re-export of the one in `ccJv.ts`.

**Verified by mutation, both directions:** breaking the tax-leg description in
`cc_jv.py` reddens the backend suite (3 failures) and leaves the frontend green;
breaking it in `ccJv.ts` reddens the frontend suite (4 of 6) and leaves the backend
green. Each implementation is independently pinned to the contract.

`ruff` clean · backend pytest exit 0 · `npm run build` clean · **507 frontend tests in
50 files** (up from 501/49).

## Refactor audit — Tier 2: deduplication

`11:20` Third of four refactor PRs from the maintainability audit. Frontend nets
**−17 lines** while *adding* three new files (338 lines), so roughly 355 lines left the
pages; backend is exactly **196/196** insertions/deletions, confirming a pure rename.
The main JS bundle drops **202.0 kB → 187.7 kB** (gzip 50.5 → 47.3).

### Frontend — `useTableData`, the fetch every admin table was hand-rolling

- **New `hooks/admin/useTableData.ts`** — companion to `useTableQuery`. Owns
  `{ rows, total, loading, reload }`, the failure toast, and the effect. Eight pages
  carried a byte-identical `useState`×3 + `useEffect` block, each with its own
  `eslint-disable react-hooks/exhaustive-deps`; **7 of those 8 disables are gone**
  (PerformancePage keeps one, for its unrelated debounce effect).
- Converted: Jobs, LLM Logs, Performance, Sessions, Anomalies, User Usage,
  Extractions, Tenants.
- **Fixes a race none of the eight handled.** Typing in a search box fires one request
  per keystroke and nothing ordered the responses, so a slow early one could land last
  and repaint the table with rows that no longer matched the filters. The hook keeps a
  monotonic request id and lets only the newest write. Fixed once instead of eight times.
- Behaviour note: ExtractionsPage started `loading` at `false` where the other seven
  started `true`. It now matches them — a loading state on first paint instead of a
  flash of "empty".

### Frontend — one admin route registry instead of two hand-synced lists

- **New `pages/admin/routes.tsx`** — `NAV_SECTIONS` carries `hash`, `icon`, `labelKey`
  *and* `component`, and `ADMIN_ROUTES` (hash → page) is derived from it. Previously the
  sidebar tree in `AdminLayout` and a 19-branch `else if` chain in `main.tsx` listed the
  same 18 hashes independently. Verified before merging that the two lists already
  agreed exactly — 18 for 18, nothing orphaned either way.
- **New `pages/admin/AdminRouter.tsx`** — the hash routing moved out of `main.tsx`. This
  is what keeps the dashboard lazy: `main.tsx` now knows only that `#/admin*` belongs to
  one lazily-loaded component, so the nav tree, its icons and the 18 `lazy()` thunks all
  left the main bundle (−14.3 kB).
- `AdminLayout` 260 → 94 lines; `main.tsx` 292 → 202. **Adding a page is now 2 edits,
  not 4** — CLAUDE.md updated in the same commit.

### Frontend — duplicated helpers, and one that must NOT be merged

Three merged; **two of the three were behaviour differences, not copies**:

- `fmtDateTime` — `OrderActions` used `toLocaleString()` while the rest of the dashboard
  uses `lib/date`'s `toLocaleString('sv-SE')`. The same dashboard rendered two date
  formats. Now one (`YYYY-MM-DD HH:mm:ss`).
- `daysAgo` — `EmailAutomationPage`'s copy went through `toISOString()`, so at UTC+7 any
  call before 07:00 local returned **the day before** the one the user meant. Now uses
  `PeriodPicker`'s local-date version. A real off-by-one, not just duplication.
- `timeAgo` — the notification bell's copy had no null/NaN guard; now uses
  `orderHelpers`', which does.
- **`fmt` and `formatThb` were left alone deliberately.** They look interchangeable but
  are not: `fmt` runs input through `parseNum` (strips commas), `formatThb` uses
  `Number()`, which returns `NaN` for `"1,234.56"` and renders `"0"`. `fmt` has ~20 call
  sites in the AP-invoice and credit-card wizards passing extracted strings. Merging
  them would have silently zeroed money on screen.

### Frontend — one accessor for `accountingConfig`

- `readAccountingConfig()` / `writeAccountingConfig(patch)` in `lib/storage.ts`, which
  already owned `appKey` and the key list. The `AccountingConfig` type moved there too
  (re-exported from `useAccountingConfig` for existing importers). Six call sites across
  four hooks each had their own `JSON.parse(localStorage.getItem(...) || '{}')`, their
  own inline cast and their own `try/catch`.

### Backend — the underscore stops lying

- Seven modules reached across into another module's `_private` names. Renamed to public:
  `_num`/`_r2` (`cc_jv`), `_wrap_network_error` (`carmen_service`), `_estimate_cost`/
  `_get_pricing` (`pricing_cache_service`), `_decode_jwt_claims` (`performance`),
  `_validate_token`/`_validate_uri` (`auth`).
- `carmen_service._get_client` became **`get_http_client`**, not `get_client`: the latter
  already exists in `llm/client.py` as the `AsyncOpenAI` factory, and two `get_client`s
  meaning different things is worse than the underscore.
- The one survivor, `from app.config import settings as _cfg_early`, is a local import
  alias rather than a reach into another module's internals — left as is.
- **`ap_invoice_postprocess_service` kept its `_num`/`_r2`.** A first pass renamed those
  too, since they share a name with `cc_jv`'s. They are a different function — `r2` there
  rounds with a `1e-9` epsilon, `cc_jv`'s with `0.0` — and nothing outside the module
  imports them, so they are correctly private. Reverted.

### Noted, not done

`services/usage_service.py` still describes itself as a *"backward-compat re-export shim —
Phase 10 will remove this file"*, and still re-exports three private names. It is not dead:
four app call sites and several test patch targets depend on it. Removing it means
repointing those and rewriting the patch targets — its own change, not this one.

**Verification:** `ruff check app/ tests/` clean · backend pytest exit 0 · `npm run build`
clean · 501 frontend tests in 49 files pass · bundle 202.0 → 187.7 kB.

No `VERSION` bump and no `releaseNotes.ts` entry — refactors bump nothing.

## Refactor audit — Tier 3b: split the IMAP transport out of email ingestion

`11:45` Last of four refactor PRs. `email_ingest_service.py` had reached **1,793 lines**,
of which ~630 were MIME decoding and IMAP plumbing sitting above the pipeline that
actually decides what a document means and what it costs.

- **New `services/email_imap.py` (698 lines).** Transport only: IMAP connect/search/
  fetch/flag, MIME and header decoding, zip expansion, tag parsing, Gmail
  auto-confirmation, filename-rule matching. **No DB, no session, no tenant** — it turns
  a mailbox into `dict`s and puts `\Seen` flags back. Blocking on purpose (`imaplib` has
  no async API), so the pipeline calls it through `asyncio.to_thread`.
- `email_ingest_service.py` 1,793 → **1,130 lines**, keeping everything that decides
  meaning and cost.
- The seam is the one the tests had already found: `test_email_ingest_routing.py` was
  almost entirely exercising what is now the transport module.

**Verified as a true move, not a rewrite.** Both halves were diffed against `HEAD`
after applying the renames below: the transport body is **byte-identical**, and the
pipeline body is byte-identical apart from two leading blank lines. No logic changed.

### The one deviation from plan: six functions had to go public

The plan called for a pure move with no renames, and for the existing tests to pass
untouched. Neither survived contact, for the same reason:

- Twelve transport functions are called from the pipeline half, and **six of them were
  underscore-private** (`_fetch_unseen`, `_fetch_confirmations`, `_mark_seen`,
  `_unmark_seen`, `_since_arg`, `_unique_names`). A name cannot stay private once it
  crosses a module boundary — that is exactly the anti-pattern Tier 2 removed seven of.
  They are now public; the other eleven transport functions stay private.
- The tests had to change **either way**. They patch through the module namespace
  (`patch.object(ingest, "_fetch_unseen", …)`), so the binding moves regardless of what
  it is called. 23 references repointed at `email_imap`, plus the private→public rename.

### What deliberately stayed with the pipeline

- **`_Skip`** — its docstring carries the refund-boundary contract (*"carries no refund
  flag on purpose"*) and only pipeline code raises it.
- **The module docstring** — the routing/refund/tag design record and its two `ponytail:`
  markers belong with `run_ingest`, not with the MIME decoders. `email_imap.py` got its
  own docstring saying it is transport and nothing else.

### Docs

- CLAUDE.md's email-ingestion diagram now names both modules and states the split rule.

**Verification:** `ruff check app/ tests/` clean · backend pytest exit 0 · the two email
suites (2,269 lines) **140 passed** · app boots with all 8 email routes registered.

⚠️ **Not run: a live mailbox poll.** `IMAP_HOST` is populated in this dev environment, so
`POST /api/v1/email/ingest` would mark real messages `\Seen`, spend document credits and
post JVs to Carmen. That end-to-end check is left for a deliberate run against the test
mailbox before merge.

No `VERSION` bump and no `releaseNotes.ts` entry — refactors bump nothing.

---

## `16:38` Input tax (ACTX) posted with a blank TaxId, and a Carmen rejection read as success

Reported from the field: the Step-6 input-tax record says it was added, but the document
never appears in Carmen. Two independent defects, stacked.

### Frontend

- **The vendor on the ACTX record now comes from the document's detected bank, not from
  localStorage** (`components/credit-card/InputTaxReconciliation.tsx`). `VnName` / `TaxId` /
  `Address` resolve through `BANK_INFO[OCR_BANK_MAP[bank]]`, with `accountingConfig.company`
  demoted to a fallback. `BranchNo` still comes off the document.
  Why it broke: that config is **localStorage-only** — `saveAccountingConfig` persists just
  `branch` (`hooks/mapping/useMapping.ts`), `bu_accounting_configs` has no column for the rest,
  and `useAccountingConfig` reads `readAccountingConfig().company` verbatim. `clearAppStorage()`
  wipes it on every 401 / logout / tenant switch, and the next extraction rebuilds `company`
  from the document as `{name, branch}` — no tax id. The Mapping page backfills identity from
  `BANK_INFO` via `normalizeConfigShape()`; the wizard never called it. `VnName` is now the
  registered legal name, matching what email ingest already posts for the same statement.
- **Add Input Tax is disabled when no bank identity resolves**, with a reason (new
  `cc.inputTaxNoVendor`, EN + TH) instead of posting a record Carmen is known to reject.
- **The Carmen verdict is now read.** `submitInputTax` → `if (res?.Code !== 0) throw` — the
  same check `useOcrSubmission.ts` does for the JV and `email_ingest_service._post_input_tax`
  does for this very document. This step was the only Carmen post that skipped it and toasted
  success over a rejection.
- New `InputTaxReconciliation.test.tsx`: identity with **no** saved config, the disabled state,
  and `Code: -1` not finishing the step.

### Backend

- `post_input_tax` (`services/carmen_service.py`) raises `CarmenAPIError` on a 4xx/5xx instead
  of returning the error body as if it were the created record. Scoped to this one function —
  `post_gljv`'s body drives the `submitted_at` bookkeeping in `routers/carmen.py`, so its
  failure mode is a separate call. Two cases added to `test_carmen_empty_body.py`.

**Verification:** frontend `vitest run InputTaxReconciliation` 3 passed · `tsc --noEmit` clean ·
eslint clean · backend `test_carmen_empty_body.py` 10 passed, `test_cc_input_tax.py` +
`test_email_ingest_pipeline.py` 107 passed.

⚠️ **Not run: a live post to Carmen.** The end-to-end check (clear
`t:<tenant>:accountingConfig`, scan a KBANK statement, confirm the TAX ID. column fills and the
record lands in Carmen) is left for a deliberate run before merge.

**Pending release note** (user-visible fix, batched — no `VERSION` bump this PR):
EN *"Input tax records sometimes reached Carmen without the bank's tax ID and were rejected
without telling you — the tax ID now always comes from the bank on the document, and a
rejection is reported instead of shown as success."* /
TH *"บางครั้งรายการภาษีซื้อถูกส่งไป Carmen โดยไม่มีเลขประจำตัวผู้เสียภาษีของธนาคาร แล้วถูกปฏิเสธ
โดยไม่มีการแจ้งเตือน — ตอนนี้เลขผู้เสียภาษีดึงจากธนาคารในเอกสารเสมอ และหาก Carmen ไม่รับ
ระบบจะแจ้งให้ทราบแทนที่จะขึ้นว่าสำเร็จ"*

### Measurement

- **`backend/db/queries.sql` item 25** — weekly `POST /gljv` vs `POST /inputTaxRec` counts off
  `outbound_call_logs`. A statement should produce both; the gap is the baseline this fix is
  meant to shrink. It **understates** the problem (Carmen answers 200 with the verdict in a body
  the log does not store) and cannot separate a user's Discard from a failed post — read the
  trend, not the number.
- **Mutation-checked, not just green:** reverting the vendor resolution fails
  `posts the issuing bank's registered identity with no saved config at all`; reverting the
  `Code` check alone fails `treats a non-zero Code as a failure, not a finished step`. Both
  restored, suite green.
