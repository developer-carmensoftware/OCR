# 2026-09-09

## `reset_tenant_email.py --keep-settings` — the hand-it-to-a-tester reset

`10:55` The script had two depths of reset and neither was the one you want before handing a
BU to a tester. Default deletes `email_ingest_settings` (the tag is released, so the
customer's forwarding address changes and the Gmail handshake has to be redone);
`--keep-tag` keeps the tag but still disables the row and nulls the Carmen token, so the
tester's first job is re-enabling automation rather than testing it.

`--keep-settings` leaves that table entirely alone and clears only what *remembers a
document*: `email_documents`, `email_queue_seen`, the posted `credit_cards`, `\Seen`.
Automation stays on with its tag, token and handshake, so a forwarded mail flows on the next
poll with no setup first.

It also has to be a separate flag rather than a `--keep-tag` tweak: `--keep-tag` nulls
`enabled_at` deliberately (re-enabling stamps it to *now*, after which every replayed mail is
older than it and lands `ingest_paused`). `--keep-settings` never re-enables, so the existing
`enabled_at` — NULL on carmencloud — stays correct and the arrival filter in `_run_message`
stays inert.

**The verb and the SQL now come from one function.** A third case meant two parallel
three-way branches — the plan line printed before `--apply`, and the statement the
transaction runs — and that plan is the only thing an operator reads before agreeing to a
destructive run, so drift between them would print `keep` while deleting the row.
`_settings_plan()` returns both; `--self-check` (hidden flag, no DB, no args) asserts the
three pairs, including that `--keep-settings` wins over `--keep-tag` and that the disable
branch still nulls `enabled_at`.

Repo-root `scripts/` is outside CI's `ruff check app/ tests/`, so it was checked by hand
against `backend/pyproject.toml` — which also cleared a pre-existing 100-column overrun on
the settings print line.

- Ops: reset `dev.carmen4.com/carmencloud` for tester handover —
  `--keep-settings --keep-sub --apply`. 13 ledger rows deleted (3 posted / 6 rejected /
  4 skipped), 1 queue-seen row, 7 `credit_cards` doc_nos soft-deleted, 9 mailbox messages
  back to unread. Kept: tag `1ad4e0b6`, Carmen token, `sub_starter` 33/200, the 12 GL
  mapping entries. JV 1032–1034 stay in Carmen — nothing here reaches the ERP.
- `11:05` Second pass with `--clear-manual` on the same BU: the 2 remaining wizard scans
  (1 of them posted) soft-deleted, so the module's Posted tab — which merges the email
  ledger with wizard-posted `credit_cards` — starts empty. Live `credit_cards` now 0.

No release note: dev tooling, nothing a customer sees. No `VERSION` bump.

## `11:24` Lite — a fourth subscription tier at 100 documents / ฿290

The catalog opened at Starter (200 docs / ฿490/mo), so a tenant leaving the 30-document
signup grant faced a 6.6× jump in volume as their first paid step. **Lite** closes that gap:
100 documents/month at ฿290, under the ฿300 a single hotel can approve without procurement.

Priced as a **subscription**, not a top-up, which is what keeps the ladder honest:

    Lite          100/mo   ฿290     ฿2.90/doc   ← new
    Starter       200/mo   ฿490     ฿2.45/doc
    Growth        500/mo   ฿990     ฿1.98/doc   Popular
    Professional 1500/mo   ฿2,490   ฿1.66/doc

More documents, cheaper per document — the rule the `≈ ฿X / doc` line on every card makes
visible. The same ฿290/100 as a *top-up* would have been ฿2.90/doc against `pack_small`'s
฿4.00/doc, so five 100-packs (฿1,450) would have beaten one 500-pack (฿2,000) for identical
credits. Nothing needed repricing.

Two invariants checked before shipping, both still hold:

- **Annual→monthly lock.** `credit_service.purchase_block_reason` rests on "the smallest
  annual price > the largest monthly price". Lite annual is ฿3,132 > Pro monthly ฿2,490. A
  cheaper tier is exactly what could have broken this.
- **Downgrade guard** ranks by `credits`, so Lite lands at the lowest rank automatically.

**No backend change.** `GET /credits/packs` is generic over `credit_packs`, `annual_price()`
derives ฿3,132 on read, and `usePricingCatalog` splits by `kind`. No new customer-facing
i18n keys either — `{name}` interpolates from `PLAN_META`.

### DB

- `11:24` **`supabase/migrations/20260909000000_add_lite_plan.sql`** — insert `sub_lite`
  (`subscription`, 100, ฿290.00, `sort_order` 0, description `'Lite Plan — 100 credits/month'`).
  Idempotent upsert. `description` is the proforma line item; without it the invoice prints
  the raw code. The annual price is **not** stored — it is derived, so there is nothing to
  keep in sync.
  > A dead `p100` row (100 credits, ฿290.00, `kind='topup'`, `is_active=false`) from the
  > pre-v2 catalog is deliberately left alone: FK-safe, filtered by `is_active`, and deleting
  > it would risk orphaning historical `credit_ledger`/`credit_orders` rows.

### Frontend

- `11:24` `constants/billing.ts` — `PLAN_META` gains `sub_lite: { name: 'Lite' }`. No badge,
  no highlight: Growth keeps the sole anchor, and Lite is meant to be the quietest card.
  ฿290/100 sitting next to ฿490/200 also makes Starter read as the generous option.
- `11:24` `components/pricing/PlanCard.tsx` — `TIER_ICONS` gains
  `sub_lite: { Icon: Sparkles, tint: 'plan-icon--lite' }`. Without a `TIER_ICONS` entry a card
  renders with no icon at all.
- `11:24` **`PlanCard.tsx` — a disabled CTA now says why.** `disabled={disabled || isDowngrade}`
  rendered nothing explaining itself. That barely bit while Starter was the floor; putting a
  tier *below* it means every Starter, Growth and Pro subscriber opens the page to the
  cheapest, most eye-catching card greyed out for no stated reason. Now shows
  `plan.downgradeNote` under the button. A `title=` tooltip would not do — browsers suppress
  tooltips on disabled buttons.
- `11:24` `i18n/dict.ts` — one new key, EN + TH: `plan.downgradeNote`
  ("Downgrading is available when your current plan ends.").
- `11:24` `pages/Pricing.tsx` + `styles/pages/pricing.css` — `plan-grid--3` → **`plan-grid--4`**
  with explicit **4 / 2×2 / 1** steps (900px, 560px). Explicit rather than `auto-fit`: inside
  the shared 860px `.pricing-section` column auto-fit fits 3 and orphans the 4th card on a row
  of its own, whereas 4 divides evenly at every step. The 860px column stays — it exists so
  plans / top-up / features line up instead of pinching then snapping wide. Cards go ~276px →
  ~203px; `plan-price-amount` is `clamp(1.5rem, 2.8vw, 2rem)` so the price stays readable.
- `11:24` `pricing.css` — the loading skeleton (`pricing-skeleton-grid--4`, 4 cards) now
  mirrors `plan-grid--4` step for step, so it can't flash a 3-up grid that reflows to 4.
- `11:24` `components/pricing/tutorial/fixtures.ts` + `screens.tsx` — `DEMO_PLANS` gains Lite
  (annual 3,132) and the tour's grid moves to `--4`. The figures mount the real `PlanCard`, so
  the fixtures have to mirror the live catalog or the tutorial teaches a product that no
  longer exists.
- `11:24` `screens.test.tsx` — expects 4 `.plan-card`.

### `12:02` The CTA at 4-up — three things the narrower card broke

Going 3-up → 4-up took each card from ~276px to ~203px (~160px of content), and three
elements had been sized for the old width. All three were found by looking at the rendered
page, not the code.

- `12:02` **The CTA label no longer fit.** `.plan-cta` was `white-space: nowrap`, so
  "Upgrade to Professional" (~165px against ~112px of room) was clipped by the card's
  `overflow: hidden`. The label was the problem, not the button: it restated the `<h3>` two
  lines above it. Visible text is now the action alone — `plan.ctaChoose` / `ctaUpgrade` /
  `ctaRenew` ("Upgrade plan"), EN + TH — and the tier-specific phrase moved to `aria-label`,
  so a screen reader moving button to button still hears four distinct actions instead of
  "Upgrade plan" four times. One line in both languages, no truncation, no two-line button.
- `12:02` `.plan-cta` — `min-height: 2.75rem` (WCAG 2.5.5 target size) pins all four CTAs to
  one height, which is what keeps the price row level; `padding-inline` 1.25rem → 0.75rem
  (the button is `width: 100%`, so the wider inline padding only squeezed the label). `nowrap`
  deliberately **not** restored: an over-long translation should wrap and be seen, not be
  clipped invisibly.
- `12:02` **`.plan-price-amount` was `clamp(1.5rem, 2.8vw, 2rem)`** — viewport-scaled type
  ignores the container, so at 4-up it still resolved to 2rem and "฿2,490" pushed "/ month"
  onto its own line, dropping Professional's price below the other three. Now a fixed
  `1.6rem` + `white-space: nowrap`, with `nowrap` on `.plan-price-period` too. Fixed rem over
  clamp is also what the product register asks for; fluid type is a brand-surface device.
- `12:02` **"Upgrade to Lite" was a lie.** The label branched on *"is there an active plan"*,
  never on direction, so any smaller tier was announced as an upgrade. Starter was the only
  tier below Growth/Pro before, so it stayed rare; a tier below Starter puts it on every
  subscriber's first card. Added `plan.ctaDowngrade` / `plan.downgrade` and a real
  `isDowngrade` branch, so the disabled card now reads **Downgrade** and its tooltip says
  when that becomes possible.
- `12:02` The downgrade reason moved from a visible line to a **hover tip on a wrapper**.
  It cannot sit on the button: `.btn:disabled` sets `pointer-events: none`, and browsers
  suppress `title` on disabled controls either way. It cannot be a visible line either —
  one taller card desyncs the price row, which is the bug above in a different costume.
  `.plan-cta-tip` carries the `title`; an `.sr-only` span (`position: absolute`, zero layout
  cost) plus `aria-describedby` carries the same sentence to assistive tech.
- `12:02` **`PlanCard.test.tsx` is new** — 5 tests over the downgrade state and the label
  split, the first tests this component has had. Mutation-checked: deleting the tooltip
  fails them.

### Cleanup — the `sub_standard` alias, dead since 2026-06-29

`20260629000000_rename_standard_to_growth.sql` cloned the row to `sub_growth`, repointed all
four FK children (`credit_orders`, `tenant_subscriptions`, `credit_ledger`,
`billing_documents`) and **deleted** the old row. Since those columns are FKs to
`credit_packs.code`, Postgres itself now rejects any write of `sub_standard` — the alias was
not merely unused, it was unreachable. Every one of its comments said "kept until DB
migration renames → sub_growth"; that migration landed two and a half months ago.

**Checked on dev** (`ycykjisvvrrbgeiirqre`) — all five reference counts 0:

    credit_packs          code        0
    credit_orders         pack_code   0
    tenant_subscriptions  plan_code   0
    credit_ledger         pack_code   0
    billing_documents     pack_code   0

⚠️ **Prod (`lhlncsjqxttcdegkqvid`) has not been checked** — see *Not shipped yet*. Dev is the
database that was never at risk; the pre-flight exists precisely because
`20260721000000_legacy_prod_catchup.sql` upserts dev's rows into prod **without deleting
extras**, so only a prod query actually settles it.

- `11:24` `constants/billing.ts`, `PlanCard.tsx`, `pricing.css` — alias entries and the
  `_growth` / `_growthIcon` consts that existed only to be shared with them, gone.
- `11:24` `pricing.css` — four more dead rules from when Free and Enterprise were cards in
  the grid. `data-tier` is only ever `pack.code` or the literal `"enterprise"`, and
  `EnterpriseCard`'s root is `.enterprise-band`, **not** `.plan-card`, so none could match:
  `.plan-card[data-tier='enterprise']`, `.plan-card[data-tier='free']::before`,
  `.plan-icon--enterprise` deleted; `.plan-icon--free` **renamed `.plan-icon--lite`** and
  handed to the new tier (same two declarations, correct name).
- `11:24` `.plan-icon--standard` → `.plan-icon--growth` — named after a tier that stopped
  existing in June.

Net for the cleanup: ~20 lines deleted, 0 added, no behaviour change. Growth's card is the
whole regression surface and is pixel-identical.

### Docs

- `11:24` `docs/Billing_Purchase_Flow.md` — "three subscription tiers" → four. The
  annual→monthly rationale named "the cheapest annual plan (Starter, ฿5,292)"; that is now
  **Lite, ฿3,132**. The argument is unchanged, the number was stale.

### Verified

`npx tsc --noEmit` clean · `npx vitest run` **57 files / 681 tests, all pass** ·
`npm run build` clean · eslint 0 errors (6 pre-existing warnings, all in AP/JV files) ·
prettier clean · `pytest -k "credit or pack or plan or subscription or billing"`
**201 selected, exit 0** (nothing asserted a 3-tier catalog) · no `sub_standard` /
`plan-icon--free` / `plan-icon--enterprise` / `plan-grid--3` reference left in `frontend/src`.

Seen rendered in dark mode at 4-up, which is how the three sizing defects above were found.
**Annual mode has not been checked at 4-up** — `.plan-rate` carries "฿26,892 / yr" plus the
save pill on one line and has no `nowrap`, so Professional is the card to look at first.

### Not shipped yet

- `11:24` **`supabase db push` has not been run** — `20260909000000_add_lite_plan.sql` is
  written but not applied, so `#/pricing` still shows three tiers until it is.
- `11:24` **The `sub_standard` pre-flight has run on dev only** (0 across all five tables).
  Re-run it on **prod** (`lhlncsjqxttcdegkqvid`) from the Supabase SQL Editor before
  trusting the cleanup there. Any non-zero count means the June rename did not fully land on
  prod, and the repoint (`20260629000000` lines 13-16) has to be re-run first — the frontend
  no longer carries the fallback.

      select 'credit_packs'         as tbl, count(*) from credit_packs         where code      = 'sub_standard'
      union all select 'credit_orders',        count(*) from credit_orders        where pack_code = 'sub_standard'
      union all select 'tenant_subscriptions', count(*) from tenant_subscriptions where plan_code = 'sub_standard'
      union all select 'credit_ledger',        count(*) from credit_ledger        where pack_code = 'sub_standard'
      union all select 'billing_documents',    count(*) from billing_documents    where pack_code = 'sub_standard';
- `11:24` **Pending release note**, user-visible, **MINOR** → `VERSION` 1.0.2 → 1.1.0 when the
  release is cut (batched, not per-PR):
  - EN: "New Lite plan — 100 documents a month for ฿290."
  - TH: "แพ็กเกจ Lite ใหม่ — 100 เอกสารต่อเดือน ราคา ฿290"
