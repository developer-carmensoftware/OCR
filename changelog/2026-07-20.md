# 2026-07-20

## `16:05` Document Preview shows only scanned/selected page

Preview iframe was showing every page of a multi-page PDF even when the user only scanned
page 1. Two flows fixed:

### Frontend

- `16:05` `hooks/ap-invoice/useAPExtraction.ts` — `confirmPageSelection` now clears the
  full-document preview URL immediately so the user never sees all pages while the subset
  PDF is being built. Fallback on failure uses `#page=N` to point at the selected page.
- `16:05` `hooks/credit-card/useFileUpload.ts` — PDF preview now uses
  `selectedPagesToPdfUrl(file, [0])` to extract only page 1 (credit card always processes
  page 1). Falls back to full PDF with `#page=1` if extraction fails.

## `16:00` Maintenance mode — global + per-tenant kill switch

Runtime-toggleable maintenance mode so a version update with a breaking migration can take
users offline briefly without shipping raw errors to in-flight wizards. Flag lives in
`system_configs` (no migration — table already existed), so a toggle takes effect in ≤10s
without a restart. Uncommitted / in-progress.

### Backend

- `16:00` **`services/maintenance_service.py` (new).** Reads `maintenance.enabled` /
  `maintenance.message` from `system_configs` + per-tenant overrides from
  `tenant_config_overrides`, cached in-process 10s. Semantics are **OR** (global OR the
  tenant's own override), not the usual override-replaces-global. `is_maintenance()` is
  **fail-open** — a DB read failure returns not-in-maintenance so a config-read outage can't
  brick the whole API.
- `16:00` **`middleware/maintenance.py` (new)** + registered in `factory.py`. Returns 503
  `{code: "maintenance"}` + `Retry-After` + `X-Maintenance: 1` for blocked requests. Allowlist:
  `/api/v1/health` (or Render restart-loops), `/api/v1/admin/*` (admins can toggle it back off),
  `/api/v1/maintenance/status`, docs. Reads tenant from JWT itself → order-independent.
- `16:00` **`GET /api/v1/maintenance/status` (new, public)** — the single source the frontend
  gate polls. **Admin `GET/PUT /admin/maintenance` + `PUT /admin/maintenance/tenant/{id}`**
  added to the existing admin maintenance router (`configs:write` guarded).

### Frontend

- `16:00` **`MaintenanceGate.tsx` (new)** mounted at root in `main.tsx` — full-screen EN/TH wall,
  triggered instantly by the `ocr:maintenance` event any 503 fires (`lib/api/client.ts`),
  confirmed on boot + polled every 30s so it clears itself when the backend returns.
- `16:00` **Admin page `#/admin/maintenance` (new)** — global switch + message + per-tenant
  toggles. 4-point wiring per CLAUDE.md (lazy, AdminRouter, NavItem, i18n en+th).

### Infra

- `16:00` **`deploy.yml`** — opt-in maintenance via `workflow_dispatch` input. Plain push
  deploys stay zero-downtime; a manual run with `maintenance=true` raises the flag (psql) before
  `db push` and lowers it after health passes (`always()`, so a failed deploy never sticks ON).

## `17:30` Maintenance mode — scheduled window (add-on)

Admin can now schedule a maintenance window ahead of time instead of flipping the switch at 2am.
Same `system_configs` store, still no migration.

### Backend

- `17:30` **`maintenance_service.py`** — new `maintenance.window_start/window_end` (UTC ISO).
  `is_maintenance` now computes `enabled OR (start <= now < end)` against the **live clock**, so the
  window auto-activates at start and auto-reopens at end with **no cron** (reuses the 10s cache).
  `set_schedule()` + `end_now()` (clears switch + window — covers "finished early" and "cancel").
  `get_status()` gained `active` (effective) + window fields.
- `17:30` **Endpoints** — `PUT /admin/maintenance/schedule` (validates `end > start`, 400 otherwise),
  `POST /admin/maintenance/end`; public `GET /maintenance/status` now returns `window_start/end` for
  the user-facing banner.

### Frontend

- `17:30` **`MaintenanceGate.tsx`** — advance-notice **banner + countdown** ("Scheduled maintenance
  … · in 2h 5m", dismissible per-window via localStorage) when a future window exists; overlay gained
  an "expected back by HH:MM" line. All times rendered in ICT.
- `17:30` **`MaintenancePage.tsx`** — Scheduled-window section: native `datetime-local` start/end +
  message, Save schedule + **End now**; effective status badge (Active / Scheduled / Off). EN/TH keys added.

### Tests

- `17:30` `test_maintenance_service.py` — added window cases (future/current/past) + `end_now`; 6 passed.
  ASGI check: during-window → 503, future-window → passthrough + banner data in `/status`.

### Fixes

- `18:10` **`MaintenanceGate` locked admins out of their own toggle.** The root-level overlay
  covered `#/admin/*` too, so once global maintenance was on there was no way to reach the switch to
  turn it off. Gate now suppresses the overlay + banner on admin/order-review routes (backend already
  allowlists `/api/v1/admin`), re-checked on `hashchange`.

## `18:40` Maintenance admin — one card, always time-bounded

Merged the separate "System-wide" and "Scheduled window" cards into one control, and **removed the
indefinite ON switch**: enabling system-wide maintenance now always requires a start+end window, so
it auto-reopens and users are never stranded. A "Now" button fills the start (and a default +1h end)
for immediate maintenance. `MaintenancePage.tsx` only; backend unchanged (`set_schedule` already the
sole enable path from the UI; the raw `PUT /admin/maintenance` {enabled} remains for deploy/emergency).

- `19:05` **Typed confirmation on system-wide enable/update.** Enabling/updating the global window
  now opens a warning modal that requires typing `MAINTENANCE` (case-insensitive) before it arms —
  guards against a fat-fingered click that blocks every user. Reuses `CustomModal`'s input +
  shake-on-mismatch (`errorNonce`); per-tenant toggles and End now are unchanged.

## `19:40` Maintenance UI — design-system pass (impeccable audit fixes)

Audit scored 14/20; this pass clears the weak dimensions (Theming, A11y, Responsive).

- `19:40` **New `styles/components/maintenance.css`** — moved all of `MaintenanceGate`'s inline styles
  into token-driven classes. Hard-coded hex/rgba → `var(--amber)` + committed scrim/ink constants;
  `z-index: 9998/9999` → the semantic scale (`--z-modal-backdrop` banner, `--z-modal` overlay);
  muted `opacity` → `var(--text-3)`; per-tenant border → `var(--border)`.
- `19:40` **A11y** — blocking overlay now moves focus to its retry button on open (WCAG 2.4.3);
  the "Now" button was pulled out of the Start `<label>` (was an ambiguous label association), inputs
  now use `htmlFor`; banner dismiss `×` given a 44px hit area + `:focus-visible` rings.
- `19:40` **Responsive** — fixed banner no longer covers app chrome: `body.has-maint-banner` reserves
  top padding and offsets the sticky `.app-header` so it docks below the banner on scroll.
- `20:00` **P3 follow-ups.** Overlay now traps Tab on its single control (keyboard focus can't reach
  the app behind the scrim); the `/status` poll skips the network call while the tab is hidden and
  re-checks on `visibilitychange`. Re-audit: 19→20/20.
- `20:20` **De-slop the wall.** Dropped the decorative 🛠️ emoji (the tell of an AI-generated
  maintenance page) for a lucide `Wrench` in an amber status chip — the app's own icon vocabulary
  (same glyph as the admin nav item) — with a slow ring-pulse that signals "working, not frozen"
  (reduced-motion aware).

## `20:55` Maintenance — review remediation

Detail pass before merge; found and fixed one real bug plus tech-debt.

- `20:55` **Bug fix (P0): per-tenant maintenance reload loop.** `MaintenanceGate.probe()` hit
  `/maintenance/status` without the auth token, so the poll only ever saw the *global* flag. For a
  tenant-scoped block, the 503 event raised the wall but the tokenless poll read `active:false` and
  fired `location.reload()` every 30s. Probe now sends the bearer token → status is tenant-aware,
  boot-detects per-tenant maintenance, and recovers without looping. Verified via ASGI: a tenant JWT
  returns `active:true`, no token returns `active:false`.
- `20:55` **Hardening: fail-open covers the whole compute.** Moved the active computation inside
  `is_maintenance`'s try/except so any error (not just the cache refresh) fails open instead of 500ing.
- `20:55` **Dead-code removal.** Dropped the caller-less manual on/off cluster after the always-bounded
  redesign: `setGlobalMaintenance` (FE), `PUT /admin/maintenance` + `set_global` + `GlobalMaintenanceRequest`
  (BE). The `maintenance.enabled` read path stays (deploy.yml writes it via psql). Unit suite green.

## `21:40` Maintenance — banner visual pass

- `21:40` **Banner re-skinned to the system's amber surface vocabulary.** It was a saturated
  `--amber` slab with a hardcoded ink token, which read as a foreign alert bar. Now `--amber-light`
  fill + `--amber-mid` hairline + `--amber-text` ink + `--shadow-sm` — all theme-aware, so dark mode
  is correct without a bespoke constant (`--maint-banner-ink` deleted).
- `21:40` **Structure over one flat sentence.** Split into lucide `CalendarClock` + label + mono
  window times + a countdown pill (mono per the data rule); the `×` glyph became a lucide `X` with
  hover/focus states. New i18n keys `maintenance.bannerTitle` / `maintenance.bannerIn` (en + th).
  Window times hide under 30rem; the countdown answers the same question.
- `21:40` **Motion.** Banner slides down (`--dur-slow`/`--ease-out`), overlay fades in; both collapse
  to a plain fade under `prefers-reduced-motion`.
- `21:55` **Per-tenant row buttons said the state, not the action.** The toggle reused the status
  strings, so a live tenant showed `ACTIVE` next to a button labelled "Under maintenance" — unreadable
  as on-or-off. Now verb + object: "Take offline" / "Bring back online" (`admin.maintenance.takeOffline`
  / `.bringOnline`, en + th), with a tenant-named `aria-label` so seven identical buttons are
  distinguishable to a screen reader. Dropped the solid `btn-confirm` fill for `btn-sm btn-outline`:
  a column of primary-blue buttons broke the one-voice rule on an otherwise quiet list.
- `22:10` **Banner dismiss is animated, not a cut.** It unmounted on click, so the bar vanished and the
  page snapped up 2.5rem in one frame. Dismiss now sets a `leaving` state → `.maint-banner--leaving`
  slides out over `--dur-base`, and the localStorage/state commit rides `onAnimationEnd` (guarded, since
  the entrance animation lands on the same handler). `has-maint-banner` is released on click rather than
  on commit so the page glides up *with* the banner; `body` gets a `padding-top` transition to carry it.
  Reduced motion swaps both directions for a fade (kept as an animation, not `none`, or the commit never
  fires) and drops the body transition.
- `22:25` **Dismiss hover was a slab.** The 44px touch target painted its own background, so hovering
  produced a square taller than the 2.5rem bar. The target stays 44px and invisible; a 1.65rem round
  `::before` puck carries the hover/focus tint instead (focus ring moved to the puck via box-shadow).
