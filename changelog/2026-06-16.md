# Changelog — 2026-06-16

_Backfilled from git history. Times = commit time (ICT, +0700)._

- `09:17` Handle password-protected PDFs; fix GL mapping snap-back after accept-all (`f17ba11`).
- `10:18` Native PDF page selection, WHT row filter, exception-log cleanup (`b7b1182`).
- `10:22` Handle session expiry and network errors with clear user feedback (`06302c6`).
- `10:22` Generate initial OCR project graph report (`2f21723`).
- `13:19` Decrypt encrypted PDFs before sending to Gemini; refund credit on extraction failure (`5364116`).
- `13:25` Merge `prod-deploy/vercel-render-neon` into Billing-Note (`8825359`).
- `14:38` `PaymentTypeModal` for configuring account-receivable mapping (`73eafbf`).
