# 2026-07-02

## Backend — fix double-VAT on tax invoice (accounting-critical)

`HH:MM` **fix(credits): tax invoice was double-VATed → Carmen AR received the wrong total** — the approve/decide flow issued the TAX_INVOICE by passing `order.amount_thb` (which is the **gross**, VAT-inclusive price the customer paid) into `issue_document`, whose contract is a **net** input that then gets VAT added on top. Net 490 → the tax invoice stored subtotal 524.30 / vat 36.70 / total **561.00** instead of 490 / 34.30 / **524.30** (`gross × 1.07`). `post_ar_entry` then forwarded the inflated total (and embedded it in the Description) to Carmen AR. The proforma was always correct (issued from net); this affected **every** tax invoice issued via approve.

- Fix ([admin/credits.py](backend/app/routers/admin/credits.py)): issue the tax invoice from the proforma's authoritative `subtotal` (net) so it reproduces the proforma exactly; fall back to `split_inclusive(order.amount_thb)` when no proforma. Added `split_inclusive` import.
- Regression guard ([test_admin_credits_api.py](backend/tests/integration/test_admin_credits_api.py)): `_proforma` fixture now carries subtotal/vat/total; the approve test asserts `issue_document` is called with the **net** (990), not the gross (1059.30).
- Data remediation ([scripts/fix_double_vat_tax_invoices.sql](scripts/fix_double_vat_tax_invoices.sql)): audit + correct already-issued double-VATed tax invoices from their proforma, plus a list of orders already posted to Carmen AR that finance must reverse/re-post (incl. `AI-202607-0005`). Carmen has no delete API from this app.
- **Ops**: needs `supabase db push` (applies `buyer_tel` column the ORM now references) + backend restart for this fix **and** the 3-line AR remark (Contact Name / Tel. / Email) to take effect — the observed single-line `Contact Name : -` was stale pre-edit code.

## Feature — buyer `Tel.` on billing info / proforma

`HH:MM` **feat(checkout): add Tel. field to billing info, printed on proforma** — new optional `Tel.` field in the checkout Billing info form ([CheckoutFlow.tsx](frontend/src/components/pricing/CheckoutFlow.tsx)), captured before proforma generation and printed in the proforma buyer block ([ProformaDocument.tsx](frontend/src/components/pricing/ProformaDocument.tsx)). Mirrors the existing `contact_name` field end-to-end.

- **DB**: migration `20260702000000_add_buyer_tel.sql` — `billing_documents.buyer_tel VARCHAR(50)` (needs `supabase db push`).
- **Backend**: `buyer_tel` column ([billing.py](backend/app/models/billing.py)); `tel` on `BuyerInfoInput` (+len validator) / `CompanyProfileResponse` and `buyer_tel` on `BillingDocumentResponse` ([schemas/credits.py](backend/app/models/schemas/credits.py)); threaded through `BuyerInfo`/insert/last-buyer prefill ([billing_document_service.py](backend/app/services/billing_document_service.py)); Carmen prefill from `RegTel`/`RegPhone` ([carmen_service.py](backend/app/services/carmen_service.py)); passed into `BuyerInfo` in [credits.py](backend/app/routers/credits.py) + [admin/credits.py](backend/app/routers/admin/credits.py).
- **Frontend**: `tel`/`buyer_tel` types ([credits.ts](frontend/src/lib/api/credits.ts)); checkout default + Carmen prefill ([useCheckout.ts](frontend/src/hooks/credits/useCheckout.ts)); `checkout.fieldTel`/`checkout.phTel` EN + TH ([dict.ts](frontend/src/i18n/dict.ts)).

## Frontend

`10:30` **fix(i18n): restore "ERP" in Carmen Cloud post-to strings** (`orev.inv.sub`, EN + TH)

`15:40` **fix(pricing): center payment-slip upload dropzone** — `SlipUpload.tsx`'s inline `display: 'block'` + `textAlign: 'left'` was overriding `.upload-drop`'s flex-centering, pushing icon/label/hint to the left edge; trimmed the inline style down to just the button-reset properties (`border`, `background`, `cursor`) and let the shared class own layout + padding.

`06:28` **fix(ap-invoice): stop infinite vendor/tax-profile refetch loop** — `useAPVendor.ts`'s `loadVendors` and `autoMatchVendor` were plain functions recreated every render; `useAPInvoice.ts`'s effects depended on them by reference, so each call's `setState` triggered a new function identity, re-firing the effect and re-fetching `/carmen/vendors` + `/carmen/tax-profiles` in an unbroken loop until Carmen 429'd and React threw "Maximum update depth exceeded". Wrapped both in `useCallback`.
