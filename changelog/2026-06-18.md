# Changelog — 2026-06-18

_Backfilled from git history. Times = commit time (ICT, +0700)._

- `14:52` Complete billing purchase flow — open-order guard, cancel, proforma + polish (`6c80236`).
- `14:54` Stop tracking `supabase/.temp/cli-latest`, already gitignored (`8b82bdf`).
- `17:02` Cast `tenant_id` to string in summary-service aggregation to prevent pg_cron data errors and key mismatches (`c3bb225`).
