# Changelog — 2026-06-23

_Times in ICT (+0700). Committed entries = commit time; uncommitted = when the work was done._

## `16:00` — Order-Review: wire up the Post-to-AR loop (FRD reconciliation)

The Post-to-Carmen-AR action existed in the backend but had **no UI** — orders could reach Paid but
never Complete. Closed the loop and surfaced AR-readiness in the queue. (FRD FT-02/FT-03.)

### Backend
- `models/schemas/credits.py` — `ArCustomerProfileUpdate.carmen_ar_code` now validated to ≤15
  `[A-Za-z0-9-]` (FRD; column stays VARCHAR(50), widen-safe). `CreditOrderResponse` gains
  `proforma_number` + `carmen_ar_code` (admin-queue only). `KpiSummaryResponse.status_counts`.
- `routers/admin/credits.py` — `list_credit_orders` LEFT JOINs the proforma + AR profile so each row
  carries its proforma number and resolved AR code. `get_kpi` adds per-status counts (group_by).

### Frontend
- `components/admin/OrderQueue.tsx` — tabs now use **status words** (View All / In Progress / Paid /
  Complete / Void) with **count badges**; rows show **proforma # + AR-code chip (✓ / ⚠ Unmapped)**;
  **Paid** tab gets selection checkboxes + "Post Batch (N)".
- `components/admin/OrderWorkspace.tsx` — `paid` branch adds a single **"Post to AR"** button (shows
  the AR code), disabled with "Map AR code first" → AR tab when the buyer is unmapped.
- `pages/admin/CreditOrdersPage.tsx` — batch-selection state; re-syncs `selected` from the reloaded
  (enriched) list; opens the AR-mapping tab from the workspace.
- `lib/api/adminClient.ts` — `proforma_number`/`carmen_ar_code` on `AdminCreditOrder`,
  `status_counts` on `KpiSummary`.
- `i18n/dict.ts` — tab labels repointed to `orev.status.*`; removed dead workflow-name keys; added
  `orev.inv.unmapped` / `orev.inv.mapToAr` / `orev.toast.postedSummary`.
- `styles/components/admin-credit-orders.css` — checkbox column, `.orev-ar-chip`, `.orev-tab-count`,
  `.orev-post-batch`.

### Tests
- Updated `_order`/`_list_result` (4-tuples) + KPI mock (5th group_by query); added coverage for
  `status_counts` and the enriched list row. **35/35 pass; tsc clean.**

### Deferred (per decision)
- Unpost/Rollback (until Carmen delete/void API exists); AR-profile address/contact display.

## `13:00` — Order-Review redesign: 2-tab layout, 4-status workflow, Carmen AR mapping (mock)

Replaced the slip-review console with a tabbed order-review page. Collapsed the 6-value
`creditorderstatus` enum to 4 (`in_progress → paid → complete → void`), added a Customer-Profile→Carmen-AR
mapping tab, KPI cards, and a mock Carmen AR batch-posting flow. Removed admin grant/adjust credit controls
(review + post only); credit ledger/history stays read-only.

### DB
- `20260623000000_ar_customer_profiles.sql` — new `ar_customer_profiles` table (buyer name/tax-id/branch + `carmen_ar_code`), partial unique index on `(buyer_tax_id, buyer_branch)`.
- `20260623000100_credit_orders_carmen_ar.sql` — `credit_orders.carmen_ar_posted_at`, `carmen_ar_ref`.
- `20260623000200_credit_order_status_v2.sql` — enum swap to `('in_progress','paid','complete','void')` (drop default + open-order index first, then `USING` cast, drop/rename type).
- `20260623000300_update_indexes_seed_profiles.sql` — rebuild `uq_credit_orders_one_open_per_tenant` (WHERE `status = 'in_progress'`); seed `ar_customer_profiles` from `billing_documents`.
- `supabase/schemas/51_billing.sql` — declarative schema brought in line (enum, default, open-order index, new columns + table).

### Backend
- `models/enums.py` — `CreditOrderStatus` → 4 values.
- `models/billing.py` — `ArCustomerProfile` model; `carmen_ar_*` columns; open-order index WHERE `in_progress`.
- `models/schemas/credits.py` — `ArCustomerProfileResponse/Update`, `PostArRequest/Response`, `KpiSummaryResponse`; `carmen_ar_*` on `CreditOrderResponse`; `HoldRequest` reduced to note-only.
- `services/ar_posting_service.py` — **mock** Carmen AR post (`post_ar_entry`); swap body when the real API lands.
- `routers/admin/credits.py` — new `ar-customer-profiles` list/patch/sync, `credit-orders/post-ar`, `credit-orders/kpi`; approve→`paid`, reject/cancel→`void`, hold→note-only; KPI scope applied consistently; sync de-dupes empty-tax-id buyers in-app.
- `routers/credits.py` — status refs → `in_progress`/`void`; slip upload keeps `in_progress`.

### Frontend
- `pages/admin/CreditOrdersPage.tsx` — rewritten: KPI cards + main tabs (AR Mapping | Invoice Verifier).
- `components/admin/OrderKpiCards.tsx`, `ArCustomerProfiles.tsx` — new.
- `components/admin/OrderQueue.tsx` — workflow tabs (View All / Requested / Accounting Verify / AR Interface); `complete` tone.
- `components/admin/OrderWorkspace.tsx` — status-aware actions (approve/void, post-AR readonly, complete/void terminal); grant/adjust removed; admin-note uses `orev.note.*`.
- `lib/api/adminClient.ts` — 4-value `AdminOrderStatus`, `ArCustomerProfile`/`KpiSummary`/`PostAr*` types, AR + KPI + post-ar fns; `holdOrder` → `updateOrderNote` (dropped dead `_hold` param).
- `lib/api/endpoints.ts` — AR profile, KPI, post-ar endpoints; `creditOrderNote`.
- `lib/api/credits.ts` + `components/pricing/OrderStatusBadge.tsx`, `PendingOrderBanner.tsx`, `pages/OrderHistory.tsx`, `Pricing.tsx`, `hooks/credits/useOrderHistory.ts` — customer-facing status refs.
- `i18n/dict.ts` — EN/TH: new mainTab/workflow-tab/KPI/AR/note/status keys; removed orphaned grant/adjust/topup keys.
- `styles/components/admin-credit-orders.css` — KPI cards, main tabs, AR table, `is-done` tone.

## `14:30` — Post-implementation audit + cleanup

Audited the redesign; fixed 2 bugs + stale artifacts.

- **Bug** — AR profiles footer rendered the raw EN empty-string via `.replace()` (broke TH, double-counted) → single `orev.ar.count` key.
- **Bug** — `sync_ar_profiles` duplicated empty-tax-id buyers every run (not covered by the unique index) → in-app `(name, branch)` de-dupe.
- **Tech-debt** — borrowed `orev.ar.*` keys in the workspace note flow → dedicated `orev.note.*`; dropped dead `_hold` param; KPI totals now respect tenant scope (global-admin only today).
- **Tests** — `test_admin_credits_api.py` + `test_credits_api.py` updated to the new statuses/flow; added coverage for post-AR (happy + no-AR-code + non-paid), KPI shape, AR profile list/update. **All 34 tests pass.**
  - Also repaired 3 pre-existing mock-fidelity failures (not status-related): `_pack` now sets `description`; `_billing_doc` sets `buyer_email`/`buyer_contact_name`; create-order tests use `side_effect` (one-open-order pre-check → None) and wire `add`/`flush` so the new order gets its uuid id.
