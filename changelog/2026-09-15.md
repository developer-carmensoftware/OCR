# 2026-09-15

`09:20` Pre-PR check on `feat/allow-plan-downgrade`: `PlanCard.test.tsx` still asserted the
old "Switch to {name}" / "Switch" wording after the branch's last commit renamed
`plan.ctaSwitch`/`plan.switch`/`plan.switchNote` → `ctaChange`/`change`/`changeNote` and
changed the accessible name to "Change to {name}" — 2 of 696 frontend tests were failing.
Updated the test assertions and the two now-stale "Switch" comments in `PlanCard.tsx` to
match. Full suite green again: frontend 696/696 + tsc clean, backend `pytest tests/` exit 0,
ruff clean.

## Uncommitted / in progress

`10:12` **Frontend — plan-change warning now stands on the payment step, not only in the
confirm dialog** (risk R1 from the PR #219 review report). `planChangeLoss()`'s message was
only ever rendered inside `SlipUpload`'s confirm dialog, which cannot open until a slip file
is attached — i.e. after the buyer has already transferred the money, with no refund policy
behind them. `SlipUpload.tsx` now also renders that same message as a standing
`.mapping-alert` banner (the app's existing alert pattern) in **both** of its states, so
it is on screen from the moment step 2 "ชำระเงิน" loads — while the proforma is still being taken to the
approver and before any transfer. The dialog gate is unchanged and still fires on the final
click. Fixed in `SlipUpload` itself rather than in its callers, so checkout and the
pending-order banner (which reaches the same button without passing through checkout) both
get it from one change.

- Tests: 3 existing assertions keyed on the warning *text* now scope to `role="dialog"`
  instead — the old "text is gone after cancel" expectation is deliberately no longer true,
  since the banner is meant to persist. Added one test pinning the banner pre-dialog.
  frontend 697/697, `tsc --noEmit` clean, eslint clean on the touched files.
- `pricing.css`: 3-line `margin-bottom: 0` override for `.mapping-alert` inside
  `.slip-chosen` / `.pending-order-actions` — both are gap-spaced columns, so the
  alert's own block margin would double the spacing (same override the review queue
  already carries for the same reason).
- Not addressed here: the printed proforma body still says nothing about the consequence
  (that is the approver-facing document — bigger call), R3 (mid-cycle renewal forfeits days
  with no warning), R4 (admin slip review does not show the buyer's current plan).
- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts`
  deliberately untouched, batch at release time:
  - Downgrading or switching from annual to monthly now shows what you lose on the payment
    step itself, before you transfer — not only at the final confirm.
