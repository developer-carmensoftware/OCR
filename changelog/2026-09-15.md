# 2026-09-15

`09:20` Pre-PR check on `feat/allow-plan-downgrade`: `PlanCard.test.tsx` still asserted the
old "Switch to {name}" / "Switch" wording after the branch's last commit renamed
`plan.ctaSwitch`/`plan.switch`/`plan.switchNote` → `ctaChange`/`change`/`changeNote` and
changed the accessible name to "Change to {name}" — 2 of 696 frontend tests were failing.
Updated the test assertions and the two now-stale "Switch" comments in `PlanCard.tsx` to
match. Full suite green again: frontend 696/696 + tsc clean, backend `pytest tests/` exit 0,
ruff clean.

`10:12` **Frontend — plan-change warning now stands on the payment step, not only in the
confirm dialog** (risk R1 from the PR #219 review report). `planChangeLoss()`'s message was
only ever rendered inside `SlipUpload`'s confirm dialog, which cannot open until a slip file
is attached — i.e. after the buyer has already transferred the money, with no refund policy
behind them. `SlipUpload.tsx` now also renders that same message as a standing
`.mapping-alert` banner (the app's existing alert pattern) in **both** of its states, so
it is on screen from the moment step 2 "ชำระเงิน" loads — while the proforma is still being taken to the
approver and before any transfer. The dialog gate is unchanged and still fires on the final
click. Fixed in `SlipUpload` itself rather than in its callers, so checkout and the
pending-order banner (which reaches the same button without passing through checkout) both
get it from one change.

- Tests: 3 existing assertions keyed on the warning *text* now scope to `role="dialog"`
  instead — the old "text is gone after cancel" expectation is deliberately no longer true,
  since the banner is meant to persist. Added one test pinning the banner pre-dialog.
  frontend 697/697, `tsc --noEmit` clean, eslint clean on the touched files.
- `pricing.css`: 3-line `margin-bottom: 0` override for `.mapping-alert` inside
  `.slip-chosen` / `.pending-order-actions` — both are gap-spaced columns, so the
  alert's own block margin would double the spacing (same override the review queue
  already carries for the same reason).
- Not addressed here: the printed proforma body still says nothing about the consequence
  (that is the approver-facing document — bigger call), R3 (mid-cycle renewal forfeits days
  with no warning), R4 (admin slip review does not show the buyer's current plan).
- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts`
  deliberately untouched, batch at release time:
  - Downgrading or switching from annual to monthly now shows what you lose on the payment
    step itself, before you transfer — not only at the final confirm.

Shipped as [PR #227](https://github.com/developer-carmensoftware/OCR/pull/227) (`6a429ba`,
merged `ff85f37`). Full CI green.

## Uncommitted / in progress

`10:59` **Frontend — the plan-change warning reported only the cheaper of two losses**
(risk R2 from the PR #219 review report; branch
`fix/plan-change-warning-covers-both-losses`). `planChangeLoss()` returned on the first
matching condition, so `packCredits < doc_allowance` always exited before the annual-term
check could run. The two losses are independent and an annual → smaller-monthly change trips
both — verified by enumerating all 64 catalog transitions: **exactly 6** land there, and each
reported only the quota drop, the cheaper half. Up to ~THB 24,651 of prepaid annual term went
unmentioned (Pro annual ฿26,892 abandoned in month 1).

Not a silent failure — the quota copy already ended with *"Documents and days remaining on
your current plan are not carried over"* — but a buyer on an annual plan reads "days
remaining" as the rest of this month, not eleven prepaid months. So this is an
understatement fixed by saying the term loss outright, not a missing warning.

- `billing.ts`: both conditions are now evaluated before returning, adding a third result
  `'both'`. The docstring already said the term loss *"costs more than any tier drop"* — the
  code contradicted it.
- `billing.ts` also gains `planChangeWarning(t, loss, prev, next)`, which absorbs the
  identical 9-line ternary that `CheckoutFlow.tsx` and `PendingOrderBanner.tsx` each carried.
  Net ~6 fewer lines, and one place to get a new loss kind right. `t` is a parameter, not an
  import, so it stays a plain function and the copy stays in `dict.ts` per that file's own
  no-rendered-strings rule. Chose this over editing both ternaries because **`CheckoutFlow`
  has no test file at all** — an unhandled `'both'` there would have fallen through to
  `undefined` and shown *no* warning, with nothing to catch it.
- `dict.ts`: one new key `slip.changeWarnBoth`, EN + TH, same `{prev}`/`{next}` placeholders
  in both (`dict.test.ts` compares placeholder sets across languages and fails on a mismatch).
- Tests: +6 (one `'both'` case, four mapping cases driven by a fake `t`, one component case).
  Mutation-checked — reverting the one-line fix fails exactly those two new behavioural tests
  and no others. frontend 703/703, `tsc --noEmit` clean, eslint 0 errors.
- Triaged the rest of both reports after this landed — see the `12:01` entry.
- Deliberately not done: naming the months or the baht at stake. Says *what* is lost, not
  *how much*; `sub.period_end` is there if that changes.
- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts`
  untouched, batch at release time:
  - Switching from an annual plan down to a smaller monthly one now says both things you
    give up — the lower quota *and* the months you already paid for.

`12:01` **Frontend — shorter warning copy, and a button that stops calling a plan change a
renewal.** Same branch as the `10:59` entry; all of it ships as one PR. Both review reports
(PR #219, PR #227) were triaged in full first: of ten risks, four are closed as deliberate
no-ops, one shipped in PR #227, and the rest are split across this PR and two follow-ups.

- **The warnings were too long to be read** — the worst case ran 44 English words in a banner
  shown to staff who, per `PRODUCT.md`, "want to finish the upload … and move on". Copy that
  long in a task flow gets skipped, which lands back where PR #227 started. All three
  `slip.changeWarn*` strings rewritten on one shape (condition, loss, carry-over rule) in EN
  and TH: 44 → 25 words worst case, 30-45% shorter each. "When approved" stays — the plan
  changes when an admin approves the slip, and a buyer told "immediately" reloads, sees the
  old plan, and reports a bug.
- **`PlanCard` called a plan change a renewal.** `isCurrentPlan` matched on the plan code
  alone, so Growth monthly → Growth annual read "Renew plan" — while `planChangeLoss()` was
  already classifying that same purchase as a change. The period is now part of the match,
  normalised `?? 'monthly'` the way `PendingOrderBanner` already does. `Pricing.tsx` passes
  `activeSub.billing_period` alongside the code and credits it already passed.
- **The state the banner exists for had no test** (report N2, verified): `renderUpload` in
  `SlipUpload.test.tsx` hardcodes `initialFile` before `{...props}`, so every test — including
  the one added in PR #227 — ran in the chosen-file state. The drop zone, which is what a buyer
  sees *before* transferring, was uncovered: deleting its banner passed CI. Closed.
- **Screen readers now get the warning** (report N3): `aria-describedby` from both actions to
  the banner, reusing `PlanCard`'s pattern. Chosen over `role="alert"`, which announces on
  insertion only and so never fires on the pending-order path, where the banner is present at
  page load.
- Tests +3, mutation-checked: reverting either fix fails exactly its own test and no others.
  frontend 706/706, `tsc --noEmit` clean, eslint 0 errors.

**Triage outcome for the remaining risks** (decisions, so they stop being re-reported):

- **Not doing, deliberately.** R3 — renewal resets the period from `now()` rather than
  extending; that matches the use-it-or-lose-it allowance the product is sold on, and the
  "Renew plan" wording stays (reasoning recorded in `PlanCard.tsx`). R4 — the approving admin
  does not need the buyer's current plan; their job is confirming money arrived, not reviewing
  a commercial decision the buyer already confirmed. R6 — the card tooltip stays invisible on
  mobile now that the payment step carries a visible banner. N1 — the banner stays below the
  invoice; shortening the copy was the fix, not moving it.
- **Next PR (prose).** R5 — `docs/Billing_Purchase_Flow.md` is wrong in 3 of 8 sections, not a
  few sentences: it documents a proration formula with a worked ฿797.15 example for a function
  that is never called, plus a whole section arguing annual→monthly is impossible. Delete both
  rather than rewrite. Plus `Software_Requirements_Specification.md:756`. Plus **three code
  comments that still describe the "Option A" purchase guard deleted in PR #219** (found while
  checking, in neither report): `activate_subscription()`'s docstring, the
  `uq_tenant_subscriptions_one_active` migration comment, and `billing.py:261`. The index
  itself does exist, so the unconditional supersede is safe — only the comments lie. Plus N4:
  `changelog/README.md` never says to drop the "Uncommitted / in progress" heading once work
  ships, which is why 21 of 57 day files still carry it.
- **Next PR (scripts).** R7 is four scripts, not the one the report named — `email_ingest_e2e`,
  `seed_review_queue` and `reset_email_test` are unguarded too. The repo already solves this at
  `scripts/email_multibu_loadtest.py:68`; copy those two lines into each.

- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts` untouched:
  - Plan-change warnings are shorter and say the same thing.
  - Switching between monthly and annual on your current plan now reads "Change plan" instead
    of "Renew plan".

`12:09` **Docs + stale comments — `docs/Billing_Purchase_Flow.md` matches the code again, and
three comments stop describing a guard PR #219 deleted** (R5 from both review reports).

- The doc was wrong in 3 of 8 sections, structurally rather than in wording: a whole section
  documenting a proration formula with a worked ฿797.15 example, for a function that is never
  called (`routers/credits.py:167` hardcodes `Decimal("0.00")`), plus a whole section arguing
  annual plans can't switch to monthly — which the product now allows outright. Deleted both
  rather than rewritten (documenting a formula nobody runs is what produced this rot); replaced
  with a short "No Proration" note that states the fact and points at the two real sources of
  truth (`routers/credits.py`, `constants/billing.ts`) instead of re-describing behavior that
  can drift again. Overview, Step 1's button-state bullets, Step 3, the Guards table, and the
  Audit Trail's `proration_credit_thb` line were each individually wrong and are now corrected.
  Plus one matching line in `docs/Software_Requirements_Specification.md:756`.
  `docs/Database_Design.md`'s proration mentions are left alone — they're migration history
  ("added on 2026-06-24/25"), which is correct as history, not a description of current
  behavior.
- Three code comments still described the "Option-A purchase guard" deleted in PR #219 — found
  while checking this, in neither review report: `activate_subscription()`'s docstring and the
  `TenantSubscription` model docstring in `backend/app/`. Both rewritten to state what actually
  stops two active subscriptions per tenant today (`uq_tenant_subscriptions_one_active`), not a
  guard that no longer runs. **Left `supabase/migrations/20260619000000_billing_subscriptions.sql`
  untouched** — CLAUDE.md is explicit that applied migration files are never edited, and unlike
  the two docstrings this comment describes the guard as it stood *when that migration was
  written*, which is accurate as history in the same way `Database_Design.md`'s mentions are.
- N4: `changelog/README.md` never said to remove the "Uncommitted / in progress" heading once
  work ships, which is why 21 of 57 day files still carried it. Added the rule. Renamed the
  heading in those 21 files to `## Not yet shipped at time of writing` (kept each file's own
  descriptive title where it had one) rather than back-filling `Shipped as PR #nnn` — that would
  mean guessing which PR closed a two-month-old entry across 21 files, with a real chance of
  guessing wrong. Left today's own still-open heading alone; it was accurate when
  written. (By the time this entry merges, PR #228 above has already shipped — same as
  the 21 files, not backfilled here either; a later entry can note it once rather than
  each one that was only true at the moment it was written.)
- Verified: `python -c "import app.services.credit_service; import app.models.billing"` — both
  edited files are docstring/comment-only, import cleanly.

Branch `docs/billing-flow-matches-code`, off `main` (separate from the frontend PR so the
security-adjacent script guards in the next PR aren't buried behind a documentation diff, and so
this one can be reviewed as pure text).

`12:11` **Scripts — four dev scripts guarded against pointing at production** (R7 from the PR
#227 review, which named only one of four). `scripts/seed_browser_session.py`,
`email_ingest_e2e.py`, `seed_review_queue.py`, and `reset_email_test.py` all write to whatever
`DATABASE_URL` resolves to, with nothing stopping that from being the production Supabase
project — `seed_browser_session.py` is the sharpest of the four, since what it writes is a
session that bypasses SSO entirely.

The repo already solved this once, at `scripts/email_multibu_loadtest.py:68,123-138`: a
`PROD_PROJECT_REF` constant checked against the resolved DSN, `sys.exit` on a match. Copied the
same two lines into all four — right after each script builds its `DSN`, so the check runs at
import time, before any connection opens. `reset_tenant_email.py` imports `DSN` from
`reset_email_test.py` (`from reset_email_test import DSN, _unread`), so it inherits the guard
for free without being touched.

- Verified: `python -m py_compile` on all four, `ruff check` clean.
- Kept as its own PR, off `main`, separate from the docs PR — a guard against writing to
  production is worth reviewing on its own, not folded into a documentation diff.


`14:11` **Frontend — warning copy trimmed further, to fit one line at desktop width.** Follow-up
on the `12:01` entry, same branch/PR (#228). Measured against `.mapping-alert`'s actual layout
(14px font, card padding) — desktop gives roughly a 90-character budget per line; mobile at
~400px gives roughly 35-45. Presented the trade-off rather than guessing: hitting the mobile
budget meant cutting real content (dropping `{prev}`, the starting number, or dropping "when
approved" and relying on the icon alone). Went with fitting desktop instead, which keeps every
number and the "when approved" framing intact.

- EN: 117/106/151 chars → 96/84/104. TH: 100/87/140 chars → 74/70/97.
- `slip.changeWarnBoth` (104 chars) still runs slightly past the desktop budget — it is
  reporting two losses at once, and is the rarest case (6 of 64 catalog transitions), so it was
  not worth cutting further at the cost of "and" or a number.
- 3 assertions in `PendingOrderBanner.test.tsx` updated to match; no behavior or test *shape*
  changed, only the strings they match against.
- frontend 706/706, `tsc --noEmit` clean.
