# 2026-09-15

`09:20` Pre-PR check on `feat/allow-plan-downgrade`: `PlanCard.test.tsx` still asserted the
old "Switch to {name}" / "Switch" wording after the branch's last commit renamed
`plan.ctaSwitch`/`plan.switch`/`plan.switchNote` → `ctaChange`/`change`/`changeNote` and
changed the accessible name to "Change to {name}" — 2 of 696 frontend tests were failing.
Updated the test assertions and the two now-stale "Switch" comments in `PlanCard.tsx` to
match. Full suite green again: frontend 696/696 + tsc clean, backend `pytest tests/` exit 0,
ruff clean.

`10:12` **Frontend — plan-change warning now stands on the payment step, not only in the
confirm dialog** (risk R1 from the PR #219 review report). `planChangeLoss()`'s message was
only ever rendered inside `SlipUpload`'s confirm dialog, which cannot open until a slip file
is attached — i.e. after the buyer has already transferred the money, with no refund policy
behind them. `SlipUpload.tsx` now also renders that same message as a standing
`.mapping-alert` banner (the app's existing alert pattern) in **both** of its states, so
it is on screen from the moment step 2 "ชำระเงิน" loads — while the proforma is still being taken to the
approver and before any transfer. The dialog gate is unchanged and still fires on the final
click. Fixed in `SlipUpload` itself rather than in its callers, so checkout and the
pending-order banner (which reaches the same button without passing through checkout) both
get it from one change.

- Tests: 3 existing assertions keyed on the warning *text* now scope to `role="dialog"`
  instead — the old "text is gone after cancel" expectation is deliberately no longer true,
  since the banner is meant to persist. Added one test pinning the banner pre-dialog.
  frontend 697/697, `tsc --noEmit` clean, eslint clean on the touched files.
- `pricing.css`: 3-line `margin-bottom: 0` override for `.mapping-alert` inside
  `.slip-chosen` / `.pending-order-actions` — both are gap-spaced columns, so the
  alert's own block margin would double the spacing (same override the review queue
  already carries for the same reason).
- Not addressed here: the printed proforma body still says nothing about the consequence
  (that is the approver-facing document — bigger call), R3 (mid-cycle renewal forfeits days
  with no warning), R4 (admin slip review does not show the buyer's current plan).
- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts`
  deliberately untouched, batch at release time:
  - Downgrading or switching from annual to monthly now shows what you lose on the payment
    step itself, before you transfer — not only at the final confirm.

Shipped as [PR #227](https://github.com/developer-carmensoftware/OCR/pull/227) (`6a429ba`,
merged `ff85f37`). Full CI green.

## Uncommitted / in progress

`10:59` **Frontend — the plan-change warning reported only the cheaper of two losses**
(risk R2 from the PR #219 review report; branch
`fix/plan-change-warning-covers-both-losses`). `planChangeLoss()` returned on the first
matching condition, so `packCredits < doc_allowance` always exited before the annual-term
check could run. The two losses are independent and an annual → smaller-monthly change trips
both — verified by enumerating all 64 catalog transitions: **exactly 6** land there, and each
reported only the quota drop, the cheaper half. Up to ~THB 24,651 of prepaid annual term went
unmentioned (Pro annual ฿26,892 abandoned in month 1).

Not a silent failure — the quota copy already ended with *"Documents and days remaining on
your current plan are not carried over"* — but a buyer on an annual plan reads "days
remaining" as the rest of this month, not eleven prepaid months. So this is an
understatement fixed by saying the term loss outright, not a missing warning.

- `billing.ts`: both conditions are now evaluated before returning, adding a third result
  `'both'`. The docstring already said the term loss *"costs more than any tier drop"* — the
  code contradicted it.
- `billing.ts` also gains `planChangeWarning(t, loss, prev, next)`, which absorbs the
  identical 9-line ternary that `CheckoutFlow.tsx` and `PendingOrderBanner.tsx` each carried.
  Net ~6 fewer lines, and one place to get a new loss kind right. `t` is a parameter, not an
  import, so it stays a plain function and the copy stays in `dict.ts` per that file's own
  no-rendered-strings rule. Chose this over editing both ternaries because **`CheckoutFlow`
  has no test file at all** — an unhandled `'both'` there would have fallen through to
  `undefined` and shown *no* warning, with nothing to catch it.
- `dict.ts`: one new key `slip.changeWarnBoth`, EN + TH, same `{prev}`/`{next}` placeholders
  in both (`dict.test.ts` compares placeholder sets across languages and fails on a mismatch).
- Tests: +6 (one `'both'` case, four mapping cases driven by a fake `t`, one component case).
  Mutation-checked — reverting the one-line fix fails exactly those two new behavioural tests
  and no others. frontend 703/703, `tsc --noEmit` clean, eslint 0 errors.
- Still open from the same report: R3 (mid-cycle renewal forfeits days silently), R4 (the
  approving admin never sees the buyer's current plan — `AdminCreditOrder` carries no
  `plan_code`, `doc_allowance` or `billing_period`), R5 (`docs/Billing_Purchase_Flow.md`
  still documents a proration engine that always writes `0.00`).
- Deliberately not done: naming the months or the baht at stake. Says *what* is lost, not
  *how much*; `sub.period_end` is there if that changes.
- **User-visible (for the eventual release note)** — `VERSION` and `releaseNotes.ts`
  untouched, batch at release time:
  - Switching from an annual plan down to a smaller monthly one now says both things you
    give up — the lower quota *and* the months you already paid for.
