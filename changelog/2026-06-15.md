# Changelog — 2026-06-15

_Backfilled from git history. Times = commit time (ICT, +0700)._

- `09:35` Order history page + standalone PromptPay dynamic QR generation demo (`b32652c`).
- `13:28` Migrate to Supabase-native stack + legacy cleanup (`89a0db9`).
- `14:09` Integrity hardening — fix correction upsert bug, enforce 1:1 `task_id`, add CHECK constraints (`8427ea4`).
- `14:10` Add temporary directory for CLI tracking (`9c97513`).
