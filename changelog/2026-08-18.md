# 2026-08-18

## Uncommitted / in progress

`09:30` **Email-automation notifications now say something and open something.**
`document_posted` / `document_failed` have been written to `user_notifications` since the
pipeline shipped, but the bell was never taught about them: the checklist in
`MEMORY.md` (`TYPE_META` + switch case + en/th keys) was skipped, so a row rendered the
literal string `document_posted` under a generic grey bell, and clicking it navigated to
`#/pricing/orders` — the credit-order history, an unrelated page it landed on only because
`order_id` is `null` for email documents.

### Frontend

- `components/common/NotificationDetailModal.tsx` (new) — the two rows now open a detail
  dialog **in place** instead of navigating. Shows document (attachment filename), bank
  (display name via `OCR_BANK_MAP`), doc no., JV no. (posted only) and the processed
  timestamp, plus a translated `reason_code` and the verbatim pipeline/Carmen error for
  support. There is no tenant-facing email-document page to navigate to — the admin list at
  `#/admin/email` is admin-only — and the payload already carries the whole story.
- `components/common/CustomModal.tsx` — gained an optional `children` slot (rendered between
  message and actions). That is the entire reuse: the detail dialog inherits the focus trap,
  Escape handling, portal, framer-motion entrance and reduced-motion path already tested there.
- `components/common/NotificationBell.tsx` — `TYPE_META` entries (`FileCheck2`/success,
  `FileWarning`/error; file-shaped so they don't read as the circle-shaped order rows),
  `notifText` cases, and a `document_*` branch in `handleItem` that opens the dialog rather
  than setting the hash. Mark-read behaviour is unchanged.
- `lib/api/notifications.ts` — `document_posted` / `document_failed` added to the type union
  (it had 4 members while the backend wrote 6).
- `i18n/dict.ts` — `notif.docPosted/docFailed`, the `notif.detail.*` labels, and the
  `notif.reason.*` sentences for the five codes the pipeline actually raises
  (`docs/email-automation/04-data-model.md` taxonomy), EN + TH.
- `styles/components/notification-bell.css` — `.notif-detail*` readout (label/value grid,
  mono values, scrolling raw-error block) and a 2-line clamp on `.notif-bell__text`, since an
  attachment filename is longer than the 340px panel.

### Backend

- `services/email_ingest_service.py` `_finish()` — the notification payload now carries
  `attachment`. It is the only identity a customer recognises when a document failed before
  extraction and therefore has no `bank_code` or `doc_no`.

`09:50` **Two gaps found on review of the above.**

- **The filename showed as "—" on every existing notification.** The payload only started
  carrying `attachment` an hour earlier, so all 8 live rows predated it. Migration
  `20260818000000_backfill_notification_attachment.sql` puts the name back by joining
  `payload->>'document_id'` to `email_documents.attachment` — applied to prod: 4 of 8 rows
  recovered, the other 4 have no ledger row left (a BU reset deleted them) and keep the "—".
  One-shot; `_finish` writes the key from now on and the table purges at 30 days.
- **A posted document now offers "Open JV in Carmen".** `jv_no` in the payload is Carmen's
  own JV id (the post's `InternalMessage`, e.g. `982`), the same value
  `hooks/credit-card/useOcrSubmission.ts` hands to `/glJv/{id}/show`, so the link is
  `getCarmenUrl()` and nothing new. Rendered as an `<a target="_blank">` in the dialog body,
  **not** through CustomModal's `onCancel` slot: that slot is what Escape fires, so wiring
  it here would make dismissing the dialog open a Carmen tab.

`10:05` **The teal button was broken in dark mode, and not only the new one.**
Measured, not eyeballed (WCAG ratios from the OKLCH values in `base.css`):

- `.btn-success` is `color: white` on `var(--emerald)`. Light theme's emerald is a dark
  green (L 0.62); **dark theme's is a light teal (L 0.72), so white lands at 2.37:1** —
  below the 3:1 large-text floor, let alone 4.5:1. Now ink (`oklch(0.18 0.01 188.43)`) on
  the same fill: **7.91:1**, hover **6.05:1**. Specificity alone beats the base rule and its
  hover, so no `!important`.
- **`--emerald-mid` was never defined for dark**, so every use fell through to the light
  theme's `oklch(0.88 0.05 145)` — a pale mint, wrong hue (145 vs 188) and far too bright.
  It is `.btn-success`'s border *and its hover fill*, so hovering turned the button pale
  green with white text at **1.41:1**. Also fixes the halo on `.status-badge.success` and
  `.modal-icon.success`. Now `oklch(0.4 0.06 188.43)`, matching `--teal-mid`.

Rest of the dialog measured clean in dark: labels 4.70:1, values 12.69:1, message 8.54:1,
raw-error block 7.99:1, success icon 6.49:1, error icon 4.85:1.

**Not fixed, pre-existing and app-wide:** `.btn-primary` in dark is white on `--primary`
at **3.23:1**, and `.btn-success` in *light* is white on emerald at **3.43:1**. Both fail AA
for body-size text. Fixing either means moving a shared token that every screen uses — a
design-system decision, not a bug this change introduced.

### Tests

- `NotificationBell.test.tsx` +5: the row names the document instead of leaking the raw type;
  clicking opens the dialog without touching `window.location.hash`; the dialog shows bank,
  doc no., translated reason and the raw error; a posted row links to `/glJv/982/show` and a
  failed row has no JV link. Full frontend suite 430 passed;
  `test_email_ingest_pipeline.py` 70 passed.

### Pending release note

User-visible, so `frontend/src/content/releaseNotes.ts` needs a line when the next release
is cut (batched, not per-PR): *"Email automation notifications now name the document and
open its details in place, with the reason a document could not be posted."*

---

`11:00` **Switching Email Automation off destroyed the mail that arrived while it was off.**
Started as a suspicion that the poll ignores the enable toggles — it does not. `resolve_by_tag()`
carried `WHERE enabled IS TRUE` and `match_rules()` filters `is_active` as its first line, both
ahead of `consume_document()` and the LLM call, both covered by tests. What was actually wrong
sits one step earlier: `_fetch_unseen()` marks the whole batch `\Seen` on the way in, *before*
anyone knows who a message belongs to. A switched-off BU then fell out as `unrouted` — no
`email_documents` row, no notification, and the mail permanently read. Turn the feature back on
and nothing arrives; every statement from the off-period is gone with no way to re-run it. The
out-of-credits path has handed mail back via `_unmark_seen()` since day one; being switched off
is the same kind of temporary, and was not treated that way.

### Backend

- `services/email_ingest_service.py` — `_process_message()` now answers `retry_later` (the
  existing hand-back path) for the three reversible states: `enabled = false`, no active package,
  and — via `_HOLD = (InsufficientCredits, ModuleDisabled)` — the module being switched off for
  that tenant. The tag also goes into `exhausted`, so the rest of that BU's mail in the same batch
  costs nothing. Only an unknown tag or a missing tenant row is still dropped as `unrouted`: there
  is nobody to hold mail for.
- **`ModuleDisabled` used to be filed as `unreadable_document`** — a red row and a
  `document_failed` in the customer's bell for a module *we* switched off, pointing whoever
  debugged it at a PDF that was fine.
- `services/email_settings_service.py` — `resolve_by_tag()` no longer filters on `enabled`. It
  answers identity; the caller decides permission. One `None` cannot mean both "no such tag"
  (drop it) and "known tag, paused" (keep it).
- **Two bounds, because held mail stays unseen and would otherwise pile up for ever**
  (`imap_batch_size` is 10, and SEARCH answers oldest-UID-first, so one switched-off BU whose bank
  mails daily would have filled every batch within ten days and silently starved every other
  tenant):
  - `_fetch_unseen()` takes the **newest** matching UIDs (`[-limit:]`), so a backlog can only ever
    use capacity nothing newer wants.
  - `SEARCH … SINCE` floor from the new `IMAP_HOLD_DAYS` (default 14) — the retry window. Past it
    a held message drops out of every poll and simply stays in the mailbox. `_fetch_confirmations()`
    got both for the same reason: a confirmation for a tag nobody is waiting on is never marked
    seen either, and five of those would pin the sweep to stale mail permanently.
  - Ceiling, stated in the code: a poller outage longer than `IMAP_HOLD_DAYS` ages out real mail
    too. `#/admin/email` cron health is what catches that.
- `run_ingest()` summary carries `retry_later` in its initial shape, not only when it happens.

### Frontend

- `pages/admin/EmailAutomationPage.tsx` + `i18n/dict.ts` (EN + TH) — the poll toast now reports
  `held unread`. Held mail deliberately writes no ledger row, so this counter is the only place a
  growing backlog is visible at all.

### Docs / config

- `backend/app/config.py` — `imap_hold_days: int = 14`.
- `docs/email-automation/05-operations.md` — `IMAP_HOLD_DAYS` in the env table, and a runbook row
  for "switched it back on and nothing arrives".

### Tests

- `test_email_ingest_pipeline.py` +6: a switched-off BU and a lapsed package both hold the mail
  and mark the tag exhausted; an unknown tag is still dropped; `ModuleDisabled` releases the claim
  and hands the message back; a poll takes the newest UIDs, not the oldest; the SEARCH carries
  `SINCE` at the configured window.
- `test_email_ingest_routing.py` — `test_only_enabled_bus_can_be_routed_to` became
  `test_a_switched_off_bu_still_resolves_here`, asserting the WHERE clause no longer mentions
  `enabled` (the gate moved, it did not disappear).
- `EmailAutomationPage.test.ts` +1 regression guard on the `held` count.
- Backend `tests/unit` full suite and `npx tsc --noEmit` both clean; frontend i18n + admin email
  page suites pass.

No migration and no release note: no schema changed and nothing here is visible to a customer.

---

`11:40` **A load test for the thing that actually costs money: many BUs, one mailbox, one
poll.** `scripts/email_multibu_loadtest.py` (new, no production code touched). The existing
load report measures `POST /credit-card/extract` over HTTP, which never touches routing, the
ledger, credits or posting; nothing measured the email pipeline, and nothing had ever run a
batch with several BUs' documents interleaved in it.

Two questions it answers:

- **Does a mixed batch stay attributed to the right BU?** `_process_attachment` sets
  `current_tenant_id` and `current_carmen_uri` as ContextVars per attachment. A leak means a
  credit charged to one company and a JV posted into another's books — the one failure
  unattended posting cannot undo. Messages are interleaved BU1,BU2,BU3,BU1,… on purpose, and
  every assertion reads `email_documents` / `ocr_tasks` / `credit_cards` / `llm_usage_logs`
  back out of the database and compares against the tag on the envelope, never against what
  `run_ingest` returned. The captured JV payloads are checked to carry their own BU's host.
- **Does one poll finish before the next tick?** Attachments are serial and `_poll_lock`
  skips an overlapping poll, so "many BUs at once" is one queue. The script reports per
  document p50/p95, whole-poll wall clock, and the documents that fit in the 10-minute cron.

Also reports tokens and USD from `llm_usage_logs` split by `call_type` — `extract` is the
vision call every document makes, `suggest` is the GL fill a BU only pays for until
`fill_missing_mappings` has learned its payment types.

Modes: `batch` (the matrix, `--gates` adds the duplicate and tax-ID cases), `concurrent`
(`_process_attachment` under `asyncio.gather` at C=1/2/4/8 — a shape we do not ship, run to
find the ceiling and to prove the ContextVars survive parallelism before anyone parallelises
the poll), `overlap` (a second `run_ingest` mid-flight must answer `busy` and charge nothing
twice), `cleanup`.

**Carmen is a dry run.** `post_gljv`, `post_input_tax`, `get_tax_profiles`, `get_account_codes`
and `get_departments` are patched on `email_ingest_service` — the names it imported them
under, not on `carmen_service`, which would leave this module bound to the originals. No HTTP
request for Carmen leaves the process. The LLM, `consume_document`/`refund_document`, the
ledger and every DB write are real, because those are what is being measured.

`es.foreign_tax_id` is patched to None **for the matrix only**: a real bank document prints a
real customer's TIN, which is very likely already registered to the dev BU, and every document
would park as `tax_id_mismatch` before the happy path ran once. The gate keeps its own
unpatched case under `--gates`.

Safety: refuses to start if `DATABASE_URL` names the production project; appends to a
`LoadTest` folder it creates rather than `AR Agent`, which the deployed cron is polling;
scratch tenants (`loadtest-%.local`, the pattern `load_test_v2.py --mode cleanup` already
knows) and every row they own are deleted in a `finally` — after the numbers are printed.
`--dry-check` swaps **both** LLM calls for canned answers so the harness can be proven before
any money is spent.

### Verified (harness only — `--dry-check`, 3 BUs × 2 generated PDFs, dev DB, real IMAP)

All four modes pass end to end: attribution clean, `charged_docs`/`docs_used` exact per BU,
duplicate and tax-ID gates both assert, overlap answers `busy` with no double charge, mailbox
and database left empty. Two harness bugs the rehearsal caught and that are now fixed:
purging by `HEADER Message-ID` deleted nothing (Gmail's IMAP `HEADER` search does not
reliably substring-match, so the test folder is emptied wholesale instead — it belongs to the
script), and the `--gates` documents ran the BU out of its exactly-sized allowance, so
`_release` dropped the claim and the duplicate gate reported no row at all.

**Not yet run against a real LLM** — that needs real bank documents in
`backend/example_field/`. Numbers go to `docs/LOAD_TEST_MULTI_BU.md` when it happens.

### Docs

- `docs/email-automation/05-operations.md` — the script in the test-surface table plus a
  "Multi-BU load test" runbook section.
