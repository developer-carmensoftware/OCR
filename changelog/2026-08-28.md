# 2026-08-28

## Infra — merged `main` into `prod-deploy/vercel-render-neon`

`14:30` The deploy branch was behind `main` by 45 commits and, worse, carried
`f221881` — a revert of merge `76a879d`. Reverting a merge does not just undo a bad
merge, it records "I do not want this content", so re-merging `main` produced 27
conflicted files **and silently dropped 36 files that `main` still has** because git
saw them as "deleted by us, untouched by them": the admin email-ingest router and its
integration test, the whole tutorial component tree (`components/tutorial/`,
`components/pricing/tutorial/`, `content/tutorials/purchase.ts`, `tutorial.css`),
`NotificationDetailModal`, `SwapLabel`, five migrations (`20260817` email-ingest cron,
`20260818` ×2, `20260819` ×2), five backend service tests, and the 8/17–8/19
changelogs.

Resolution: the branch's own delta since the merge base was **13,151 deletions against
2,435 insertions** and contained exactly one piece of real work, so the merge result is
`main`'s tree plus that one file —
`supabase/migrations/20260727000000_perf_log_retention.sql` (from `da81a45`, the
migration that only ever existed in prod's history). Also removed four files `main` had
deliberately deleted and the revert had resurrected: `admin/ui/Badge.tsx`,
`lib/dates.ts` (merged into `lib/date.ts`), `styles/tailwind.css`,
`supabase/schemas/40_quotas.sql`, plus `services/quota_service.py` (renamed to
`services/module_gate.py` on 8/18 — nothing imported the old name).

Checks after the merge: backend pytest green, ruff check + format clean; frontend
tsc, eslint (5 pre-existing `exhaustive-deps` warnings), prettier, vitest 510/510,
and `vite build` all green.
