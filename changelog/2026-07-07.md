# 2026-07-07

## `10:15` Structure-cleanup refactor (part 2/2) — helper consolidation, test migration, service split

Continuation of the `refactor/structure-cleanup` branch started `2026-07-06 17:56`. See that
day's entry for the audit + dead-code-removal half; this is the reorganization + test-migration
+ backend-service-split half.

### Frontend

- `09:19` Consolidated duplicate money/date helpers: `lib/format.ts` lost `toNum` (dupe alias of `parseNum`) and `fmtQty` (identical to `fmt`); `lib/money.ts` lost `formatRate` (identical to `formatThb(v, true)`) and `formatDate` (moved into `lib/date.ts`, so all date formatting lives in one place).
- `09:19` Moved `numericHelpers.ts`, `usageHelpers.ts`, `orderHelpers.ts` out of `components/` into `lib/` (components/ now holds only `.tsx`); moved `useOrderActions.ts` into `hooks/admin/` to match the documented hook-subdirectory convention.
- `09:19` Deleted the `components/{common,credit-card,pricing}` barrel `index.ts` files — each was bypassed by most of its own directory's importers already; call sites now import directly from the source file.
- `09:19` Renamed `credit-card/AccountMappingTable.tsx` → `MainMappingTable.tsx` (it maps the 3 fixed fields: commission/tax/net) to stop colliding by name with `ap-invoice/AccountMappingTable.tsx` (maps line items) — two different components, same name, in two different feature folders. Dropped its two dead props (`amountMappedCount`, `allPaymentTypes`) that were received and discarded.
- `09:40` Migrated all 9 legacy `src/test/*.test.js` files to colocated, typed `.test.ts(x)` next to their subject (`NumericInput`, `date`, `useFileUpload`, `useAPExtraction`, `useOcrExtraction`, `useAPSubmission`, `useOcrSubmission` as new files; `dict.test.js` and `useAPValidation.test.js` merged into their existing colocated counterparts, dropping 4 duplicate assertions). Heavily-mocked hook tests use a `vi.mocked()` shadow-import so `.mockResolvedValue`/`.mockRejectedValue` call sites needed no changes. `src/test/` now holds only `setup.ts`. Test count 240 → 236 (the 4 intentional dedupes); all behavior preserved and verified with `npm run type-check && npm run lint && npm run test && npm run build`.

### Backend

- `09:59` Split `routers/admin/credits.py` (878 lines, 19 endpoints, 42 inline SQLAlchemy queries) per the project's own service-layer contract (services never raise `HTTPException`; routers stay thin). New `services/credit_order_service.py` owns the credit-orders queue, AR customer profiles, Carmen AR posting, and the KPI query; `services/credit_service.py` gained `topup_order`/`adjust_balance`/`get_ledger` next to `grant_credits`/`get_credit_balance`. New typed exceptions `NotFoundError` (→404) and `ConflictError` (→409) replace ad-hoc `HTTPException(404/409)` calls; `get_order()`/`get_order_for_update()`/`ensure_decidable()` centralize patterns that were copy-pasted at 6+ call sites.
- `09:59` Behavior change (untested path, no regression): `hold_batch`/`post_ar_batch` now record a per-item tenant-scope violation as a per-item batch error instead of raising 403 and aborting the whole batch — consistent with how these two endpoints already handle "not found" and "wrong status" per item.
- `09:59` Router is now ~300 lines with zero inline `select()`/`db.execute()` calls. Full backend suite (532 tests) passes unchanged.

### Backend (security)

- `10:30` CodeQL `py/clear-text-logging-sensitive-data` on `admin/credits.py:194` — resolved as a false positive at the real source. `credit_order_service.approve()` now returns the bare literal `"subscription"` instead of `f"subscription:{order.pack_code}:{order.billing_period}"`; CodeQL was classifying those `order` attributes as "private" and tainting the returned string into the caller's `logger.info`. The two earlier fixes today (ad0fec9, 468f1bd) chased `admin.email`, but the surviving alert path never touched it — the attributes are recoverable from the already-logged `order_id`, so dropping them from the string kills the taint without losing anything.

### Docs

- `10:15` `CLAUDE.md`: updated the GL-suggestion architecture reference (`tools/map_gl.py` → `services/gl_suggestion_service.py`) to match the deleted tools/ layer.

## `14:30` Docs audit & modernization — delete MariaDB/IIS/Neon-era docs, sync everything to the Supabase/Render stack

### Docs (deleted — 7 files, git history preserved)

- `14:30` Deleted `docs/IIS_Deployment_Guide.md` (IIS/SQLite era; deploy is Render via `deploy.yml`), `docs/CORRECTION_LEARNING_SETUP.md` (MySQL/`receipts`/App.jsx-era tutorial; feature documented in Database_Design), root `CHANGELOG.md` (superseded by this `changelog/` convention), `docs/PRODUCTION_DEPLOYMENT_CHECKLIST.md` (completed one-time Neon-era migration checklist), `docs/PRODUCTION_ROADMAP.md` (phases executed differently; Thai-law retention matrix salvaged into Database_Design §10), `docs/LOAD_TEST_REPORT.md` (MariaDB-localhost V1; V2 remains), `docs/RESTORE.md` (MariaDB restore; folded into the rewritten incident playbook).

### Docs (rewritten / status-refreshed)

- `14:30` `docs/INCIDENT_PLAYBOOK.md` rewritten for the current stack: Render dashboard triage/restart/rollback, Supabase health + connection cap, OpenRouter status, and a Supabase restore section (`supabase db push` for schema, dashboard backups / `supabase db dump` for data).
- `14:30` `docs/SECURITY_PDPA_CHECKLIST.md`: every item re-verified against code and ticked with evidence (JWT auth on all endpoints, CORS hard-fail, guarded debug endpoint, security headers/HSTS, rate limiting, audit trail, soft delete, Dependabot+CodeQL, no-PAN-by-design). Remaining open items are business/legal only (DPA, server-side consent log, privacy notice, erasure policy).
- `14:30` `docs/Software_Requirements_Specification.md` → **v3.0**: Neon→Supabase throughout; extract spec updated (`bank_code`/`selected_pages`/`pdf_password`, warnings, credit-safe validation order); tasks endpoints' real response shapes; correction feedback schema (`doc_no`/`bank_code`, batch, bug-report); new sections for Auth exchange, AP Invoice module, Config/Files, Billing & Credits, Admin dashboard; §6 DB schema now defers to Database_Design.md (dropped stale `credit_card_transactions` dump); banks 3→8; file types incl. BMP/TIFF/HEIC; frontend section updated (hook subdirs, i18n pricing pages, tenant-aware storage, Tailwind-for-AR note).

### Docs (updated in place)

- `14:30` `docs/Database_Design.md`: added the whole Billing & Credits layer (`credit_packs`, `tenant_credits`, `credit_ledger`, `credit_orders` incl. on-hold flow, `billing_documents`, `tenant_subscriptions`, `ar_customer_profiles`, `document_sequences`) + `bug_reports`; migration count 8→31 with incremental summary; §10/§11 reconciled with pg_cron/pg_partman reality (9-job table; only `_perf_flush_loop` remains in Python); banks 3→8; §14 pruned (admin auth, `require_permission()`, bootstrap CLI now built).
- `14:30` `README.md`: Supabase + Render/Vercel stack table, 8 banks, real file-type list, full env block (ADMIN_JWT_SECRET / INTERNAL_JOB_TOKEN / AP model), deploy section → `deploy.yml` flow (was: nonexistent `deploy.ps1` IIS script).
- `14:30` `docs/Billing_Purchase_Flow.md`: Standard→Growth rename, on-hold order state documented.
- `14:30` `docs/Marketing_Brief.md`: bank coverage → 8 (BAY + KTC/GHL/PayPal/SiamPay fee invoices).
- `14:30` `CONTRIBUTING.md`: deploy section → Render/`deploy.yml`; dead backup-scripts ref removed; PR checklist now includes the CI-enforced changelog entry; mypy strict list synced with `pyproject.toml`.
- `14:30` `CLAUDE.md`: Billing row added to the DB layer table; `bug_reports` added; supported-files list corrected (BMP/TIFF/HEIC via pillow-heif); fixed AP suggest endpoint name (`/api/v1/ap-invoice/suggest`, not `suggest-gl`).
- `14:30` `render.yaml`: stale "Neon" comments → Supabase.

### Status

- `14:45` Refactor half merged via PR #44. Docs pass ships separately on `docs/stack-modernization` (`main` ← branch); backend suite + frontend type-check/236 tests/build all green before commit.
