# 2026-09-25

_Several of this day's PRs (#252–#254, then #255, #256 and `fix/mapping-legacy-fallback`) each
carry this file byte-identical, so they merge in any order without conflicting on it. Each
entry names the PR or branch it ships with; an entry can land slightly before its own PR._

`09:37` **Email review: one document, one JV — approve takes a real claim, and stamps only this
BU's own card.** *(shipped as PR #251 — `10b91cd`)*

Fixes DEF-1 and DEF-2 from the multi-BU QA run
([`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md)).

- **DEF-1 (HIGH) — two approvals of one document both posted a JV.** `_claim_for_review` took
  `SELECT … FOR UPDATE` in a session that closed before `post_gljv` was awaited, so the lock was
  gone when it mattered; two reviewers in one BU (the bell notifies everyone) could both post.
  Now a compare-and-set on a new column, `email_documents.posting_started_at`
  (`20260925000000_email_documents_posting_claim.sql`): `UPDATE … WHERE status='pending_review'
  AND (unclaimed OR claim older than POSTING_CLAIM_TTL) RETURNING`. The second click gets 409
  "Another reviewer is posting this document right now". `approve_document` gives the claim back
  on every exit before Carmen accepts (no credential, duplicate, Carmen unreachable, Carmen
  rejected) and keeps it after; `reject_document` takes the same claim, so a reject racing an
  approve is refused rather than marking rejected a JV that is going into Carmen; `_finish`
  clears it. A timestamp rather than a status on purpose — no tab or count changes, and a claim
  left by a dead process expires (5 min, ≥ two Carmen posts at the 30 s timeout) instead of
  stranding the document. Holding the row lock across the Carmen call (the wizard's approach) was
  rejected: it pins a pooled connection for up to a minute against a pool of 10.
- **DEF-2 (MEDIUM) — approve stamped another BU's card.** `_mark_submitted` took the
  client-supplied `extracted.id` and loaded it with no tenant filter. Now
  `_mark_submitted(tenant_id, task_id)` finds the card by the ledger row's own `task_id`
  (`uq_credit_cards_task`) with the tenant in the WHERE clause — the wizard's `proxy_gljv` pattern.
  Both call sites (auto-post in `_run_document`, and approve) use it; `extracted.id` is no longer
  read anywhere on this path.
- **Tests:** `tests/tenancy/test_email_approve_integrity.py` — the two `xfail(strict=True)`
  reproductions now pass on the real dev DB, plus five new ones (reject racing approve, Carmen
  rejection releases the claim, expired claim retaken, live claim refuses a second approve, own
  card still stamped). `tests/unit/test_email_ingest_pipeline.py` — `_ReviewDB` fake now plays the
  compare-and-set; four new unit tests; `_mark_submitted` assertion pinned to (tenant, task).
- **DB:** migration applied to the dev project by direct SQL (additive, `if not exists`) because
  `supabase db push` from this branch is blocked by five migrations other unmerged branches applied
  to dev (`LegacyDbPushMissingLocalError`); the normal `db push` after merge re-runs it as a no-op.
  **Not yet applied to prod.**

Stacked on `test/email-automation-multi-bu` (it needs that branch's test file). Release note
pending — batch with the next release (fixes a user-visible duplicate-post risk).

---

`09:55` **Email routing: the `+tag` must be on our exact address — lookalike domains no longer
route.** *(PR #252 — `456b94d`)*

Fixes F-3 from the multi-BU QA run
([`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md)).

- **F-3 (HIGH):** `email_imap.tag_from_recipients` built its regex as `user\+(tag)@domain` with no
  edges, so it was a substring search. `aragent+<tag>@carmensoftware.com.evil.test` was routed
  live, during the QA run, to the BU owning `<tag>`; `xaragent+<tag>@…` would have matched too.
  Now bounded on both sides: no local-part character before `user`, and no label continuing the
  domain after it (`-x`, `.x`) — while `>`, `;`, a comma, a space, or a sentence-ending dot still
  end the address, because candidates can be free text like `for <…>; date`. One function, so the
  document poll, the confirmation sweep and the confirmation branch of `_process_message` are all
  fixed at once.
- **Tests:** `tests/unit/test_email_ingest_routing.py` +11 — six lookalikes (domain suffix, bare and
  inside a `for <…>` line, hyphenated suffix, three prefixed local parts) must yield no tag; five
  real header punctuation shapes must still yield it.

No release note: a routing hardening with no visible behaviour for correctly addressed mail.

---

`10:12` **Email ingest: the mailbox queue is our own `$OcrDone` flag, not `\Seen` — opening
the mailbox no longer loses mail.** *(PR #253 — `b6a5888`)*

Fixes F-1 and F-4 from the multi-BU QA run
([`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md)).

- **F-1 (HIGH):** the poll's queue was `SEARCH UNSEEN`, and `\Seen` is something anyone can
  set. During the QA run 39 appended mails were marked read by a reader outside the poller and
  never produced a row. In production, support opening `AIAGENT@` to look into a complaint would
  have erased exactly the document in question. `email_imap.fetch_unseen` → `fetch_pending`
  (`UID SEARCH NOT KEYWORD $OcrDone …`, all three searches incl. the `SMALLER` fallback and the
  beyond-window count); `mark_seen` → `mark_done` stores `(\Seen $OcrDone)` — `\Seen` kept only
  so the label reads as handled to a person. Deleting a mail or removing its label still loses it.
- **F-4 (LOW):** `fetch_confirmations` fetched `(RFC822)`, which on Gmail sets `\Seen`, so a
  confirmation for a tag nobody was waiting on was consumed instead of left for the document poll
  (its docstring promised the opposite). Now `BODY.PEEK[]`, same `$OcrDone` queue.
- An odd `FETCH` response shape was skipped with no trace; it now logs a warning naming the UID
  (the mail stays pending, as before).
- **Transition:** new `scripts/imap_mark_done_backfill.py` (dry run by default) flags mail older
  than the hold window done, so the first poll does not raise `email_ingest_beyond_window` for
  the mailbox's whole history. In-window mail is left pending on purpose: already-decided mail
  dedupes on `email_documents` for free, and whatever F-1 hid is found and processed. Dev dry run:
  209 → done, 86 in-window left for the poll. **Run it against prod before this deploys** —
  see [`05-operations.md`](../docs/email-automation/05-operations.md).
- `scripts/reset_email_test.py --unread`, `reset_tenant_email.py`, `email_ingest_e2e.py` now
  clear/check `$OcrDone`; clearing only `\Seen` would replay nothing.
- **Tests:** unit — the queue never mentions `UNSEEN`, `mark_done` sets both flags, the
  confirmation sweep peeks, the odd-FETCH skip is logged.

No release note: no customer-visible behaviour change.

---

`10:17` **Email ingest: a document's own verdict outranks a dead Carmen credential; settings
saves keep rule fields they don't know.** *(PR #254 — `fb02bbc`)*

Fixes F-6 and F-5 from the multi-BU QA run
([`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md)).

- **F-6 (MEDIUM):** the GL suggester runs before the tax-ID and "already posted" checks on
  purpose (parked documents still get suggestions), but it re-raises a 401/403, so on a BU whose
  token had died a foreign-TIN document parked as `carmen_unauthorized`. The reviewer was sent to
  fix a credential and never told the document was not theirs. `_run_document` now reads the tax
  ID with the accounting config, decides the verdict (`tax_id_mismatch`, else
  `duplicate_document` "Already posted to Carmen") before the suggester, and raises it after. A
  401/403 from the suggester still flags the token (new `_flag_dead_token`, shared with the
  handler) but no longer replaces the verdict. With no verdict nothing changes. Module docstring
  and `02-architecture.md` Diagram 6 follow the real order (the diagram also stopped claiming
  suggestions are saved at ingest; they are saved when a human approves).
- **F-5 (LOW-MED):** `email_settings_service._merge_rule` rebuilt each rule from the five fields
  it knows, so a key written by a newer version (`doc_type`, on the unmerged
  `feat/detailed-cc-ar-reconciliation`) was deleted by the next save from an older one. A rollback
  after AR merges would have turned settlement rules back into fee-invoice rules. It now starts
  from the stored rule. The AR branch edits the same function; the merge is trivial.
- **Tests:** unit +3. A foreign TIN or an already-posted document with a suggester 401 keeps its
  reason and still flags the token; an unknown rule key survives a save. All three fail on `main`.

No release note: no change for a BU whose credential is alive.

---

`11:01` **QA: live re-test of the email-automation fixes — 5/5 pass.**

Re-ran F-1, F-3, F-6, DEF-1 and DEF-2 against real Gmail, a real LLM, the real dev DB and the
real router, on `main` after #251–#254 merged. Evidence is in the new "Re-test after fixes"
section of
[`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md).

- **F-1:** a fixture appended already `\Seen` was processed and flagged `$OcrDone`.
- **F-3:** both lookalike addresses (domain suffix, local-part prefix) came back `unrouted` with
  0 rows.
- **F-6:** a foreign TIN to carmencloud (dead token) parked as `tax_id_mismatch`, and the token
  was still flagged.
- **DEF-1:** two concurrent approves gave `[200, 409]` with one `post_gljv`.
- **DEF-2:** a cross-BU `extracted.id` left the other BU's card unstamped.
- The dev mailbox was switched to the `$OcrDone` queue with
  `imap_mark_done_backfill.py --apply --seen-as-done` (295 flagged, re-run reports 0). Cost
  $0.0015. Teardown was re-verified by fresh SQL and IMAP queries.
- `scripts/email_multibu_qa.py`:
  - new phases `fixcheck-setup` / `fixcheck` and a `fixcheck` wave
  - `MsgSpec.seen` (APPEND as already read) and `dispatch_patches(dry_delay=)`
  - scratch-BU specs lifted to `SCRATCH_SPECS` / `_create_scratch()`
  - teardown now restores what it changed on a real BU: `verified_at`, the subscription
    documents the run consumed, and exactly the cron jobs it paused
  - it reverts `auto_post` only when setup toggled it

No release note: test tooling and a report.

---

`11:47` **QA round 2: E-06, Q-09, S-07 (no real credential) and F-4 pass; real JV waits on a
carmencloud token; two new observations.**

Run `0925044057`, evidence in the "Round 2" section of
[`docs/email-automation/qa/2026-09-24-multi-bu-report.md`](../docs/email-automation/qa/2026-09-24-multi-bu-report.md).

- **Passed:**
  - **E-06:** an out-of-credits BU's mail is held (no row, no charge, not `$OcrDone`) and
    replays after a top-up.
  - **Q-09:** with 50 pending, mail is held uncharged and replays below the cap.
  - **S-07:** 6/6 through the real app (401 / 401 / Carmen 401 / cached 401 / 400 / 429 on the
    21st call).
  - **F-4:** code plus 10 unit tests.
- **Not run yet:**
  - **Real JV into Carmen:** needs carmencloud's token refreshed. carmen already posted the
    supplied KBank receipt.
  - **S-07 cases with a real BU token or minted admin JWT:** held back, since they need a stored
    credential handled.
- **F-7 (MEDIUM, observation):** carmen posted that receipt under doc_no
  `041125E00023869767` (the printed number with an extra "767"). Both duplicate checks compare
  doc_no exactly, so one misread digit is enough for the same document to post twice. Not fixed.
- **S-07 concern (unverified):** `validate_token` proves a token against the host, not the BU,
  and `_resolve` and `/auth/exchange` trust the requested `bu`. Whether that is a cross-BU hole
  depends on Carmen's token scoping. Not fixed.
- Harness:
  - phases `round2-setup`, `r2-token` (credential metadata only), `r2check`, `approve-real`
    and `s07`
  - `toggle s6-exhaust|restore|backlog-fill|backlog-drain`
  - `_pause_cron()` shared with `fixcheck-setup`
  - teardown keeps a document that really posted to Carmen, so the duplicate guard still
    remembers it
- `email_ingest_service.py`: two comments still named `\Seen` as the queue marker (drift from
  #253), now `$OcrDone`. Comments only.

No release note: test tooling, a report and comments.

---

`12:08` **QA: S-07 with real credentials — the host, not the BU, is the boundary, as designed.**

With the owner's permission (dev only), carmen's stored credential (decrypted in memory, never
printed) read carmencloud's settings and got a carmencloud session through `/auth/exchange`.
Admin-JWT scope held (403 outside the scoped tenant). This matches
`docs/CARMEN_INTEGRATION.md`: one host is one corporate group, and a token valid on the host
manages every BU under it. Carmen has no API that names a token's BU. The owner decided to keep
the design:

- Pinned by `test_a_token_valid_for_the_host_may_act_on_any_bu_under_it`
  (`tests/integration/test_email_automation_router.py`) and by the `s07-creds` harness phase,
  8/8.
- The report asks the Carmen team whether a user can be restricted to some BUs of a group, and
  whether Carmen can expose an endpoint for a token's BUs.

No release note: a test, a harness phase and a report.

---

`12:54` **Email ingest: a near-duplicate of a posted document parks instead of posting (F-7).**
*(branch `fix/email-possible-duplicate`)*

Both duplicate checks (`has_submitted_doc`, `_already_pending`) compare doc_no exactly. A
KBank receipt printed `041125E00023869` had been posted by carmen as `041125E00023869767`,
so a correct reading of the same receipt would have posted a second JV.

- New `_possibly_posted()` in `email_ingest_service.py`. It looks for a document this BU
  already submitted **on the same date** whose number contains this one or is contained in it
  (shorter side at least 8 characters). A match parks the document as `duplicate_document`
  with the message "Possibly already posted to Carmen as `<number>` — same date, overlapping
  number".
  - The document is not refused, because nothing proves the two are the same statement, but
    it never auto-posts.
  - A one-digit substitution is deliberately not matched: sequential numbers on the same day
    are the ordinary case.
  - The check is part of the verdict, so a dead token cannot hide it (see F-6).
- No amount in the key: `credit_cards` stores none, and rows posted before a new column would
  not have one either.
- Tests:
  - unit: parks and does not post; the matching table covers the F-7 misread both ways,
    sequential numbers, exact equality and too-short numbers
  - real DB (`tests/tenancy/test_email_possible_duplicate.py`): the lookup never sees another
    BU's documents or another date
- Docs: `04-data-model.md` reason_code taxonomy.

No release note until the next release batch: a reviewer may now see "Possibly already posted"
on a queue row.

---

`13:14` **QA: a real JV reached Carmen through the email pipeline (JV 1103); F-8 found.**
*(PR #255)*

- **R-JV:**
  - The owner's KBank receipt was mailed to carmencloud after its token was refreshed. It
    was extracted exactly (gross 428,513.98 · fee 14,140.97 · VAT 989.87 · net 413,383.14),
    parked, and approved through the real router.
  - Carmen `POST /gljv` → **JV 1103**, plus the input-tax record. It posted under
    carmencloud's tenant only.
  - Teardown kept the posted row, its card and its charge, so the duplicate guard remembers
    it.
  - The unmapped payment type used carmencloud's own existing account (`GEN/1021009`) via
    the harness's new `approve-real --map`.
- **F-8 (MEDIUM, open):**
  - #248's backfill set `bu_accounting_mapping_entries.bank_code` only where the config row
    had a bank. carmencloud's config row had none, so its 29 pre-#248 entries stay NULL, and
    per-bank reads (`bank_code = 'KBANK'`) see none of them.
  - Every such BU's documents park `mapping_missing`, and an `auto_post` BU stops
    auto-posting.
  - Not fixed; a decision is recorded in the report.

No release note: a report and a harness flag.

---

`13:24` **GL mappings saved before bank scoping answer per-bank reads again (F-8).**
*(branch `fix/mapping-legacy-fallback`)*

`20260924000000_bank_scoped_mapping_entries.sql` (#248) backfilled an entry's `bank_code`
only where its config row named a bank. A BU whose row did not (carmencloud: all 29 entries)
kept bank-less entries that `bank_code = '<bank>'` never matches. Email ingest and the
per-bank wizard therefore saw none of that BU's mappings: every document parked
`mapping_missing` with AI-guessed accounts, and an `auto_post` BU stopped posting. Found
during QA round 2 (#255).

- `accounting_config_service.get_accounting_config`: a read scoped to a bank now falls back
  to the BU's bank-less entry for any field that bank has no entry of its own for.
  - The bank's own entry always wins.
  - The next save of that bank writes the fields as its own (`save_accounting_config` only
    ever touches the bank being saved).
  - Unscoped reads are unchanged.
- No migration: there is no way to know which bank a tenant-wide entry "belonged" to.
- Tests:
  - unit: the bank wins, a bank-less entry fills the gap, and an unscoped read is still one
    query
  - real DB (`tests/tenancy/test_mapping_legacy_fallback.py`): KBANK / SCB / unscoped reads
    for a bank-less BU, and another BU's entries never leak in
  - Both fail on `main`.

Release note pending with the next batch: BUs whose mappings predate bank scoping get them
back in the review queue.

---

`15:16` **Refactor Phase 0: safety net + baseline for the god-file split.**
*(branch `refactor/phase0-safety-net`, plan: `codebase-refactor-recursive-scroll.md`)*

No production code touched — this phase is entirely test infrastructure and a recorded
baseline, ahead of splitting `email_ingest_service.py` (2,209 lines), `dict.ts` (3,188
lines) and the other oversized files the plan targets.

- **Backend real-DB guard** (`tests/conftest.py`): outside `tests/tenancy/`, `DATABASE_URL`
  is now overridden to an unreachable host before any `app.*` import, and a
  `do_connect`-event listener + autouse fixture fails any test that still reaches a real
  connection attempt — even one an app-level `except Exception` would otherwise swallow
  silently. Running it for the first time found 16 tests (`test_ocr_router.py`,
  `test_ap_invoice_router.py`, one in `test_email_ingest_pipeline.py`) that were making a
  real, unmocked call to `module_gate.assert_module_enabled` or
  `pricing_cache_service.get_pricing` against the real dev DB on every run, silently
  succeeding only because the dev DB happened to be reachable and both functions fail
  open. Fixed at the source (missing mocks added), not marked as exceptions — the new
  `db_attempt_ok` marker exists but nothing uses it yet.
- **Backend layout guard** (new `tests/unit/test_layout.py`, AST-based): services may not
  import routers (live today); `services/shared/*` may not import a domain package, and
  `services/email_automation/*` may not bind `post_gljv`/`post_input_tax`/
  `extract_stateless` by name instead of through the module (both inert until Phase 1–2
  create those packages, verified via a deliberate throwaway violation in each).
- **Frontend mock-path guard** (new `src/test/mockPaths.test.ts`): scans every
  `vi.mock('relative/path')` across the suite and fails if the path does not resolve to a
  real file — vitest otherwise falls through to the real module silently. Verified against
  a deliberately broken path.
- **`useAPInvoice` characterization tests** (new `useAPInvoice.test.ts`, 22 tests) — the one
  hook in `hooks/ap-invoice/` with no coverage at all. Exercises the orchestration this
  file owns directly (adjustField's three branches, applyDocRepair/fixDocFigures,
  blurHeader's tax propagation, blurLineItem's recalc-trigger gate, applyLineTax's
  interlock, group/ungroup, debounced draft save/restore) through the real sub-hooks, only
  mocking true network/browser boundaries. Along the way: a non-memoized `useT` mock (fresh
  `t` reference every render, unlike the real `LanguageContext`) fed into
  `useAPVendor.autoMatchVendor`'s `useCallback` and produced a genuine infinite render
  loop, OOM-crashing the vitest worker — fixed by making the mock's `t` stable, matching
  the real contract.
- **Baseline recorded** (see PR description): backend 1,320/1,320 tests, 80% coverage,
  ruff/format/bandit clean, mypy 77 errors (unchanged); tenancy suite 88/88 on the real dev
  DB; frontend 63 files / 802 tests, lint/type-check/format clean; `openapi_before.json`
  (118 paths) saved for a diff check after Phase 1+; production build baseline (96 chunks,
  main chunk 213.58 kB / 52.81 kB gzip) for the Phase 5 i18n-split comparison.
- **`scripts/email_ingest_e2e.py` baseline**, run against the real mailbox and dev DB: free
  suite 19/19 + the `$0` no-charge self-check, `--paid` 3/4 (the one failure was a stale
  `pending_review` row left over from an earlier, unrelated run of this same script —
  duplicate detection correctly caught it; nothing posted to Carmen). The dev tenant
  (`carmen` / dev.carmen4.com) had no active subscription, which made every routed message
  in the free run fall into `retry_later` on the first attempt — a `sub_growth`
  subscription was granted via the real `activate_subscription()` service function to
  establish the baseline, then reverted (`status=superseded`) immediately after, with the
  owner's approval both times.

No release note: test infrastructure and a baseline snapshot, no application behaviour
changed.

---

`16:02` **Refactor Phase 1: backend services/routers moved into domain packages.**
*(branch `refactor/phase1-domain-packages`, stacked on Phase 0, plan:
`codebase-refactor-recursive-scroll.md`)*

`backend/app/services/*.py` (39 files, flat) and 5 of `backend/app/routers/*.py` moved into
`{email_automation,credit_card,ap_invoice,billing,admin,shared}/` sub-packages — mechanical
only, verified against a byte-identical OpenAPI diff (118 paths, before/after) and the full
suite passing at the Phase 0 baseline counts.

- **Commit 1 — pure `git mv`.** Domain placement was checked against actual importers
  (grep), not assumed from a filename: `credit_service.py` and `anomaly_service.py` both
  turned out to be used across 3+ domains (`consume_document`/`refund_document` from every
  extraction router; anomaly alerts from `llm/client.py`, email ingest, and the admin
  dashboard), so both went to `shared/` instead of the plan's draft placement in
  `billing/`/`admin/`. `routers/config.py` stayed flat (it serves both credit-card
  accounting config and AP vendor mapping — genuinely two domains, not one). Deleted
  `services/usage_service.py`, a re-export shim whose own docstring said "Phase 10 will
  remove this file"; its ~13 importers now import the real module directly.
- **Commit 2 — codemod repointing every `app.services`/`app.routers` reference** (imports,
  `patch("...")` strings, `patch.object(module, ...)` targets) to the new dotted paths,
  aliased back to each file's existing bare name (`from app.services import
  maintenance_service` → `from app.services.shared import maintenance as
  maintenance_service`) so no call site elsewhere in that file needed touching.
  - The codemod itself needed three fix-and-redo cycles before it was safe to commit —
    each caught by `python -c "import app.main"` or the test suite, never by a merge:
    (1) it initially mishandled `from app.services import X` (the dominant style in this
    codebase) as if `X` were a name defined *inside* the renamed module, rather than the
    module itself; (2) a `# noqa: E402` comment after an import line broke its end-of-line
    anchor; (3) `"email_automation"` is both the old `email_automation.py` router's name
    and the new package `email_automation.py`/`email_review.py` both moved under, so a
    correct substitution's own output (`app.routers.email_automation.review`) still
    contained a re-matchable `app.routers.email_automation` — a second run over an
    already-correct file corrupted it further. Fixed by reordering the two substitution
    passes so nothing runs a second time over text the first pass just wrote, and
    documented as "run exactly once" in the script's own docstring.
  - One real (behavior-preserving) fix beyond path-rewriting: `email_automation/ingest.py`
    imported `post_gljv`/`post_input_tax` **by name** from `shared/carmen.py` — exactly
    what Phase 0's new `test_layout.py` guard exists to catch, since a dry-run script
    patches `ingest.<name>` and a future caller that moves to a different module (Phase 2)
    would silently stop being covered. Fixed now: import the `carmen` module and call
    `carmen.post_gljv(...)`/`carmen.post_input_tax(...)`, with every
    `patch.object(ingest, "post_gljv"/"post_input_tax", ...)` site (2 test files, the
    real-DB tenancy suite, both dry-run QA scripts) repointed to `patch.object(ingest.carmen,
    ...)`.
- **`pyproject.toml`** mypy strict-module list updated for the 3 moved modules; the deleted
  `usage_service` entry dropped.
- **`CLAUDE.md` and the live architecture docs** (`docs/Database_Design.md`,
  `docs/SQL_PERFORMANCE_AUDIT.md`, `docs/Software_Requirements_Specification.md`,
  `docs/email-automation/{README,01-requirements,02-architecture,03-api-reference,
  04-data-model,05-operations,07-human-in-the-loop}.md`) — file-path references updated.
  Deliberately **not** touched: `docs/email-automation/06-decision-log.md` and the `qa/*.md`
  reports, which are historical records of what a file looked like on the date they were
  written, same reasoning as never editing an old changelog entry. Several `:NNN` line-number
  citations in the docs that *were* touched are now stale by more than my ~10-line import-block
  edit could explain (e.g. one pointed at line 1043 for a function actually at 1498) — already
  stale before this PR, not something this pass attempted to fix.

Baseline after both commits: backend 1,386/1,386 tests (+66 over Phase 0's count, from
`test_layout.py`'s parametrize now walking real files), 80% coverage, unchanged; tenancy
suite 88/88 on the real dev DB; ruff/format/bandit clean; mypy 77 errors, unchanged;
`openapi.json` diff against the Phase 0 baseline is empty.

No release note: pure internal reorganisation, no application behaviour changed.
