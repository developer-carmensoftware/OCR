# 2026-09-23

`10:35` **Email ingest — `owner_emails` matches whole addresses, not substrings.**
Branch `fix/ca102-m009-sender-exact-match`. CA-102 TC-M-009, ticket T-04a.

The QA report on M-009 said a message from an unregistered sender reached the review
queue. Checking it against the code showed that `sender_allowed()` exists and runs before
any charge. So the report was not about a missing check. It turned up a real precision
bug instead:

- **Bug:** `email_imap.sender_allowed()` lowercased the raw From/To/Cc text and tested
  each registered address with `in`. A registered `a@b.com` also admitted
  `xa@b.com`, `a@b.com.evil.io`, and a display name the sender typed as
  `"a@b.com" <x@evil.io>`.
- **Fix:** the headers are parsed by the existing `people_addresses()` (`getaddresses`)
  and compared as a set against `owner_emails` (stripped, lowercased). Empty
  `owner_emails` still accepts every sender. A header with no readable address is refused
  once a list is set. `28da9b0`
- **Test:** `test_sender_allowed` fixtures now use the comma-joined shape `fetch_unseen`
  actually builds. The old `"From: … To: …"` strings are not a real shape and parse to
  nothing. Added four cases, three of which failed before the fix. `fa0b4bb`
- Email suites: 321 passed.

**Unchanged on purpose:** the gate still reads From/To/Cc, so an auto-forward still passes
because the BU's own mailbox is in `To:`. This gate stops misdirected mail. It does not
stop someone attacking on purpose; the envelope `+tag` does that (decision-log #16). QA
should retest M-009 by sending *directly* from an unregistered address, not through a
forward filter.

Also: `CONTEXT.md` gains **Skipped**, **Failed** and **Owner emails**, the terms CA-102
used differently from this codebase.

No release note. A few rows move from posted to `sender_not_allowed` for a BU whose list
only ever matched as a substring, which is the gate working as documented.

---

`10:50` **Email ingest: a second copy of a queued document no longer rings the bell.**
Branch `fix/ca102-m027-duplicate-pending-no-bell`. CA-102 TC-M-027.

- **Bug:** `_run_document`'s `_park_or_finish` sends the `_already_pending` duplicate
  (`_Skip(reviewable=False)`) through `_finish` with notify on. Every re-sent copy
  therefore raised a `document_failed` notification, even though the copy the customer
  can act on is already waiting in the queue, which rang its own bell.
- **Fix:** that path passes `notify=reviewable`, so the duplicate finishes silently. It is
  the only `reviewable=False` raise site. `e57d39b`
- **Test:** `test_a_second_copy_of_something_already_in_the_queue_does_not_park` now also
  asserts that no `UserNotification` is created. It fails before the fix. `c75c40e`
- Email suites + `test_credit_card_activity_api`: 352 passed.
- `docs/email-automation/04-data-model.md`: the taxonomy row says the duplicate raises no
  notification, and drops the stale "review mode only".

**Deliberately unchanged:** the status stays **`failed`** even though CA-102 asked for
`skipped`. The duplicate is only detectable after extraction (it keys on `doc_no`), so it
has always been charged, and `skipped` means "stopped before the charge". Decided with the
owner. The QA Coverage Matrix gets a spec correction; the code keeps its meaning. Credit
logic and the duplicate-after-posted branch (CA-102 W-1) are also unchanged.

No release note (no visible change beyond one fewer notification).

---

`14:50` **Email ingest — the bell is kept current, not appended to.**
`/grilling` session with the user first (requirements below), design:
[`docs/email-automation/07-human-in-the-loop.md#§22`](../docs/email-automation/07-human-in-the-loop.md).

The user reported the email-automation bell as "too many notifications". Grilling settled
the bell's role — an **action inbox**, not an activity feed — before touching code; §22
records the full decision table (#102–#106).

- **Backend:**
  - `notification_service.notify_collapsed()`: folds into the tenant's newest *unread* row
    of a `(type_, key)` instead of adding a new one — replaces the payload, bumps
    `created_at`. Read it, and the next call starts a fresh row. `build_payload(prev)` lets
    a caller recompute a live gauge or increment a running count.
  - `_notify_pending`: `document_blocked` (the queue's blocked count) no longer gets its own
    row — it merges into `document_pending_review` as `{"pending", "blocked"}`. Both figures
    are recounted from the queue every poll, not carried from what this poll alone parked.
  - `_finish`: `document_blocked`/`document_failed` collapse per `reason_code` with a
    running `count`, not per document (`{"reason_code", "count", "attachment", "message"}`,
    no `document_id`). `document_posted` is unchanged — still one receipt per document.
  - `NOTIFIABLE_SKIPS` drops `sender_not_allowed`. The ledger row is unaffected; only the
    bell stops ringing for mail a stranger sent to the BU's `+tag` address, which the
    customer cannot act on and cannot stop a stranger from repeating.
- **Frontend:**
  - `NotificationBell.tsx`: `notifText`/`handleItem` read the new collapsed shape
    (`isCollapsed`), render `"{count} files: {reason}"`, and open the queue's `unposted`
    chip instead of a per-document dialog. Old-shape rows already in a customer's unread
    list still open `NotificationDetailModal` exactly as before — no backfill.
  - `useReviewQueue(limit, initialFilter?)`: an optional chip that skips the
    today→review→success fall-through, so a bell link lands where it says it will.
    `ReviewQueue.tsx`'s `filterFromHash()` reads `?filter=` the same way `docIdFromHash()`
    already read `?id=`.
  - `i18n/dict.ts`: `notif.docPendingReviewBlocked`, `notif.docsBlockedCount`,
    `notif.reasonShort.*` (EN + TH).
- **Tests:** `test_notifications.py` +4 (`notify_collapsed` contract, incl. two different
  unread reasons not folding into each other), `test_email_ingest_pipeline.py` updated for
  the merged/collapsed shapes (164 backend tests passed). `NotificationBell.test.tsx` +3,
  `ReviewQueue.test.tsx` +1 (713 frontend tests passed). `tsc --noEmit` clean.

**`15:10` Browser-tested with Playwright MCP, found and fixed two follow-on bugs.**
`npm run build` clean, then real sessions seeded for two dev BUs
(`scripts/seed_browser_session.py --bu <name> --host dev.carmen4.com`, `--bu demo` and
`--bu carmencloud`) and driven through Chrome.

- **`demo` (no `email_ingest_settings` row at all):** bell shows only the release note, zero
  console errors — confirms the change is inert for a BU that never touched the feature.
- **`carmencloud` (heavy real usage, 187 existing notifications — the exact noise this whole
  change targets):** surfaced two bugs the unit tests couldn't catch, both real backend data:
  1. **Deep link did nothing when already on the queue page.** `main.tsx` keys pages by
     path and strips the query string, so `#/CreditCardOCR?filter=unposted` didn't remount
     `ReviewQueue` when the user was already sitting on `#/CreditCardOCR` — the mount-time
     `filterFromHash()` never re-ran. Fixed the same way `ReviewQueue.tsx` already handles
     `?id=` for opening a document: a `hashchange` listener that calls `setFilter()` live.
     2 new regression tests (already-mounted page; a hashchange with no `filter`, e.g.
     opening a document, correctly leaves the chip alone).
  2. **A bell row with nothing in it** — screenshot from the user: "Could not read this
     document / Document —, Bank —, Doc no. —". Traced (read-only queries against the dev
     DB, port 6543 like `seed_browser_session.py`) to a *third* `document_blocked` payload
     shape that existed before today and that the collapsing work hadn't accounted for: a
     bare `{"blocked": N}` queue-level summary from the pre-fix `_notify_pending`, which was
     never about a single document — 53 rows in dev, 7 still unread across 2 real tenants.
     `NotificationBell.tsx` opened the per-document dialog for it anyway because it isn't
     the *new* collapsed shape (no `count`) and isn't old per-document data either (no
     `document_id`). Added `isOrphanBlockedCount()` alongside `isCollapsed()`, one new
     i18n key (`notif.docsBlockedOrphan`, EN+TH), routes to the same `unposted` chip as a
     collapsed row. Purely a frontend read of data already in the DB — no migration, and it
     fixes the 7 unread rows the moment the build ships. 2 new tests. Re-verified live
     against `carmencloud` after the fix: the row now reads "7 documents need attention" and
     opens the queue instead of the dialog.
- Frontend suite now 717 passing (was 713). `tsc --noEmit` clean throughout.

**User-visible (for the eventual release note):** fewer, more useful email-automation
notifications — a batch of documents failing the same way (e.g. wrong PDF password) now
raises one bell row with a count instead of one per file, the "waiting for review" row stays
current instead of piling up across polls, and a stranger emailing the BU's ingest address
no longer rings the bell at all. Draft bullets, EN: *"Email notifications: fewer, clearer —
repeated problems now show as one row with a count, and the review-queue notification always
shows the current total."* TH: *"การแจ้งเตือนอีเมล: น้อยลงและชัดเจนขึ้น — ปัญหาที่เกิดซ้ำแสดงเป็นแถวเดียวพร้อมจำนวน
และการแจ้งเตือนคิวตรวจสอบแสดงยอดรวมปัจจุบันเสมอ"*. VERSION untouched — batch into the next release
per [[release-note-batching]].

**Uncommitted / in-progress:** everything above is unstaged on `main` as of this entry —
not yet branched, committed, or opened as a PR.

`16:44` **Credit Card landing page: activity log, not an email pitch.** Grilling session
→ decision-log §23 (#107–#110). Branch `feat/cc-activity-log`.

- **Frontend:**
  - Deleted `NotSetUp`, the full-page forwarding pitch.
  - A BU with no rows now gets `NoScansYet`, a flat "No scans yet" card with no button of its own. The top bar's Upload documents is the one entry point.
  - Chips hide on `counts.all === 0` alone.
  - `useReviewQueue` no longer calls `getReviewStatus`; the function and `API.emailReview.status` were removed.
  - Dead `review.intro*`/`step*`/`copyAddress`/`openSettings` strings and `.rq-address`/`.rq-steps` CSS removed.
  - `QueueEmpty` lost `hasHistory`, which is always true now.
- **Backend (`credit_card_activity.py`):** charged-but-never-posted manual scans are listed.
  - They appear as `status: "scanned"` under `unposted`/`today`/`all`, timestamped by `created_at`.
  - `MANUAL_FILTERS` → `MANUAL_CHIPS`.
  - `_manual_counts_stmt` replaces two `count_rows` calls with one aggregate.
  - No attention dot for these rows.
- **Row (`QueueRow.tsx`):** a `scanned` row shows a red "Not posted" pill and "Scanned by {name} — not posted", with no action.
- **Tests:** backend activity suite 38 passed (3 new). Frontend ReviewQueue + credit-card suites 160 passed. `npm run build` clean.
- **Browser-verified (Playwright, dev BU `carmen`):** a manual KTC scan abandoned at step 2 appeared at the top of Today and Not posted with a red pill, "Scanned by browser-test — not posted" and no action.
  - Not posted went from 32 to 85: 52 older abandoned manual scans on that BU surfaced for the first time, plus this one.
  - The attention count stayed at 10, and the scan appeared under neither Posted nor Review.
  - Posting to Posted was not browser-tested, because it would create a real JV in Carmen dev.
- **User-visible, pending release note:** manual scans now show on the Credit Card page even
  when they were not posted, so every credit spent is visible, and the email-automation
  pitch is gone from the page.
