React Doctor v0.5.7



  ✖ Bugs: Hook called conditionally ×2
    `useAuth` changes Hook order between renders when called
    conditionally, so React can attach state to the wrong Hook.
    → Call hooks at the top level of a React function component
    or custom Hook so React sees the same hook order on every
    render.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rules-of-hooks

    src/components/common/ProtectedRoute.tsx:20-22
    ┌──────────────────────────────────────────────────────────────┐
    │   19 |   // eslint-disable-next-line react-hooks/rules-of-h… │
    │ > 20 |   const { isAuthenticated, loading } = useAuth() as … │
    │ > 21 |   // eslint-disable-next-line react-hooks/rules-of-h… │
    │ > 22 |   const { exchanging, error } = useCarmenSSO()        │
    │   23 |                                                       │
    └──────────────────────────────────────────────────────────────┘
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/rules-of-hooks` in your eslint-disable comment does not silence `react-doctor/rules-of-hooks` — change it to `react-doctor/rules-of-hooks`.

  ⚠ Security: HTML injection sink with dynamic content ×2
    HTML is injected from a dynamic-looking source, which can
    become XSS if the value is user-controlled or unsanitized.
    → Prefer rendering structured React nodes. If HTML is
    required, sanitize with a well-reviewed sanitizer and keep
    the trust boundary close to the sink.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/dangerous-html-sink

    src/components/credit-card/DetailTable.tsx:69

    src/components/credit-card/HeaderCard.tsx:27

  ⚠ Bugs: Array index used as a key ×8
    Your users can see & submit the wrong data when this list
    reorders or filters, so use a stable id like
    `key={item.id}`, not the array index "i".
    → Use a stable id from the item, like `key={item.id}` or
    `key={item.slug}`. Index keys break when the list reorders
    or filters.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-array-index-as-key

    src/components/ap-invoice/APReviewStep.tsx:275

    src/components/ap-invoice/APGroupModal.tsx:132

    src/components/credit-card/JournalVoucher.tsx:132

    src/components/common/PDFPageSelector.tsx:194

    src/components/credit-card/AccountingReview.tsx:233

    src/components/admin/MetricChart.tsx:75

    src/components/common/CustomSearchSelect.tsx:280

    src/pages/admin/ErrorsPage.tsx:112

  ⚠ Bugs: Missing effect dependencies ×7
    `useEffect` can run with a stale `vendor.loadVendors` &
    show your users old data.
    → Don't blindly add missing dependencies. Read the hook
    callback first.

    Bad:
    useEffect(() => {
      setCount(count + 1);
    }, [count]);

    Better:
    useEffect(() => {
      setCount((currentCount) => currentCount + 1);
    }, []);

    If the missing value is recreated every render, move it
    inside the hook or stabilize it before adding it to deps.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/exhaustive-deps

    src/hooks/ap-invoice/useAPInvoice.ts:59
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/hooks/ap-invoice/useAPInvoice.ts:65
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/hooks/ap-invoice/useAPInvoice.ts:70
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/hooks/ap-invoice/useAPInvoice.ts:107
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/hooks/ap-invoice/useAPInvoice.ts:259
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/components/common/CustomModal.tsx:88
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

    src/hooks/mapping/useMapping.ts:92
    ↳ oxlint matches plugin rules only by their full name, so `react-hooks/exhaustive-deps` in your eslint-disable comment does not silence `react-doctor/exhaustive-deps` — change it to `react-doctor/exhaustive-deps`.

  ⚠ Security: iframe missing sandbox attribute ×2
    An `<iframe>` with no `sandbox` is a security hole: the
    embedded page gets full access to your site.
    → Add `sandbox=""` or a curated value so embedded pages
    cannot get full access to your site by default.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/iframe-missing-sandbox

    src/components/common/DocumentPreview.tsx:248

    src/pages/admin/CreditOrdersPage.tsx:102

  ⚠ Bugs: Sequential independent awaits
    This await doesn't use the previous result, so your users
    wait twice as long for nothing.
    → These two awaits don't depend on each other. Wrap them in
    `Promise.all([...])` so they run at the same time.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/server-sequential-independent-await

    src/lib/pdfPages.ts:12

  ⚠ Accessibility: Label missing associated control ×2
    Screen reader users can't tell which input this label names
    because it's tied to none, so add `htmlFor` or wrap the
    input inside it.
    → Tie every label to a control with `htmlFor`, or by
    nesting the input.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/label-has-associated-control

    src/components/credit-card/TopLevelConfigSection.tsx:89

    src/components/credit-card/HeaderCard.tsx:27

  ⚠ Bugs: Derived state stored in an effect ×2
    You pay an extra render for state you can derive from other
    values.
    → Work out derived values while rendering: `const x =
    fn(dep)`. To reset a component's state when a prop changes,
    give it a key prop: `<Component key={prop} />`. See
    https://react.dev/learn/you-might-not-need-an-effect
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-derived-state-effect

    src/components/common/CustomSearchSelect.tsx:39

    src/hooks/useUserConsent.ts:30

  ⚠ Performance: await inside a loop
    This makes the for…of loop slow because each await runs one
    after another, so collect the independent calls & run them
    together with `await Promise.all(items.map(...))`
    → Collect the items, then use `await
    Promise.all(items.map(...))` so independent work runs at
    the same time
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/async-await-in-loop

    src/lib/imagesToPdf.ts:64

  ⚠ Maintainability: Component rendered by inline function call
    Your users lose state because "renderExpandedRow()" builds
    UI from an inline call that React remounts, so pull it into
    its own component instead.
    → Make it a named component so React preserves its identity
    and does not remount its state.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-render-in-render

    src/components/admin/DataTable.tsx:167

  ⚠ Performance: setState reads a stale value
    You can lose this update because setStep(step - ...) reads
    a stale value.
    → Use the callback form: `setState(prev => prev + 1)` to
    always read the latest value
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-functional-setstate

    src/hooks/credit-card/useOcrWizard.ts:286

  ⚠ Performance: Unstable context provider value ×3
    Every reader of this context redraws on each render because
    you build its `value` inline.
    → Wrap the context value in `useMemo` or move it outside
    the component so consumers do not redraw every render.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/jsx-no-constructed-context-values

    src/contexts/AuthContext.tsx:123

    src/contexts/AdminAuthContext.tsx:58

    src/i18n/LanguageContext.tsx:47

  ⚠ Performance: Non-passive scroll listener ×4
    "touchstart" listener without { passive: true } makes
    scrolling janky for your users. Only add it if the handler
    doesn't call event.preventDefault(), since passive
    listeners silently ignore preventDefault().
    → Add `{ passive: true }` as the third argument:
    `addEventListener('scroll', handler, { passive: true })`.
    Only do this if the handler doesn't call
    `event.preventDefault()`, since passive listeners ignore it
    (which breaks pull-to-refresh, custom gestures, and nested
    scrolling).
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/client-passive-event-listeners

    src/components/common/InlineSelect.tsx:83

    src/components/common/DocumentPreview.tsx:110-112

    src/components/common/CustomSearchSelect.tsx:70

  ⚠ Performance: Heavy library loaded eagerly
    "recharts" ships extra code to your users up front & slows
    page load. Load it on demand with React.lazy() or
    next/dynamic.
    → Load it only when needed: `const Component = dynamic(()
    => import('library'), { ssr: false })` from next/dynamic,
    or React.lazy().
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-dynamic-import

    src/components/admin/MetricChart.tsx:1

  ⚠ Accessibility: Control missing accessible label ×21
    Blind users can't tell what this control does because
    screen readers find no label, so add visible text,
    `aria-label`, or `aria-labelledby`.
    → Give every interactive control a label screen readers can
    read.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/control-has-associated-label

    src/components/credit-card/DetailTable.tsx:69

    src/components/admin/DataTable.tsx:102

    src/components/credit-card/TopLevelConfigSection.tsx:56

    src/components/credit-card/TopLevelConfigSection.tsx:77

    src/components/credit-card/TopLevelConfigSection.tsx:90

    src/components/ap-invoice/APLineItemsTable.tsx:346-347

    src/components/ap-invoice/APLineItemsTable.tsx:354-363

    src/components/common/BugReportButton.tsx:148

    src/components/credit-card/CompanyInfoSection.tsx:63

    src/components/common/CustomSearchSelect.tsx:156

    src/components/ap-invoice/APVendorSearch.tsx:110

    src/pages/admin/PerformancePage.tsx:78

    src/pages/admin/SessionsPage.tsx:68

    src/pages/admin/CreditOrdersPage.tsx:130

  ⚠ Accessibility: outline:none removes focus ring
    Your keyboard users can't tell where they are because
    outline: none hides the focus ring, so style :focus-visible
    instead, or add a box-shadow focus ring.
    → Style `:focus-visible { outline: 2px solid
    var(--color-accent); outline-offset: 2px }` so the focus
    ring shows for keyboard users but not mouse clicks.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-outline-none

    src/components/common/CustomSearchSelect.tsx:190

  ⚠ Bugs: Multiple setState calls in one effect ×15
    5 setState calls in one useEffect redraw your screen each
    time they run together.
    → Combine related updates in `useReducer` so one effect
    does not redraw the screen once per `setState` call.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-cascading-set-state

    src/hooks/credits/useCheckout.ts:81

    src/hooks/credit-card/useOcrWizard.ts:95

    src/components/common/DocumentPreview.tsx:41

    src/components/common/CustomSearchSelect.tsx:43

    src/hooks/credit-card/useAccountingConfig.ts:88

    src/components/pricing/FeatureFlows.tsx:29

    src/pages/admin/ErrorsPage.tsx:25

    src/pages/admin/Overview.tsx:23

    src/pages/admin/LLMLogsPage.tsx:61

    src/pages/admin/PerformancePage.tsx:61

    src/pages/admin/JobsPage.tsx:68

    src/pages/admin/TenantRankingPage.tsx:54

    src/hooks/useCarmenSSO.ts:16

    src/pages/admin/CreditOrdersPage.tsx:43

    src/pages/admin/UsagePage.tsx:54

  ⚠ Bugs: Derived value copied into state ×6
    Storing "filePrefix" in state when you can derive it from
    other values costs an extra render.
    → Work out the value while rendering (or with useMemo if
    it's expensive) instead of copying it into useState through
    a useEffect. See
    https://react.dev/learn/you-might-not-need-an-effect#updating-state-based-on-props-or-state
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-derived-state

    src/hooks/credit-card/useOcrWizard.ts:102-103

    src/hooks/credit-card/useOcrWizard.ts:107

    src/components/common/CustomModal.tsx:65

    src/components/common/CustomSearchSelect.tsx:40

    src/hooks/useUserConsent.ts:31

  ⚠ Performance: Full Framer Motion import ×13
    Importing "motion" ships about 30 kb of extra code and
    slows page load. Use "m" with LazyMotion instead.
    → Use `import { LazyMotion, m } from "framer-motion"` with
    `domAnimation` features. Saves about 30kb.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/use-lazy-motion

    src/components/ap-invoice/APGroupModal.tsx:3

    src/components/common/StepWizard.tsx:2

    src/components/common/CustomModal.tsx:3

    src/components/common/LoadingOverlay.tsx:2

    src/components/common/UsageIndicator.tsx:2

    src/components/common/PDFPageSelector.tsx:3

    src/components/common/UserConsentModal.tsx:3

    src/components/pricing/CheckoutFlow.tsx:2

    src/components/pricing/FeatureFlows.tsx:2

    src/pages/CreditCardOCR.tsx:3

    src/pages/APInvoice.tsx:2

    src/pages/Pricing.tsx:2

    src/pages/Home.tsx:2

  ⚠ Bugs: All state reset on prop change ×2
    Your users briefly see stale state when a prop changes
    because this useEffect clears all state.
    → Pass the prop as `key` so React resets the component for
    you when the prop changes, instead of clearing every state
    value by hand in a useEffect. See
    https://react.dev/learn/you-might-not-need-an-effect#resetting-all-state-when-a-prop-changes
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-reset-all-state-on-prop-change

    src/components/ap-invoice/APGroupModal.tsx:25

    src/components/common/UserConsentModal.tsx:88

  ⚠ Accessibility: Role used instead of HTML tag ×9
    Screen reader users get more reliable semantics from
    `<address>` than `role="group"`, so use `<address>` instead.
    → Use the matching HTML element when one exists so browsers
    and assistive tech get native semantics.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-tag-over-role

    src/components/ap-invoice/APGroupModal.tsx:122

    src/components/pricing/SlipUpload.tsx:94

    src/components/common/InlineSelect.tsx:172

    src/components/common/BugReportButton.tsx:113

    src/components/common/PDFPageSelector.tsx:167

    src/components/common/PDFPageSelector.tsx:195

    src/components/common/UserConsentModal.tsx:159

    src/components/admin/MetricChart.tsx:49

    src/components/pricing/UsageSummaryStrip.tsx:39

  ⚠ Accessibility: Interaction on static element ×10
    Screen reader users can't tell this click handler is
    interactive because it has no `role`, so add a `role` or
    use a button or link.
    → Give clickable static elements a `role`, or use a button
    or link.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-static-element-interactions

    src/components/credit-card/PaymentTypeModal.tsx:71

    src/components/ap-invoice/APUploadStep.tsx:57

    src/components/common/BugReportButton.tsx:114

    src/components/common/DocumentPreview.tsx:224

    src/components/credit-card/AccountMappingTable.tsx:112

    src/components/credit-card/UploadSection.tsx:50

    src/components/common/CustomSearchSelect.tsx:207

    src/components/common/CustomSearchSelect.tsx:279

    src/components/ap-invoice/APVendorSearch.tsx:134

    src/pages/Pricing.tsx:41

  ⚠ Performance: Array lookup inside a loop ×4
    This scales poorly because `array.includes()` inside a loop
    scans the whole list every time. Use a Set for
    constant-time lookups.
    → Use a `Set` or `Map` when you check for the same items
    over and over. `Array.includes`/`find` scans the whole list
    each time
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-set-map-lookups

    src/hooks/ap-invoice/useAPExtraction.ts:105-108

  ⚠ Maintainability: deslop/unused-file
    Unused file is not reachable from any entry point, so it
    adds maintenance surface without shipping any code.
    → Delete the file if it is truly unreachable, or import it
    from an entry point.

    src/components/ap-invoice/index.ts

  ✖ Bugs: State synced to a prop inside an effect ×13
    This effect adjusts state after a prop changes, so users
    briefly see the stale value.
    → Adjust the state inline during render with a `prev`-prop
    comparison (`if (prop !== prevProp) { setPrevProp(prop);
    setX(...); }`), or refactor to remove the duplicated state.
    Routing the adjustment through a useEffect forces an extra
    render with a stale UI between the two commits. See
    https://react.dev/learn/you-might-not-need-an-effect#adjusting-some-state-when-a-prop-changes
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-adjust-state-on-prop-change

    src/components/ap-invoice/APGroupModal.tsx:27-28
    ┌──────────────────────────────────────────────────────────────┐
    │   26 |     if (show) {                                       │
    │ > 27 |       setDesc('')                                     │
    │ > 28 |       setSelected(new Set())                          │
    │   29 |     }                                                 │
    └──────────────────────────────────────────────────────────────┘

    src/hooks/credits/useCheckout.ts:84
    ┌──────────────────────────────────────────────────────────────┐
    │   83 |     let alive = true                                  │
    │ > 84 |     setLoadingProfile(true)                           │
    │      |     ^                                                 │
    │   85 |     getCompanyProfile()                               │
    └──────────────────────────────────────────────────────────────┘

    src/components/common/CustomModal.tsx:71-72
    ┌──────────────────────────────────────────────────────────────┐
    │   70 |     if (show) {                                       │
    │ > 71 |       setInputErrored(false)                          │
    │ > 72 |       setRevealed(false)                              │
    │   73 |     }                                                 │
    └──────────────────────────────────────────────────────────────┘

    src/components/common/CustomModal.tsx:80
    ┌──────────────────────────────────────────────────────────────┐
    │   79 |     if (!errorNonce) return                           │
    │ > 80 |     setInputErrored(true)                             │
    │      |     ^                                                 │
    │   81 |     if (!reduceMotion) {                              │
    └──────────────────────────────────────────────────────────────┘

    src/components/common/DocumentPreview.tsx:42-44
    ┌──────────────────────────────────────────────────────────────┐
    │   41 |   useEffect(() => {                                   │
    │ > 42 |     setZoom(1)                                        │
    │ > 43 |     setRotate(0)                                      │
    │ > 44 |     setPan({ x: 0, y: 0 })                            │
    │   45 |   }, [previewUrl])                                    │
    └──────────────────────────────────────────────────────────────┘

    src/components/common/UserConsentModal.tsx:90-91
    ┌──────────────────────────────────────────────────────────────┐
    │   89 |     if (show) {                                       │
    │ > 90 |       setChecked(false)                               │
    │ > 91 |       setLang('en')                                   │
    │   92 |     }                                                 │
    └──────────────────────────────────────────────────────────────┘

    src/components/pricing/FeatureFlows.tsx:31
    ┌──────────────────────────────────────────────────────────────┐
    │   30 |     if (!active) {                                    │
    │ > 31 |       setPhase(0)                                     │
    │      |       ^                                               │
    │   32 |       return                                          │
    └──────────────────────────────────────────────────────────────┘

    src/components/pricing/FeatureFlows.tsx:43
    ┌──────────────────────────────────────────────────────────────┐
    │   42 |     const timers: number[] = []                       │
    │ > 43 |     setPhase(1)                                       │
    │      |     ^                                                 │
    │   44 |     timeline.forEach((t, i) => timers.push(window.se… │
    └──────────────────────────────────────────────────────────────┘

  ⚠ Accessibility: Text is too small ×6
    Your users strain to read 11.52px text, so use at least
    12px for body text, & 16px is best.
    → Use at least 12px for body text, and 16px is best. Small
    text is hard to read, especially on phones.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-tiny-text

    src/components/common/PDFPageSelector.tsx:133

    src/components/common/PDFPageSelector.tsx:257

    src/components/common/PDFPageSelector.tsx:307

    src/components/common/CustomSearchSelect.tsx:250

    src/components/common/CustomSearchSelect.tsx:266

    src/components/ap-invoice/APVendorSearch.tsx:163

  ⚠ Performance: Listener re-subscribes on every handler change ×2
    useEffect re-adds the "close" listener every time the
    handler changes.
    → Store the handler in a ref and have the listener read
    `handlerRef.current()`. The subscription stays put while
    the latest handler still runs.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/advanced-event-handler-refs

    src/components/common/InlineSelect.tsx:70

    src/components/common/DocumentPreview.tsx:107

  ⚠ Performance: State only used in handlers
    Each update to "dragStart" redraws your component for
    nothing because this useState is set but never shown on
    screen.
    → Use useRef instead of useState when the value is only set
    and never shown on screen. `ref.current = ...` updates it
    without redrawing the component.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-state-only-in-handlers

    src/components/common/DocumentPreview.tsx:37

  ⚠ Bugs: Button missing explicit type ×5
    Your users can submit the form by accident because a
    `<button>` with no `type` defaults to submit.
    → Set an explicit button `type` so plain buttons do not
    submit forms by accident: `type="button"`, `"submit"`, or
    `"reset"`.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/button-has-type

    src/components/credit-card/ActionBar.tsx:12

    src/components/credit-card/BankDetectionBanner.tsx:61

    src/components/credit-card/BankDetectionBanner.tsx:77

    src/components/pricing/FeatureFlows.tsx:382

    src/pages/admin/SessionsPage.tsx:105

  ⚠ Bugs: Event logic handled in an effect ×7
    Faking an event handler with a prop plus a useEffect costs
    an extra render & runs late.
    → Run the side effect in the event handler that triggers
    it, instead of watching its state from a useEffect. See
    https://react.dev/learn/you-might-not-need-an-effect#sharing-logic-between-event-handlers
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-event-handler

    src/components/ap-invoice/APGroupModal.tsx:26

    src/hooks/ap-invoice/useAPInvoice.ts:68

    src/hooks/ap-invoice/useAPInvoice.ts:97

    src/hooks/ap-invoice/useAPInvoice.ts:244

    src/components/common/CustomModal.tsx:65

    src/components/common/CustomModal.tsx:70

    src/components/common/UserConsentModal.tsx:89

  ⚠ Bugs: State updates chained through effects ×13
    Chaining state updates triggers an extra render each step.
    → Set all the related state together in the event handler
    that starts it, instead of having one useEffect react to a
    state change and set more state. See
    https://react.dev/learn/you-might-not-need-an-effect#chains-of-computations
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-chain-state-updates

    src/components/common/DateInput.tsx:62

    src/pages/admin/ErrorsPage.tsx:26

    src/pages/admin/Overview.tsx:27

    src/pages/admin/LLMLogsPage.tsx:62

    src/pages/admin/PerformancePage.tsx:62

    src/pages/admin/JobsPage.tsx:69

    src/pages/admin/TenantRankingPage.tsx:55

    src/pages/admin/SessionsPage.tsx:23

    src/pages/admin/AnomaliesPage.tsx:26

    src/pages/admin/CreditsPage.tsx:54-58

    src/pages/admin/UsagePage.tsx:55

  ⚠ Bugs: State initialized from a mount effect ×7
    Your users see an extra render with empty "profilesLoading"
    because a useEffect sets its starting value.
    → Pass the initial value directly to useState() instead of
    setting it from a mount-only useEffect. For SSR hydration,
    prefer useSyncExternalStore().
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-initialize-state

    src/components/credit-card/InputTaxReconciliation.tsx:46

    src/contexts/AdminAuthContext.tsx:28

    src/components/credit-card/AccountingReview.tsx:94

    src/components/pricing/UsageSummaryStrip.tsx:18

    src/hooks/mapping/useMapping.ts:110

    src/pages/admin/CreditOrdersPage.tsx:183-184

  ⚠ Accessibility: Click handler missing keyboard handler ×6
    Keyboard users can't trigger this click handler because
    there's no keyboard one, so add `onKeyUp`, `onKeyDown`, or
    `onKeyPress`.
    → Pair `onClick` with a key handler so keyboard users can
    trigger it.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/click-events-have-key-events

    src/components/credit-card/PaymentTypeModal.tsx:71

    src/components/ap-invoice/APUploadStep.tsx:57

    src/components/common/BugReportButton.tsx:114

    src/components/credit-card/AccountMappingTable.tsx:112

    src/components/credit-card/UploadSection.tsx:50

    src/pages/Pricing.tsx:41

  ⚠ Performance: Empty default prop breaks memo ×5
    This keeps redrawing children that compare props because
    default prop value {} makes a brand new object every
    render, so move it to a constant at the top of the file
    → Move it to the top of the file: `const EMPTY_ITEMS:
    Item[] = []`, then use that as the default value
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-memo-with-default-value

    src/components/credit-card/AccountingReview.tsx:81

    src/components/ap-invoice/APAccountMappingStep.tsx:136-140

  ⚠ Performance: Import from a barrel file ×13
    This ships extra code to your users & slows page load.
    Import directly from "../../constants/fields".
    → Import from the direct path: `import { Button } from
    './components/Button'` instead of `./components`
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-barrel-import

    src/components/credit-card/DetailTable.tsx:3

    src/components/credit-card/InputTaxReconciliation.tsx:20

    src/hooks/credit-card/useOcrExtraction.ts:3

    src/components/credit-card/AccountingReview.tsx:20

    src/components/credit-card/HeaderCard.tsx:2

    src/components/pricing/CheckoutFlow.tsx:8

    src/components/credit-card/BankDetectionBanner.tsx:3

    src/pages/CreditCardOCR.tsx:4

    src/pages/Mapping.tsx:5

    src/pages/APInvoice.tsx:4

    src/pages/Pricing.tsx:7

    src/pages/Home.tsx:6

    src/pages/OrderHistory.tsx:7

  ⚠ Maintainability: Static value rebuilt every render ×2
    `WIDTHS` inside `DataTable` uses no local state but is
    rebuilt every render, so it looks new each time & breaks
    memoized children. Move it to the top of the file, outside
    the component.
    → Move the value above the component, at the top of the
    file. It doesn't use local state, so rebuilding it each
    update is wasted and makes it look new every time.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-module-scope-static-value

    src/components/admin/DataTable.tsx:73

    src/hooks/mapping/useMapping.ts:172

  ⚠ Accessibility: Custom modal instead of dialog
    Keyboard users can tab out of this `role="dialog"` modal
    because it has no built-in focus trapping, so use the
    native `<dialog>`, which gives you focus trapping, `Escape`
    to close, and the backdrop for free.
    → Replace the wrapper with `<dialog>` and open it with
    `dialog.showModal()`. For the trigger, prefer `<button
    commandfor="id" command="show-modal">` (Chrome 135+), or a
    `useRef` with `dialogRef.current?.showModal()`.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-html-dialog

    src/components/common/BugReportButton.tsx:113

  ⚠ Maintainability: deslop/unused-export ×11
    Unused export: `default` is exported but no module imports
    it, so it expands the public surface and can mislead
    callers about supported API.
    → Drop the `export` keyword (or remove the declaration) if
    no other module uses this symbol.

    src/components/ap-invoice/TaxTypeDropdown.tsx:17

    src/constants/billing.ts:48

    src/constants/billing.ts:103

    src/constants/billing.ts:118

    src/hooks/ap-invoice/useAPValidation.ts:42

    src/lib/api/adminClient.ts:23

    src/lib/api/credits.ts:95

    src/lib/bankTransforms.ts:21

    src/lib/bankTransforms.ts:30

    src/lib/bankTransforms.ts:34

    src/lib/bankTransforms.ts:38

  ⚠ Performance: flushSync skips View Transitions
    `flushSync` forces an immediate update, which skips View
    Transitions and concurrent rendering.
    → flushSync forces an immediate update that skips View
    Transitions and concurrent rendering. Use startTransition
    for updates that are not urgent.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-flush-sync

    src/i18n/LanguageContext.tsx:2

  ⚠ Accessibility: Autofocus on an element
    `autoFocus` moves focus on load, which can disrupt screen
    reader and keyboard users. Remove it and let users choose
    where to focus.
    → Do not use `autoFocus`. It disorients users on load.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-autofocus

    src/pages/admin/CreditOrdersPage.tsx:137

  ⚠ Maintainability: Large inline style object rebuilds every render ×15
    This inline style has 8 properties, which is hard to read &
    rebuilds every render. Move it to a CSS class, CSS module,
    or styled component.
    → Move the styles to a CSS class, CSS module, Tailwind
    utilities, or a styled component. Big inline objects are
    hard to read and rebuild on every update.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-inline-exhaustive-style

    src/components/common/PDFPageSelector.tsx:60

    src/components/common/PDFPageSelector.tsx:76

    src/components/common/PDFPageSelector.tsx:141

    src/components/common/PDFPageSelector.tsx:208

    src/components/common/PDFPageSelector.tsx:274

    src/components/common/ErrorBoundary.tsx:43

    src/components/common/ErrorBoundary.tsx:55

    src/components/common/ErrorBoundary.tsx:84

    src/components/common/ErrorBoundary.tsx:106

    src/components/common/ErrorBoundary.tsx:125

    src/components/common/CustomSearchSelect.tsx:177

    src/components/common/CustomSearchSelect.tsx:219

    src/components/ap-invoice/APVendorSearch.tsx:67

    src/components/ap-invoice/APVendorSearch.tsx:89

    src/pages/Mapping.tsx:167

  ⚠ Maintainability: Pure function rebuilt every render ×2
    `profileLabel` inside `APGroupModal` uses no local state
    but is rebuilt on every render, so it wastes work & breaks
    memoized children. Move it to the top of the file, outside
    the component.
    → Move the function above the component, at the top of the
    file. It doesn't use local state, so rebuilding it each
    update is wasted work.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-module-scope-pure-function

    src/components/ap-invoice/APGroupModal.tsx:41

    src/hooks/ap-invoice/useAPValidation.ts:121

  ⚠ Maintainability: Large component is hard to read and change ×4
    Component "InputTaxReconciliation" is 315 lines long, which
    is hard to read & change. Split it into a few smaller
    components.
    → Pull each section into its own component so the parent is
    easier to read, test, and change.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-giant-component

    src/components/credit-card/InputTaxReconciliation.tsx:31

    src/components/ap-invoice/APLineItemsTable.tsx:45

    src/components/common/PDFPageSelector.tsx:15

    src/components/common/CustomSearchSelect.tsx:24

  ⚠ Performance: .map().filter(Boolean) loops twice ×2
    This loops over your list twice because
    .map().filter(Boolean) makes two passes, so use .flatMap()
    to change & drop items in one pass
    → Use `.flatMap(item => condition ? [value] : [])` to
    change and drop items in one pass, instead of building a
    throwaway array in between
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-flatmap-filter

    src/hooks/ap-invoice/useAPInvoice.ts:467

    src/components/credit-card/AccountingReview.tsx:126

  ⚠ Performance: Chained array iterations ×11
    This loops over your list twice because .map().filter()
    makes two passes, so do it in one pass with .reduce() or a
    for...of loop
    → Combine `.map().filter()` style chains into one pass with
    `.reduce()` or a `for...of` loop, so you only loop over the
    list once
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-combine-iterations

    src/hooks/ap-invoice/useAPValidation.ts:116

    src/hooks/ap-invoice/useAPValidation.ts:136

    src/hooks/ap-invoice/useAPValidation.ts:168

    src/hooks/ap-invoice/useAPSubmission.ts:63

    src/hooks/ap-invoice/useAPSubmission.ts:72

    src/hooks/ap-invoice/useAPSubmission.ts:148

    src/lib/api/carmen.ts:50

    src/hooks/mapping/useMappingData.ts:42

    src/hooks/mapping/useMappingData.ts:52

    src/hooks/mapping/useMappingData.ts:70

  ⚠ Bugs: Unversioned localStorage key
    sessionStorage.setItem("ocr_user", JSON.stringify(...)) has
    no version, so changing the data shape later crashes your
    users' saved sessions. Add one to the key (e.g.
    "ocr_user:v1").
    → Put a version in the storage key (e.g. "myKey:v1"). If
    you change the data shape later, old saved data can be
    ignored instead of crashing the app.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/client-localstorage-no-version

    src/contexts/AuthContext.tsx:103

  ⚠ Performance: Transition duration too long
    Your users wait through a sluggish 1400ms transition, so
    keep UI transitions under 1000ms & save longer ones for big
    page-load animations.
    → Keep UI transitions under 1s. Use about 100 to 150ms for
    instant feedback, 200 to 300ms for state changes, and 300
    to 500ms for layout. Save longer ones for big page-load
    animations.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-long-transition-duration

    src/components/common/Skeleton.tsx:24

  ⚠ Bugs: Many related useState calls ×10
    6 useState calls in "InputTaxReconciliation" can each
    trigger a separate render.
    → Group related state in `useReducer` so one logical update
    does not fan out into separate renders.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-useReducer

    src/components/credit-card/InputTaxReconciliation.tsx:36

    src/components/pricing/PendingOrderBanner.tsx:27

    src/components/common/BugReportButton.tsx:31

    src/components/common/DocumentPreview.tsx:32

    src/pages/admin/ErrorsPage.tsx:18

    src/pages/admin/Overview.tsx:16

    src/pages/Pricing.tsx:112

    src/pages/admin/CreditsPage.tsx:42

    src/pages/admin/CreditOrdersPage.tsx:34

    src/pages/admin/UsagePage.tsx:47

  ⚠ Maintainability: Multiple components in one file ×4
    This file declares several components, so each component is
    harder to find, test, and change.
    → Move secondary components into their own files so each
    component stays easier to find, test, and change.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-multi-comp

    src/components/pricing/PlanCard.tsx:76

    src/components/pricing/PlanCard.tsx:107

    src/components/common/Skeleton.tsx:34

    src/components/common/Skeleton.tsx:54

  ⚠ Maintainability: Non-component export in component file
    This file exports non-components, so Fast Refresh can't
    safely preserve component state.
    → Move non-component exports out of component files so Fast
    Refresh can preserve component state instead of
    full-reloading.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/only-export-components

    src/components/common/NumericInput.tsx:11

  ⚠ Performance: Animating scale from zero
    This looks abrupt to your users because scale: 0 pops the
    element in from a single point, so use scale: 0.95 with
    opacity: 0 for a smoother entrance
    → Use `initial={{ scale: 0.95, opacity: 0 }}`. Elements
    should gently shrink and fade, not vanish into a point
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-scale-from-zero

    src/components/common/StepWizard.tsx:73

  ⚠ Performance: JSON parse/stringify deep clone ×3
    `JSON.parse(JSON.stringify(x))` deep-clones by
    re-serializing: it is slow on large objects and silently
    drops `undefined`, functions, `Date`/`Map`/`Set`, and
    cyclic references. Use `structuredClone(x)`.
    → Replace `JSON.parse(JSON.stringify(value))` with
    `structuredClone(value)`. It is faster and preserves Dates,
    Maps, Sets, and cyclic references.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-json-parse-stringify-clone

    src/hooks/credit-card/useOcrExtraction.ts:183-184

    src/hooks/mapping/usePaymentTypes.ts:100


  ────────────────────────────────────────────────────────────

  All 268 issues

  Security › 4 warnings
  Bugs › 15 errors, 84 warnings
  Performance › 67 warnings
  Accessibility › 57 warnings
  Maintainability › 41 warnings

Agent guidance
  - Treat React Doctor diagnostics as starting hypotheses. Read the relevant code before confirming or suppressing each finding.
  - For each group, decide true positive, false positive, or needs-human-review, then assign high/medium/low confidence.
  - Do not suppress a finding without evidence from the file in question. Confidence requires code context.
  - Understand the root cause before editing. Fix the underlying code instead of changing react-doctor config or suppressing rules unless explicitly asked.
  - Investigate deeply where relevant: race conditions, security-sensitive flows, state propagation, multi-file refactors, and downstream dependency chains.
  - Ignore pure style preferences, theoretical issues without real impact, missing features, and unrelated pre-existing code.
  - Start with high-confidence fixes that preserve behavior. Leave low-confidence or product-dependent changes as notes.
  - Run `npx react-doctor@latest --verbose --scope changed` before and after changes, plus relevant tests after each focused batch.
  - When available, spawn subagents or isolated worktrees for independent rule families, then review and merge only the best safe fixes.
  - Split unrelated, broad, or behavior-changing work into separate PRs/branches instead of one large cleanup.
  - When one rule spans dozens of files (a migration-scale change), fix a representative sample first, confirm the recipe holds, and get the code owner's sign-off before changing the rest. Don't mass-fix a broad pattern in one unreviewed pass.
  - For confirmed issues that cannot be fixed now, create GitHub issues with the rule, file/line, confidence, impact, and proposed fix.
  - If a fix needs an API, UX, or architecture decision, stop and ask before editing.

  ┌─────┐  36 / 100 Critical
  │ x x │  ██████████████████▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░
  │  ▽  │  React Doctor (https://react.doctor)
  └─────┘

  You could improve +9% by fixing the top 3 issues
  Full diagnostics written to C:\Users\User\AppData\Local\Temp\react-doctor-ea45e1de-53f4-4b5a-bba8-3a81c86c954c

  ────────────────────────────────────────────────────────────

  Share: https://react.doctor/share?p=ocr-frontend&s=36&e=15&w=253&f=83
  Tell others how you did on socials

  Docs: https://react.doctor/docs
  Learn more about fixing issues, setting up CI/CD, and
  configuring rules with a config file

  GitHub: https://github.com/millionco/react-doctor
  Report issues and star the repository!
